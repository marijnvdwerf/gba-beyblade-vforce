# Gameloop

## sub_80520F4 (0x080520F4)

- The matched body requires four wide ABI parameters and two callback fields at offsets 0x34 and 0x78.
- `arg0` needs a halfword field at offset 0x40: a word declaration emitted `ldr`, while `unk16` emitted the target `add; ldrh` sequence. Its offset 0x18 field is word-sized and remains live across `sub_8052180` and the first callback.
- Inlining `callbackArg` into the first call changes bytes when the callback locals remain; calling `arg1->unk34` and `arg1->unk78` directly is byte-identical, and all three folds together are byte-identical.
- Final `sub_80520F4` diff has no differing instructions; full US compare passed with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
