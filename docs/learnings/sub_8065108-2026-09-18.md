# sub_8065108 — 2026-09-18

Matched 0x08065108 in src/spritestring.c, 0x36 bytes. US and EU full-ROM SHA1 comparisons pass after formatting.

The function counts actors whose unk70 field is nonzero. The count and loop index are unk16, matching the lsl/lsr #16 pairs after increment. Its unk8 return matches the final lsl/lsr #24 normalization and the sole assembly caller in sub_806513E, which normalizes the returned byte before comparing against string->count. There are no C callers and no new header declaration is needed.

A plain while loop with an Actor pointer computed per iteration matches every instruction. Folding that local into string->actors[index].unk70 swaps the mov #0xC4 and actor-array load at +0x0C/+0x0E, and changes the addition at +0x14. The record-pointer local is therefore byte-required. No field, signedness, layout, or artificial control-flow changes are needed.

The old 8065108.s contained both this function and sub_806513E. Moved the latter's complete unchanged assembly into the existing 806513e.s and removed 8065108.s. The non-word-aligned entry at 0x0806513E, including its initial lsl r0,r0,#0 instruction, is preserved. sub_806513E still has an exact function diff.

The expected snapshot already contains the original matching bytes; its shared symlink was not rewritten outside this worktree.
