# Review 2026-08-28

## sub_8048FFC

The queried target is the `unkEC` bit in the object pointed to by `FrontendState.transition.unk590.var0C`. The conforming source draft is:

```c
unk8 sub_8048FFC(void)
{
    if ((_unk3000650.transition.unk590.var0C->unkEC & 1) != 0) {
        return 1;
    }
    return 0;
}
```

This draft's first generated instruction loads `_unk3000650` and derives the nested address with a literal offset (`ldr _unk3000650`, `ldr 1436`, `add`), while the target loads `_unk3000BE0` directly and then performs `ldr [r0,#12]`. The direct-address form required a separate view type, which is not a valid representation of the shared FrontendState storage. The conforming draft is parked under `#if 0`; the original function is retained from the assembly dump. The reconstructed dump matches the expected object exactly.

## sub_8043558

`MenuState.objectCount` is `s32`, as required by the signed loop compare. The direction temporary and `_8068868` table are `s32`; the target materializes `-1` with `mov #1` followed by `neg`. Braces were added around the single-statement branch without changing code generation. The only diff display is the relocation spelling for `_8068868`; the ROM comparison passes.

## sub_80436B0

The countdown source form `while (count-- != 0)` emits the same target as the prior explicit sentinel spelling. Braces were added around the early return without changing code generation.

## sub_805024C

Removing the unnecessary `(s16)` cast from the first XOR preserves the target. Replacing `(u32)(arg0->prefix.words.unk6 << 22) >> 22` with `arg0->prefix.words.unk6 & 0x3FF` was rejected: agbcc loads `0x3FF` from the literal pool and emits a different instruction sequence. Replacing `(u32)(arg0->unkD << 28) >> 28` with `arg0->unkD & 0xF` also changed the target normalization sequence. The shift forms remain.

## sub_80501F8

Replacing the `lsl #22` / `lsr #22` normalization with `& 0x3FF` was rejected because agbcc introduced a literal-pool load and an `and`, unlike the target. The shift form remains.

## sub_80516E0

The 8-byte record at `CurrentGameState + 0x544` is named `LevelSlot`. Its layout and accessor code generation are unchanged.
