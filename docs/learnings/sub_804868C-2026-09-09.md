# sub_804868C

Matched at 0x0804868C in src/festate.c. Reachable through the frontend table in asm/data12.s; the dispatcher passes FrontendState* and an event word. No rule-breaking shapes.

The existing draft omitted the second argument to sub_8050DF8. The target loads signed packet byte 4 into r1, compares it with menu.selection, and retains it for the call. Passing record->packet.unk4[0] reproduces that dataflow with the existing Packet type.

RAM globals 030005D0 and 030005D4 are sprite pointers; 030005D8 and 030005DC are signed positions, proven by subtraction followed by asr #3. Definitions retain their four-byte extents. The menu descriptor is declared as const FrontendMenuData.

Measured source-shape trials:

- Staging sub_8057C40 in a phase local adds a mov after the call in each sprite update. Inline calls in the indexed expression match.
- Reading menu->selection through the cached menu alias retains its register and omits the target 0x4B4 address construction. Direct state->menu.selection restores it.
- An early negative connection guard in case 2 emits the offline branch ahead of the connected path. A positive connected branch with the offline else arm emits the target ordering.
- One post-switch packet-send call leaves the send site after the offline arm. Separate calls in cases 0, 1, and the connected case 2 arm merge to the target shared send site.
- Folding the menu alias is byte-identical and was retained. Folding either packet alias changes bytes. Folding the sprite allocation result into its global assignment changes bytes; the local sprite is retained.

Final clang-format and cmake --build build --target compare pass. Instruction diff is exact. ROM SHA1: cd527c8c24e20e33913fc45199e64b3e6138a6e5.

The requested five-arm switch fold was tested as `_currentGameState->unk6A5 = state->menu.selection * 2 + 1`. It was byte-required: the first divergence was at function offset `0x252` (`ldr r2, [pc, #136]` in the target versus `ldr r2, [pc, #224]` after the fold). The original switch is retained.
