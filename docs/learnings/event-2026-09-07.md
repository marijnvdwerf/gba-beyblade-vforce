# Event handlers 2026-09-07

## processMetadata_3 (0x080544BC)

- The ROM handler table dispatcher passes five arguments: `LevelGeometryAddresses*`, `GeometryLine*`, line index, `LineMetadata*`, and the selected `LineMetaObject*` as a stack argument. The implementation uses only the line index and selected object.
- `GetStruct4(lineIndex)` returns `EnvironmentObject*`; its field at offset `0x3E` is a halfword, so `EnvironmentObject` now names that field `unk3E` while preserving the existing layout with `pad40[0xC]`.
- The selected metadata object's `id` is compared against `0x37AE` and `0x4ECB`; the latter stores `event->unk8.word` to `target->unk3E`.
- The two literal `event->id` cases are expressed as a source-level sparse `switch`; agbcc emits the same compare chain as the prior if/else draft. To preserve target block and literal-pool order, the source cases are ordered `0x4ECB` then `0x37AE`, matching the target body order; the rebuilt function has no differing instructions.
- Folding `target` into `GetStruct4(lineIndex)->unk3E` was tested; the first divergence was at `0x00000000` (the folded form changed `push {r4, r5, lr}` to `push {r4, lr}`), so the `target` temporary is required for the target register/lifetime shape.
- No extra temporary beyond `target` survived comparison; `target` is required for the returned environment-object pointer and is allocated in `r2` as in the target.

## processMetadata_6 (0x08054248)

- The table callback receives five arguments; the fifth selected `LineMetaObject*` is loaded from `[sp, #8]` after the `{r4, lr}` prologue.
- The handler stages `event->id` in an `unk16` local across `handleEventListeners`, matching the target's `ldrh` into `r4`, then forwards that value to `sub_804FF88` when `_gameData->unk1618` is nonzero.
- Folding `id` into the two `event->id` uses was tested; the first divergence was at `0x00000002` (the folded form changed the stack-argument load/register sequence), and it also reloaded the halfword before `sub_804FF88`, so the `id` temporary must remain live across `handleEventListeners`.
- No alternate control-flow spelling was needed; all generated instructions match the dump exactly.

## processMetadata_10 (0x0805473C)

- The fifth callback argument is the selected `LineMetaObject*`, and `GetStruct4(lineIndex)->actor` is the actor pointer checked for null.
- The metadata payload is the existing `LineMetaTransform` union arm at `event->unk8.transform`; its x, y, and z words are each shifted left by eight and stored to `Actor` fields `x`, `y`, and `z` at offsets `0x4`, `0x8`, and `0xC`.
- Direct typed field expressions match the target's load/shift/store sequence without extra locals or casts.

## processMetadata_9 (0x080546A4)

- The fifth callback argument is the selected `LineMetaObject*`; `GetStruct4(lineIndex)->actor` supplies the nullable `Actor*` destination.
- The three payload words at offsets `0x8`, `0xC`, and `0x10` are the existing `LineMetaTransform` fields and store directly to `Actor` offsets `0x40`, `0x44`, and `0x48` (`unk40`, `unk44`, `unk48`).
- The typed actor pointer and direct transform-field stores match the target exactly without additional temporaries or casts.

## processMetadata_C (0x080546E8)

- The fifth callback argument is the selected `LineMetaObject*`; `GetStruct4(lineIndex)->actor` supplies the nullable `Actor*` destination.
- The existing `LineMetaTransform` payload fields x, y, and z store directly to `Actor` fields `unk4C`, `unk50`, and `unk54` at offsets `0x4C`, `0x50`, and `0x54`.
- Direct typed field expressions match the target's three word load/store pairs exactly without additional temporaries or casts.

## processMetadata_E (0x08054714)

- The fifth callback argument is the selected `LineMetaObject*`; `GetStruct4(lineIndex)->actor` supplies the nullable `Actor*` passed to `ActorSetSpriteOffset`.
- The payload uses the existing signed `LineMetaOffset` union arm: `event->unk8.offset.x` and `.y` produce the target's `ldsh` loads at offsets `0x8` and `0xC`.
- The direct typed call matches the target's argument setup and call sequence exactly without extra locals or casts.

## processMetadata_8 (0x08054684)

- The fifth callback argument is the selected `LineMetaObject*`; `GetStruct4(lineIndex)->actor` supplies the nullable `Actor*` passed to `actor_80580C0`.
- The payload's halfwords at offsets `0x8` and `0xA` are represented by the `LineMetaSequence` union arm and pass as the sequence and callback-sequence arguments.
- Direct typed field access produces the target's two `ldrh` loads and call exactly without extra locals or casts.

## processMetadata_A (0x080546C8)

- The fifth callback argument is the selected `LineMetaObject*`; `GetStruct4(lineIndex)->actor` supplies the nullable `Actor*` destination.
- The existing `LineMetaTransform` payload x and y words store directly to `Actor` fields `unk68` and `unk70` at offsets `0x68` and `0x70`.
- Direct typed field expressions match the target's two word load/store pairs exactly without additional temporaries or casts.

## processMetadata_default (0x08054388)

- The callback receives five arguments; only the line index is used.
- A bare `GetStruct4(lineIndex);` call is retained as a target-required discarded call even though `GetStruct4` has no side effect; removing it first diverged at `0x00000000` by changing the prologue from `push {r4, lr}` to `push {lr}`. The retained call is followed by `printf(Str_87296D8, lineIndex)`.
- The direct call sequence matches the target exactly without additional temporaries.

## processMetadata_D (0x0805470C)

- The callback uses the fifth `LineMetaObject*` argument and copies its payload byte at offset `0x8` to `GeometryLine.unk10` at offset `0x10`.
- A byte union arm `LineMetaObjectValue.unk0` represents this access; the target uses `ldrb` and `strb`, proving the accessed widths without changing the union size.
- Direct typed field access matches the four target instructions exactly.

## nullsub_14 (0x08054738)

- This ROM-table callback has a five-argument signature but ignores every argument, compiling to a single `bx lr`.
- The following `processMetadata_10` begins at the expected aligned address, so the target's trailing alignment bytes are preserved by the normal function emission.

## nullsub_15 (0x08054764)

- This ROM-table callback has a five-argument signature but ignores every argument, compiling to a single `bx lr`.
- The target has zero-filled alignment bytes after the function while agbcc's default file-end alignment emits `0x46C0`; a file-scope `.align 2, 0` restores the target trailing bytes.

## nullsub_42 (0x080543A4)

- This ROM-table callback has a five-argument signature but ignores every argument, compiling to a single `bx lr`.
- The following `processMetadata_1` begins at the expected aligned address, so no explicit padding adjustment is needed between the functions.

## processMetadata_2 (0x0805444C)

- The fifth handler argument is a `LineMetaObject*`; its `id` is checked against `0x524D` before `GetStruct4(lineIndex)` is called. The returned `EnvironmentObject` supplies the nullable `Actor*` at offset `0`.
- The actor setup uses the existing typed fields: preserve `unkB4`, optionally release `unkB8`, pass `event->unk8.config`, `actor->unk3C`, the three signed fixed-point coordinates shifted right by eight, and `actor->unk70` to `actor_8057C58`, then set `unkB0`, clear `unk68`, set `unkBC`, and restore `unkB4`.
- `Actor* actor` and `unk32 saved` are both required; removing either changes the target prologue or later address/lifetime sequence. The direct callback assignment `actor->unkB0 = convert3DCoordsto2DCoords + 1` matches without an explicit cast.
- The rebuilt function matches all target instructions and `compare` passed.

## Actor.unkB0 callback typing

- `convert3DCoordsto2DCoords` is defined as `void (Actor*, unk32*)`; `sub_8058754` and `renderRider` both call `unkB0` with exactly an `Actor*` and a three-word `unk32` output buffer. `ActorPositionFunc` now names that shared callback type in `common.h`, and both `Actor.unkB0` and the parked `RenderActorDraft.unkB0` use it.
- The parked `initRider` draft now stores `convert3DCoordsto2DCoords` directly at both prior casted sites. Its source is disabled, so no live object changed. `actor_8057C58`'s parked clear uses `NULL` rather than an integer temporary for the function-pointer field.
- Re-diffed every live `unkB0` user: `sub_8058754`, `renderRider`, `initLevelEnvironmentActors`, and `processMetadata_2`; each remained instruction-identical and the full ROM compare passed.
- For `processMetadata_2`, removing the explicit `+ 1` from the callback store changed the literal-pool relocation from `convert3DCoordsto2DCoords + 1` to `convert3DCoordsto2DCoords`; the target-required Thumb callback value therefore remains `actor->unkB0 = convert3DCoordsto2DCoords + 1` and type-checks without a cast. The two disabled `initRider` stores follow the requested plain function-pointer spelling.
