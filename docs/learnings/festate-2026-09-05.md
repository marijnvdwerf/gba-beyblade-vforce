# festate functions — 2026-09-05

## sub_8046A0C (0x08046A0C)

Matched in `src/festate.c`.

The callback signature is `void sub_8046A0C(FrontendState *state, unk32 arg1)`, from its frontend handler-table use. The state accesses at `state + 0x140` and `state + 0x250` are `FrontendState.unk140` and `FrontendState.unk250`.

The three fixed-address scroll/count globals `_unk30004B4`, `_unk30004B8`, and `_unk30004BC` are signed 32-bit values. The target uses arithmetic right shifts for the fixed-point position and delta, and signed comparisons against `0x78` and `0x258`.

The target's case-1 gate tests `_unk30004C1 != 0 && _unk30004C0 == 0`, then stores `arg1` to `_unk30004C0`. The later completion gate tests `_unk30004C1 == 0` and stores `1` to `_unk30004C1`.

The fixed-point position expression is `(s16)(-(_unk30004B8 >> 8) + 0x10)`. `sub_80515A4` returns `unk8`, producing the target's byte normalization before the language-table selection. The language table is indexed with rows `1`/`2` for the nonzero/zero result and row `0` for the final completion message.

Initial draft divergences were caused by reversed `_unk30004C0`/`_unk30004C1` roles and assignment. After correcting those roles, the function instruction diff and full-ROM comparison matched. The target literal pool represents the odd-address byte global `_unk30004C1` as the raw address `0x030004C1`; this is byte-identical to the final linked ROM.

## sub_8044054 (0x08044054)

Matched in `src/festate.c`.

The callback signature is `void sub_8044054(FrontendState *state, unk32 arg1)`, from the frontend handler table. The function calls `sub_8051734()` before dispatch and uses `FrontendState.unk140`, `FrontendState.unk250`, and `FrontendState.unk80`.

The ten-element `_unk3000180` table is `SpriteTextCleanup *[10]`. `_unk3000178` occupies eight bytes; its first word is the progress value accessed by this function. `_unk300016C` and `_unk3000170` are signed 32-bit fixed-point values. `_unk3000174` is kept as `unk32` because its comparisons use unsigned branches. `_unk30001A8` is a byte flag.

The case-0 source reuses `_unk3000170` as the negative argument to `sub_80596AC` after assigning `0x10000`. Writing `-_unk3000170` rather than the constant `-0x10000` reproduces both the target's `ldr`/`mov` ordering and the target's final dispatch shape (`beq` to case 2 followed by `b` to the epilogue). The direct global reuse also preserves the target's `r6` state pointer allocation.

The helper prototype for `sub_8043F40` has three arguments: the cleanup table, the `CurrentGameStateTail` pointer, and the signed scroll value. `sub_8043DB8` takes the cleanup table, `LevelState *`, `CurrentGameStateTail *`, and the combined progress value.
The shared `FrontendScalarWithPad` type is reused for `_unk3000178`; its `s32` field preserves the signed fixed-point uses of the other instances. In `sub_8044054`, the incremented field value is staged in an `unk32` local before the `0x20` comparison, preserving the target's unsigned `bls` while keeping the shared layout type.

## sub_8044C48 (0x08044C48)

Matched in `src/festate.c`.

The callback signature is `void sub_8044C48(FrontendState *state, unk32 arg1)`. The switch body order is cases `0`, `7`, `1`, and `2`, which reproduces the target's dispatch and layout. Case 0 initializes the score/result display, case 7 releases its fonts and sprite, case 1 performs the movement and scroll interpolation, and case 2 handles the transition trigger.

The fixed-address objects at `_unk3000204`, `_unk3000210`, `_unk3000218`, and `_unk3000248` are respectively `SpriteEntry *`, an 8-byte `FrontendScalarWithPad`, and two `SpriteTextCleanup` objects. `_unk3000208`, `_unk300020C`, and `_unk3000278` are signed 32-bit fixed-point values. The accessed current-game-state bytes are `unk6A6`, `unk6A7`, and `unk6A8`.

The case-1 text positions are computed into separate signed temporaries before conversion to the signed 16-bit `sub_8061844` arguments. The initial scroll value is retained as a local through the sprite-sheet call, matching the target's saved-register lifetime.

## sub_8044ED4 (0x08044ED4)

Matched in `src/festate.c`.

The callback signature is `void sub_8044ED4(FrontendState *state, unk32 arg1)`, from the frontend handler table. The switch body order is cases `0`, `7`, `1`, and `2`, matching the corresponding score/result callback layout. Case 0 initializes the result display, case 7 releases its fonts and sprite, case 1 interpolates the sprite, text, and scroll positions, and case 2 handles the transition trigger.

The fixed-address objects at `_unk300027C`, `_unk3000288`, `_unk3000290`, and `_unk30002C0` are respectively `SpriteEntry *`, an 8-byte `FrontendScalarWithPad`, and two `SpriteTextCleanup` objects. `_unk3000280`, `_unk3000284`, and `_unk30002F0` are signed 32-bit fixed-point values. The accessed current-game-state bytes are `unk6A6`, `unk6A7`, and `unk6A8`; initialization clears `GameData` bytes `unk1618`, `unk1619`, and `unk161A`.

The case-1 text positions use separate signed temporaries before conversion to the signed 16-bit `sub_8061844` arguments. The initial scroll value is retained through `LoadSpriteSheet`, and the first language-table lookup uses row `2` while the second uses row `0`. The sprite frame update advances `frame.word` every fourth timing interval and wraps values above `3` to zero.

## sub_8048AE8 (0x08048AE8)

Matched in `src/festate.c`.

The callback signature is `void sub_8048AE8(FrontendState *state, unk32 arg1, unk32 arg2)`, from the three-argument frontend object callback slot and `sub_80490CC` dispatch. The switch body order is cases `0`, `7`, `1`, `8`, and `2`, matching the target jump-table layout. Case 0 initializes three sprites and scroll state, case 7 releases the sprites, case 1 updates the scroll and delegates sprite animation, case 8 records the completion flag when `arg2 == 1`, and case 2 advances the multiplayer transition state.

The fixed-address storage at `_unk30005F0` is a 16-byte `FrontendSpriteTriple` containing three `SpriteEntry *` fields followed by two halfwords for state and timer. `_unk30005E0` and `_unk30005E2` are halfword counters, `_unk30005E4` and `_unk30005E8` are signed 32-bit fixed-point scroll targets, and `_unk30005EC` is a byte flag. The initialization and transition paths clear `GameData` bytes `unk1618`, `unk1619`, and `unk161A`.

The initial scroll value must be retained in a local after assigning `_unk30005E8`; this produces the target's global-address load before the `0x10000` materialization and reuses the value for the negative scroll call. The timer increment compares the stored halfword against `0x3E7` using the target's shifted unsigned comparison shape.

## sub_8047E5C (0x08047E5C)

Revalidated in `src/festate.c`; the implementation was already committed in `1c3cb328` and remains an exact match.

The callback signature is `void sub_8047E5C(FrontendState *state, unk32 arg1)`, from the frontend handler table. The source preserves the target's switch body order, three sprite allocations, fixed-point movement targets, text cleanup object, and frame update sequence. The function-level instruction diff is exact and the full-ROM comparison passes after the surrounding frontend changes.
The timing condition in the three result-display callbacks was experimentally rewritten from `((sub_8057C40() >> 4) & 3) == 0` to `(sub_8057C40() & 0x30) == 0`. The direct-mask form did not match: in each function the target emitted `asr r1, r0, #4` followed by `mov r0, #3`, while the direct-mask build emitted `mov r1, r0` followed by `mov r0, #48`. The original shift-and-mask spelling is therefore retained.

## sub_80448F4 (0x080448F4)

Parked in `src/festate.c`. The natural implementation matches except for a stable six-instruction register-coloring residual at the signed remote-selection comparison beginning at offset `0x272`; the target keeps the record pointer/result in `r0`/`r1`, while the build uses `r1`/`r0`. The requested four-bit bitfield form was tested, but it removed the target's final low-nibble mask and shifted the literal pool, so the scalar-byte expression remains in the parked draft.

## sub_8048310 (0x08048310)

Parked in `src/festate.c`. The semantic draft recovers the four handler arms, sprite/menu updates, packet gates, remote-selection comparison, current-game-state writes, and the shared packet-save call. Natural control-flow variants consistently left the initial case-two failure handler before the shared `sub_8043960` tail, while the target places that handler after the tail at `0x866C`; a goto-based layout was not retained. The best draft is preserved under `#if 0` immediately above the untouched assembly inclusion. The signed remote-selection comparison also retains the stable `r0`/`r1` coloring residual seen in the parked draft. The packet state remains a scalar byte in this draft; the four-bit bitfield experiment was not used.

## sub_804868C (0x0804868C)

Parked in `src/festate.c`. The semantic draft recovers the four frontend handler arms, sprite/menu setup, packet-state initialization, local and remote packet gates, selection mapping to `CurrentGameState.unk6A5`, and the shared packet-save tail. An active matching experiment produced the target dispatch and most case instructions, but the clean source retained a stable register-coloring difference from the prologue onward (`state`/packet-state allocation), and `isMultiplayer` emitted an `u8` normalization (`lsl`/`lsr #24`) while the target uses halfword normalization (`#16`). The target's case-two failure block and shared-tail placement were also not pursued with artificial control flow. The best draft is preserved under `#if 0` immediately above the untouched assembly inclusion.

## sub_804444C (0x0804444C)

Parked in `src/festate.c`. The semantic draft recovers the initialization, five-way text setup, animation/update path, packet exchange, progress interpolation, flag-dependent selection state, and transition events. The packet payload reads from `_currentGameState + 0x6EC` prove overlapping accesses at offsets `+0`, `+2`, and `+3` (word, signed halfword, and signed byte); the parked draft models these with an explicit punned union and keeps the assumption isolated under `#if 0`. No exact matching implementation was retained, and the assembly inclusion remains intact.

## sub_8048A74 (0x08048A74)

Matched in `src/festate.c`.

The callback signature is `void sub_8048A74(FrontendSpriteTriple *arg0, s32 arg1)`, from the call in `sub_8048AE8`. The state and timer members are 16-bit fields, while the three sprite pointers use `SpriteEntry.x` and the animation assignment uses `SpriteEntry.frame.word` at offset `0x18`.

The first state branch writes `arg0->sprite2->frame.word = arg0->timer & 3`; the second directly tests the timer field for the unsigned `<= 3` condition before adding four. The timing expression remains `((sub_8057C40() >> 4) & 0xF) == 0`, and the three x-position constants are written as plain `0x1800`, `0x9800`, and `0x5800` literals; agbcc emits the same shift materialization. The instruction diff is exact and the full-ROM comparison passes.

## sub_8043F40 (0x08043F40)

Matched in `src/festate.c`.

The callback signature is `void sub_8043F40(SpriteTextCleanup **arg0, CurrentGameStateTail *arg1, s32 arg2)`, from `sub_8044054`. The ten cleanup entries are updated individually rather than through a loop. Each x expression uses the signed `arg2 >> 8` value directly, which reproduces the target's argument copy to `r6`, arithmetic shift, and late signed-halfword normalization for the final two entries. The text y positions use each cleanup's fixed-point `y >> 8` value.

A `SpriteTextCleanup *text` local keeps the selected cleanup pointer live across each timing call, while an `unk8 mode` local carries the palette mode. The timing calls and mode initialization order reproduce the target's `0xF` default and conditional `0xE`/`0xD` values. The instruction diff is exact and the full-ROM comparison passes.

## sub_8043DB8 (0x08043DB8)

Matched in `src/festate.c`.

The function signature is `void sub_8043DB8(SpriteTextCleanup **arg0, LevelState *arg1, CurrentGameStateTail *arg2, unk32 arg3)`, from the call in `sub_8044054`. The packed fourth argument uses its low byte as an unsigned scale and its upper bits as the display-section mask. The low-byte extraction is expressed as `scale = arg3 & 0xFF` with a 32-bit local; this reproduces the target's `0xFF` materialization and preserves the argument pointer allocation.

The language strings use the existing `_806E97C` table as nine groups of five pointers, indexed as `_806E97C[group][language]`. The five ordinary mask sections select groups `0` through `4`, while the final branch selects group `5` or `6` from `CurrentGameStateTail.unkC` bit `1`. The `LevelDescription` returned by `getLevelDescription2` is retained across the display calls and its `unk1` byte is passed to `showNumber`.

The four scaled values are written as direct call expressions `(scale * value) >> 5`; all four match the target's logical `lsr`, including the `LevelState.unk4` expression, because the `unk32` scale operand makes the multiplication unsigned under the usual arithmetic conversions. The target instruction diff is exact and the full-ROM comparison passes.

## Review questions — 2026-09-06

1. Folding `description` into `getLevelDescription2()->unk1` failed: it added a second helper call and changed register allocation, so the `LevelDescription *description` local is retained.
2. Folding the `position = arg2` alias was exact: direct uses of signed `arg2` are retained.
3. Folding `text = arg0[2]` into the first timing call failed: the target's retained pointer load and call ordering changed, so the alias is retained.
4. Folding `text = arg0[3]` into the second timing call failed similarly, so the alias is retained.
5. Folding `text = arg0[9]` into the final timing call failed similarly, so the alias is retained.
6. Folding the first `mode` assignment into a direct ternary call was exact.
7. Folding the second `mode` assignment into a direct ternary call was exact.
8. Folding the final `mode` assignment into a direct ternary call was exact.
9. Folding the `value = arg0->timer` temporary was exact after removing the now-unused declaration, so the timer field is used directly.
10. Replacing the shifted x-position constants with plain literals was exact, so the plain literals are retained.
11. Replacing the ten unrolled position calls with an eight-entry loop failed from the prologue and loop setup, so the calls remain unrolled.
12. Collapsing the final language-table arms into a conditional row expression failed in the final branch and literal-pool layout, so the two explicit arms are retained.
