# sub_8063E18 (0x08063E18, src/teletype.c) — matched 2026-09-10

Previously parked draft (TeletypeStateDraft) was 12 differing rows from a
match. Header: TeletypeState gains unk11, unkA4, unkA8, unkB8, unkE0, unkE4
(replacing padding, same offsets); FontStyle gains unkA (byte, read in the
unk14 fill loop). compare passes with the header change.

Measured (non-.word differing rows):
- draft as parked (separate `unkB0 = 0; unkAC = 0;`, `for (i = 0x7F; i >= 0; i--)`) → 12
- `state->unkAC = state->unkB0 = 0;` → 7. Target materializes &unkAC (from
  the unkA4 address, +8) and &unkB0 (from unkA8, +8) before storing B0 then
  AC — chained assignment stores right-to-left. Extending the chain to
  unkA8 (`unkA8 = unkAC = unkB0 = 0`) → 20.
- Clear loop: descending `i = 0x7F..0` forms (for, do/while, buffer alias,
  separate `j`) stay 7–11: .loop says "giv not worth while, 0 vs 7", so the
  index stays. Ascending `for (i = 0; i < 0x80; i++)` (or `<= 0x7F`) → 0:
  loop reversal emits the target's pointer countdown `sub r0,#1; cmp r0,r1;
  bge`. A signed `bge` on a pointer compare = reversed ascending signed loop.
- unk14 fill `do/while (i <= 7)` and `for (i = 0; i < 8; i++)` identical;
  sprite loop `while` vs `for` identical. Kept the for forms.
- `s32 i` vs `unk32 count`: `i < count` promotes to unsigned (bhs/blo),
  while the unk14 loop's constant bound gives signed `ble`; both from one
  s32 index.
