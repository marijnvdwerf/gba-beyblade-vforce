# sub_8043370 at 0x08043370

The function remains in assembly. A semantic C draft was tested under `#if 0` and matched the expected instructions exactly, but it used raw offset dereferences and therefore is not suitable as final source.

The accepted typed layout facts are in `src/common.h`: `FrontendMenuObjectData` fields at offsets 0x00, 0x02, 0x06, 0x08, 0x0C, 0x10, 0x12, 0x14-0x17; `FrontendSubobject.unk20`; `MenuState.unkA`; and `FrontendState.unk7D`/`unk7E`/`unk7F`.

The typed rewrite preserved the initial register allocation but diverged at the object-count test. The target computes `object + 0x490` using the original object pointer, while the typed rewrite loads `state->objectCount` through `state` at `object + 0x478`; this changes the address calculation and shortens the function by 0x18 bytes. A second typed divergence was that the compiler emitted an arithmetic shift for a signed `state->unk9` expression instead of the target's logical sign-bit extraction.

Near-miss experiments:

| Change | Result |
| --- | --- |
| Raw pointer draft with generic call declarations | Exact instruction match, including literal pool and padding; rejected because of raw offsets. |
| Typed fields with normal `spritetext.h` prototype | Incoming object allocated in r6 instead of target r7 and function was four bytes longer. |
| Generic local declaration for `sub_8061844` | Restored target r7/r6 allocation, but did not solve typed object-count address calculation. |
| Scoped `FrontendState*`/`MenuObject*` aliases | Added instructions or worsened literal-pool placement; discarded. |
| Pointer declaration reordering and temporary object aliases | No useful reduction of the typed diff. |

The best draft is retained immediately above the `INCLUDE_ASM` line in `src/menuobject.c`. Do not copy its raw dereference expressions into active source; the remaining obstacle is representing the overlapping object-level and nested `MenuState` fields without a forbidden duplicate view struct.