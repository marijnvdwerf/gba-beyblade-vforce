# sub_804967C — matched

Matched in src/frontend.c. Called without arguments by sub_8049458 and reachable
from mainLoop. The m2c draft was checked against the dump; its unsupported ldsh
annotations were resolved directly from the target instructions.

Final ROM SHA1: cd527c8c24e20e33913fc45199e64b3e6138a6e5.

## Corrected behavior and shared types

- The startup callback is at FrontendState +0x58C, not +0x588. Both it and the
  fallback FrontendObject +0xC callback are called without arguments. The
  +0x58C field and sub_8049330 setter now use void (*)(void).
- FrontendState +0x424 owns an AllocatedBlock, and +0x428 is SpriteTextCleanup.
  The existing motion group remains at +0x458. Direct typed member access
  reproduces the _unk3000A78 literal as _unk3000650 +0x428 without introducing
  another RAM view, alias symbol, or raw offset.
- Consolidated the old FrontendSelectionRecord/FrontendObject and
  FrontendSelectionData/FrontendSubobject duplicate layouts. The ROM table now
  uses FrontendObject and its pointer is const; existing consumers use unk4.
- FrontendSubobject +0x18 is the descriptor array. Its 0x2C-byte records have
  signed x/y halfwords at +0/+2 (ldsh), font pointer +8, five language pointers
  +C, halfword tile count +0x20, entry motion +0x24, and existing exit motion
  +0x28. The font descriptor's allocation argument is halfword +8, while the
  text attribute is byte +0xA; the former draft conflated these.
- FrontendMotionData +0x24 supplies newMotionGroup's halfword count. Its signed
  +0x26 value remains the argument to sub_8050578 (ldsh).
- Added declarations for the existing menu setup/cleanup functions in
  menuobject.h and included the existing font and language headers.

## Matching observations and fold tests

| Source change | Measured result |
| --- | --- |
| Replace the draft's artificial zero-indexed count access with motion.count | Matches the target signed load without the zero local or extra block |
| Direct descriptor reads for title allocation and string setup | Removes the extra r8 save and restores object in r7 |
| Store block address into unk470, then read it into destination | Folded initialization diverges first at +0x13C; the retained store-then-reload also restores the store-base-first order at +0x14A |
| Fold `record` into `selected = _8069FC8[base->unk4].unk4` | Diverges first at +0x0C; retain the `record` alias |
| Use a distinct second title descriptor local instead of reusing array cursor | Restores the in-place address add at +0x1E8 |
| Fold that second title local into direct object->unk10 access | Still exact; local removed |
| Fold the initial title cursor snapshot into direct object->unk10 access | Still exact; snapshot removed |
| Fold staged allocation result into direct unk424 assignment | Still exact; block local removed |
| Remove the bare outer scope and hoist its declarations | Still exact; bare block removed |
| Replace allocation stride literal with sizeof(SpriteTextCleanup) | Still exact |
| Combine cleanup count load and decrement into one expression | +0x36 changes ldr r4,[r0,#20] to ldr r0,[r0,#20], then three-register subtraction; rejected |

The cleanup cursor/count and allocation destination/descriptor/count are real
loop state. The selected object is captured before callbacks; motionData is
captured before font calls and remains live across them. Those snapshots follow
the target's dataflow. No raw struct offsets, casts on fields, register pinning,
volatile matching tricks, value-less returns, or padding hacks remain.

## Verification

Compiled 21 size/offset assertions using the project's actual agbcc command:
font descriptor size 0xC; row stride 0x2C and all accessed offsets; ROM record
stride 0x18; subobject size 0x28; motion description size 0x28; FrontendState
size 0x5AC with members at 0x424, 0x428, 0x458, 0x470, and 0x58C.

Searched all active users of the changed types. Instruction diffs remained exact
for sub_8049264, sub_8049330, sub_8049344, sub_8049458, sub_80495C4, and
sub_804A280. The complete ROM SHA1 passed after formatting and dump removal.
The baseline was refreshed only from that verified ROM.
