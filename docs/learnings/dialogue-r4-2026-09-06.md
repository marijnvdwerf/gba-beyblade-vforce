# sub_80420C4 (0x80420C4)

Status: parked with the original assembly dump retained in `src/dialogue.c`.

The best natural-C draft is retained in a bare `#if 0` block immediately above
`INCLUDE_ASM("asm/dump/8040d18/80420c4.s")`.

The best draft's first instruction divergence is at offset `0x226`, where the
target materializes `REG_WIN0V` before shifting the first interpolation result:

    ldr r3, REG_WIN0V
    asr r2, r5, #9

The draft shifts first and materializes the register address afterward. Both
interpolation blocks match through offset `0x224` in this draft.

The interpolation allocation was recovered by staging the global flag value
before each flag phase:

    flags = _unk3000EEC.unk0;

The first staging preserves the target's `r4`/`r7` allocation and, together
with the natural interpolation result local, produces the target's `r6`/`r5`
first interpolation and `r2`/`r1` second interpolation. Reusing a separate
current-value temporary for `_unk30000C4` introduced an extra load and moved
the first divergence to `0x1D8`.

| Change | Result |
| --- | --- |
| Typed `FrontendSubobject` records with 0x88-byte stride | Recovered the four repeated record accesses without raw offset arithmetic. |
| `sub_804A364` before record initialization | Matched case-0 call order. |
| Correct five-argument `sub_8055914` call and sequential talking-head loads | Matched the helper argument setup. |
| Thumb-adjusted `teletypeDefaultUserCodeHandler` callback | Matched callback address formation. |
| `allocSprite` result temporary before storing `_unk30000D8` | Matched the target's address-materialization order around the call. |
| Direct `_unk30000C4` interpolation assignment | Kept the second interpolation compact; a separate current temporary added a load and changed literal-pool displacement. |
| `flags = _unk3000EEC.unk0` before each flag phase | Best draft: both interpolation blocks match through `0x224`; first divergence `0x226`, 41 differing rows. |
| Direct second-phase global reload while retaining the staged flag local | Regression: first divergence `0x208`, 43 differing rows. |
| Display-mode temporaries and equivalent staged constant forms | No improvement; register roles remained unlike the target or increased code size. |

Parking review follow-up:

- The disabled draft now uses `DialogueDisplayRecordDraft[4]`, a scratch record
  with size `0x88` and only the accessed fields at `0x14`, `0x24`, and `0x7C`.
- The callback is passed as the direct `teletypeDefaultUserCodeHandler`
  function symbol. The block was temporarily enabled with the assembly include
  disabled and compiled successfully; no byte comparison was retained while
  parked.
- `TalkingHeadDraft` models the accessed fields at `0x18` and `0x20`.
- `DialogueStateDraft` is a 40-byte scratch global model for `_unk3000EEC`.
  The fixed-address dialogue globals are declared inside the block with their
  existing widths, including `SpriteEntry* _unk30000D8` and signed `s32`
  interpolation globals `_unk30000BC` through `_unk30000D0`.
