# sub_805A93C follow-up, 2026-09-21

Status: parked, assembly retained. No active header or layout changes.
The explicit user request selected this function; it is absent from the current
mainLoop callgraph, and there are no C callers. m2c was run on the target dump.
The referenced residual-analysis-2026-08-28.md file is absent; its processed
archive was not read.

## Retained draft

A wide count parameter assigned to an unk16 local after the context store
reproduces the target instruction order. Direct destination-parameter use
removes the original draft's early callback store and delayed destination move.
The remaining ten instruction differences are exclusively registers:
source r7 instead of r5, destination r6 instead of r7, count r5 instead of r6.
The first difference is +0x02, mov r7,r1 versus target mov r5,r1.
The body is 0x46 bytes, with identical branch offsets, calls, and padding.
The retained draft lives immediately above the assembly inclusion in keystate.c.

There is no signedness evidence for these fields or parameters. The source
stride is four bytes, the destination stride twelve bytes, and the helper reads
the first source halfword. The callback signature remains inferred from the
neighboring callback invocation; no caller validates its ABI.

## Controlled probes

| Shape | First difference / effect |
| --- | --- |
| Original narrow formal plus destination alias | +0x04: stack load instead of destination move; callback store hoisted; same size |
| Wide formal, narrow count after context store, direct destination | +0x02: source register; same size and exact instruction order (retained) |
| Fold count into narrow formal | +0x02: source register; normalization moves ahead of context store; same size |
| Fold count into wide parameter with explicit narrowing | +0x02: source register; additional parameter move; +2 bytes |
| Wide count local narrowed from parameter | Same remaining register substitutions as retained draft |
| for / while / guarded do-while / early-return guarded loop | Same register substitutions as retained draft |
| Wide index with explicit halfword wrap | Same register substitutions as retained draft |
| Source/destination aliases introduced at each initialization boundary | No match; early destination alias first differs at +0x04 and leaves seven prologue diff rows; late aliases move bases after stores |
| Stack-argument locals for callback, context, and delay | No match; narrow-count baseline retains +0x04 stack load with destination alias, or +0x02 source-register mismatch without it |
| Source record pointer versus first-halfword pointer in helper | No change to retained draft's register mismatch |
| Halfword-pair / word-array / record-word diagnostic source views | Same ten register mismatches; speculative layouts discarded |
| Per-iteration destination record local | Same ten register mismatches; folded away |
| Per-iteration source record local | Additional address scheduling differences; discarded |
| Store count from original parameter, chained count store, count readback | Additional store/normalization ordering differences; discarded |

The allocation dump for the direct-parameter draft reports source and
 destination bases used four times across 33 and 32 instructions respectively,
versus five count uses across 25 instructions. This explains the observed
allocator ordering but does not establish a legitimate source change to fix it.
No forced registers, volatile qualifiers, raw-offset accesses, artificial
control flow, or compiler-flag changes are retained.

Final validation: the assembly-backed US and EU SHA1 comparisons pass.

## sub_805ABC0 (0x0805ABC0): matched

Activated the parked UnkKeyInput implementation and deleted the assembly dump.
No C callers were found. Existing record/global types and headers are unchanged;
the input fields remain unsigned halfwords and the timestamps unsigned words.
The >100 and >65 elapsed-time tests reproduce the target unsigned branches.

The residual was operand ordering, not a missing alias or a different record
layout. Writing the mask merge as `arg0->unkA |= arg0->unk8 & _unk3005DA0`
reproduces the target's single saved r4, argument in r2, second mask load in r0,
and key-state load/AND in r1. The old `_unk3005DA0 & arg0->unk8` spelling
introduced two moves, an extra saved register and four bytes. The completion
test uses `(arg0->unk8 & arg0->unkA) != arg0->unk8`.

### Reduction checks

The explicit `unkA = (unk8 & global) | unkA` match was reduced to the compound
assignment above with the fold runner and a full US SHA1 check; the reduction
was retained. The final function has no temporary locals or cached aliases.
Mask/state temporary trials did not improve the final direct-expression form:
wide mask staging removed the extra saved register but changed the merge/test
loads and removed the target's halfword normalization pair. Narrow mask staging
also differed. No casts, artificial scopes, register declarations, or asm levers
are needed, and no fakematch marker is warranted.

The exact symbol is 0x68 bytes, including its literal pool. Final formatted source
passes both enforced US and EU ROM SHA1 comparisons.

## sub_805A984 (0x0805A984): matched

Activated the sequence recognizer and removed the dump. No C callers were
found. Existing record/global declarations and headers are unchanged. The index
is s32: the target increments without halfword normalization and uses signed
blt against the zero-extended halfword count.

Corrected the parked draft's predecessor timing condition: increment when
previous->unk4 + config->unk8 >= current->unk4; reset to zero when the sum is
less. This follows target +0x9A cmp / +0x9C bcs, which skips the reset when the
sum is at least the current timestamp. The initial previous = NULL precedes
the index load, preserving the target's ip initialization.

The recognition block is recovered as static inline checkKeyInput, shared by
sub_805A984 and the existing sub_805ABC0 wrapper. Its nested success path returns
1 and falls through to return 0 otherwise. Inlining retains the target's
boolean-result join and compare, while keeping the current record in r2 and
previous in ip. No standalone helper is emitted. For the required operand
order, the entry test puts the global first, the mask merge puts the record mask
first, and the completion test puts accumulated keys first. Direct field
expressions produce the target's cached halfword copies automatically; no
explicit mask alias is retained.

### Fold checks

All trials used the enforced full US SHA1. The exact target symbol is 0xCC bytes,
including its literal pool.

| Reduction | Result |
| --- | --- |
| Fold triggered into the helper-call condition | Exact; retained |
| Fold current into repeated indexed accesses | Same size; first difference +0x02: argument move replaces high-register save; restored |
| Fold previous into repeated conditional pointer expressions | -14 bytes; first difference +0x00: r7 no longer saved; restored |
| Replace sub_805ABC0 body with the shared helper call | Exact for both functions and full ROM; retained |

The index is mutable sequence state carried through recognition and the optional
callback; it is not a disposable alias of the config field. Current and previous
are required by the tested forms. No casts, artificial scopes, register pinning,
volatile qualifiers, or inline assembly are used.

Final formatted source passes both enforced US and EU ROM SHA1 comparisons.

## sub_805A93C: second follow-up

Still parked. The existing direct-parameter draft remains the best complete
instruction sequence: ten differing register operands, first at +0x02 (source
r7 rather than r5), with the same 0x46-byte symbol, instruction order and branch
targets. No source/header/layout changes were retained from this investigation.
The target loop keeps source in r5, count in r6 and destination in r7; the draft
uses source r7, count r5 and destination r6.

The current allocation dump reports source four references across 35
instructions, destination four across 34, and count five across 25. This differs
slightly from the earlier dump's lifetimes but has the same priority/order.

| Additional controlled probe | Result |
| --- | --- |
| Separate loop-limit copy, four widths and three assignment positions | No match; 10–12 differing rows |
| Count field assignment/readback or chained assignment | 12–13 differing rows |
| Extract initialization loop into inline helper, all six parameter orders | Same ten register differences |
| Source/destination loop aliases, early/late and repeated assignments | Closest destination-alias variant has seven differences but wrong setup order |
| Destination alias with wide/narrow arg4, callback staging, pointer/void/integer argument diagnostic | Same seven setup differences; discarded |
| Count formal/local signedness and width combinations | No match; narrow formal moves normalization, wide forms retain allocation residual |
| Both per-iteration record aliases, declaration and expression order | Same ten differences or address calculations reordered |
| Explicit scalar/array-pointer source views and typed address expressions | Same ten differences; no alternate types retained |
| Equivalent nullable stores, zero staging, redundant null normalization and identical call arms | No match; unchanged residual or additional instructions |

The seven-difference alias variant is not a match: target +0x04 destination
move is delayed until +0x0C, stack-argument loads use different registers, and
the callback store moves from target +0x1E to +0x08. Its loop register allocation
is exact. Combining that alias with narrower/wider count declarations did not
recover target setup order. No register/asm/volatile or compiler-flag levers
were used. The original parked draft and dump are preserved.

Both enforced US and EU SHA1 checks pass after restoring the assembly-backed
source. These checks establish preservation, not a C match for this function.

## sub_805A93C: pointer-walk follow-up

The existing indexed form remains retained because none of the requested natural
loop forms is byte-identical. Each probe was built independently and diffed
against the target object.

| Shape | First divergence |
| --- | --- |
| Both input and source walked | +0x02: target `mov r5,r1`; probe `ldr r5,[sp,#20]` |
| Input walked, source indexed | +0x02: target `mov r5,r1`; probe `mov r7,r1` |
| Source walked, input indexed | +0x02: target `mov r5,r1`; probe `mov r7,r2` |
| Row local `UnkKeySource* row = &arg1[i]` | +0x02: target `mov r5,r1`; probe `mov r7,r1` |

The TODO marker and current source shape were restored after the probes. No
header or layout changes were retained.
