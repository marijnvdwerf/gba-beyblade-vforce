# Menu matching notes — 2026-09-17

## sub_805B1E0 (0x0805B1E0)

- The function signature is `s32 sub_805B1E0(MenuState*, s32)`. The second parameter is used by signed `blt`/`bge` bounds checks and as the `UnkMenuItem` index; the `s32` return carries either `UnkMenuItem::value` or `-1`.
- `UnkMenuItem* item = arg0->items + arg1` followed by bounds checks, `next` selection, `options` presence check, and `value` return matches the target 24 instructions exactly. No temporary or artificial control-flow shape is required.
- `bun run tools/diff/diff.ts sub_805B1E0` reports every instruction equal. The US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805B0F0 (0x0805B0F0)

- The signature `void sub_805B0F0(MenuState*, s32, unk8)` matches the indexed item bounds, byte flag assignment, and selected/unselected text refresh exactly. The `unk8` parameter produces the target `lsl #24`/`lsr #24` normalization before both the flag test and palette selection.
- `bun run tools/diff/diff.ts sub_805B0F0` reports every instruction equal. The US ROM SHA1 remains `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805B0BC (0x0805B0BC)

- The function signature is `unk32 sub_805B0BC(MenuState*)`. It indexes `MenuState::items` by `unk24`, invokes `unk34` with the selected item and index when present, follows `UnkMenuItem::next`, and returns the index OR the item's value shifted into the high halfword.
- The direct `unk32` fields and `MenuStateValueCallback` prototype match all target loads, the indirect callback, and the final `lsl #16` without casts or extra temporaries. `bun run tools/diff/diff.ts sub_805B0BC` reports every instruction equal. The US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805B1A0 (0x0805B1A0)

- The signature `void sub_805B1A0(MenuState*, s32, s32)` matches the indexed item bounds, `next` selection, option presence, signed value bounds, value store, and refresh call exactly.
- Retyping `UnkMenuItem::count` from `unk32` to `s32` changed the target's final `bhs` to the required signed `bge`; `sub_805B050`, `sub_805B0BC`, `sub_805B1E0`, and `allocateMenuItems` remained instruction-identical after rebuilding.
- `bun run tools/diff/diff.ts sub_805B1A0` reports every instruction equal. The US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805B13C (0x0805B13C)

- The function signature is `void sub_805B13C(MenuState*, s32)`. The selected index is copied into a signed `s32` parameter and checked against the signed `MenuState::itemCount` using `blt`/`bge`; `UnkMenuItem::unk44` is read as a byte.
- The matched body rejects the current index, out-of-range indices, and disabled target items. It refreshes the target item with `unk2E`, refreshes the prior item with `unk2D` when its `unk44` byte is nonzero or `unk2C` otherwise, then stores the selected index to `unk24`.
- A separate `unk8 value` local is byte-required: assigning the two palette fields to it before the common refresh call emits the target's shared call block and branch layout. A pointer local or direct calls in each arm produced a shorter or differently scheduled function.
- `bun run tools/diff/diff.ts sub_805B13C` reports every instruction equal. The US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
