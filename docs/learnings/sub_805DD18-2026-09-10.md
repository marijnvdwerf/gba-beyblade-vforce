# sub_805DD18 (0x0805DD18, src/geometry.c) — matched 2026-09-10

Spline sample with a quadratic-Bezier curve between segment endpoints.
Previously parked (frame 12 vs 8); the parked draft was also semantically
wrong (firstX = midX * fraction >> 10; curve offsets used angle0/angle1
instead of the previous segment's angle and the adjusted angle1). Rewritten
from the asm. Header: GeometrySplineLine gains `unk16 unkA` (ldrh at +0xA).

Step table (non-.word differing rows):
- fresh rewrite, literal 0x180, linear branch first → 189 (frame already 12)
- `lines = &spline->pointIndices[count]` (not via the pointIndices local:
  target `lsl #2; add #0x20; add spline`), clamp as
  `((count - 1) << 10) | (position & 0x3FF)`, curve branch first
  (`angle0 != x && angle2 != x`; target beq's to the linear block at the
  end), `s16 previous` + `(unk8)previous` index (target ldrh + lsl/lsr #24;
  an unk8 local gives ldrb) → 142
- offsets first, then `x0 += point0->x` (target order) → 137 (literal)
- `radius = 0x180` local at top → 155 (but restores the real `mul` with
  0x180 rematerialized per x/y pair: the pseudo spills with a REG_EQUIV
  constant; a literal `* 0x180 >> 8` is synthesized as `(x*3) >> 1`)
- in-place reuse: `x0 = (x0 + x1) >> 1; ... x0 = firstX + ...; result[0] = x0;
  result[1] = y0;` (target keeps offset → sum → midpoint → final value in
  one register, r7/r6, and stores both results together) → 0.

Fold tests on the match (each alone):
- literal 0x180 instead of radius → 93; radius assigned inside the curve
  block → 49; assigned after `fraction` → 0 (kept at function top).
- `unk8 previous` without casts → 30.
- storing result[0]/[1] directly (no x0/y0 reuse for the final values) → 79.
- dropping the pointIndices local → 31; `lines` via pointIndices → 10.
Byte-required: radius local, s16 previous, x0/y0 reuse, pointIndices local.

## Measured fold tests (2026-09-10)

- `angle0`: kept. Folding `(s16)lines[index].unkA` into the curve condition
  while preserving sign-extension diverged at `0x0A` (`sub sp, #12` target vs
  `sub sp, #16` trial). The uncast direct fold also failed (`0x36`, target pool
  load `#72` vs trial `#68`).
- `angle2`: kept. Folding the conditional source into the curve condition with
  an `(s16)` cast diverged at `0x0C` (`mov r7, r0` target vs `mov r6, r0`
  trial). The uncast direct fold also failed at `0x0C`.
- `firstX`: kept. Folding the first quadratic interpolation into the final
  x expression inserted `ldr r5, [sp, #8]` at trial offset `0x13E` before the
  target's `sub r0, r7, r4`.
- `firstY`: kept. Folding the first quadratic interpolation into the final
  y expression inserted `ldr r2, [sp, #8]` at trial offset `0x14A`; the target
  proceeds directly with `sub r0, r6, r2` at `0x14C`.
- `secondX`: kept. Folding the second quadratic interpolation into the final
  x expression diverged at `0x0A` (`sub sp, #12` target vs `sub sp, #8` trial).
- `secondY`: kept. Folding the second quadratic interpolation into the final
  y expression first diverged at `0xD4` (target pool load `#180` vs trial
  `#184`).

No individual fold held, so no combination trial was run. All trials were
restored and each restoration passed the full ROM compare.
