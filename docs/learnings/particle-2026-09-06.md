sub_804E560 (0x0804E560): matched exactly. The function copies the current system coordinates at offsets 0x10, 0x14, and 0x18 into the previous-coordinate fields at 0x24, 0x28, and 0x2C before storing the new coordinates. All parameters remain unk32 because the matched instructions are plain 32-bit loads and stores.

sub_804C208 (0x0804C208): matched exactly. The coordinate projection uses `asr #1` after adding the X and Y coordinates, proving signed arithmetic for ProjectileEntry.x/y/z. Each six fixed-point target update uses `mul` followed by `asr #12`, proving signed arithmetic for ProjectileEntry.duration. The delay countdown loads ProjectileEntry.unk2C with `ldsh`, proving that field is s16. Velocity, acceleration, and ProjectileSystem.unk1C/unk20/unk24 remain unk32 because this function only performs plain 32-bit loads, stores, and additions on them. Folding the single-use x temporary into sub_804ABD0 was rejected: one build changed the first differing instruction to target 0x18 (`ldr r3, [r6,#0x18]`) and changed the loop register allocation. Folding y was rejected similarly: one build first differed at target 0x18 and changed the coordinate-update register allocation.

sub_804E594 (0x0804E594): parked. The target has an eight-byte frame-record stride, with halfwords at record offsets 0 and 2, and its `lsl #16; asr #13` sequence proves the frame selector is signed. The committed parked signature therefore uses s16 for arg7; the typed ParticleSystem.unk0 and ParticleFrameData declarations model the proven pointer and frame-record layout. The Particle fields used only by this parked draft are intentionally not added to the shared Particle definition.

sub_804E594 retry table (each row was one temporary activation build against the retained semantic draft; candidate size is the final emitted address before the literal pool):

- Folding `previous` into `particle->sprite`: first divergence target 0x0A (`sub sp,#8` versus the candidate's smaller frame); candidate ended at 0xF4. Rejected.
- Removing `scale`: first divergence target 0x0A (`sub sp,#8` versus `sub sp,#4`); candidate ended at 0xFC. Rejected.
- Folding x into the particle assignment: first divergence target 0x0E (the target spills r3, while the candidate does not); candidate ended at 0x100. Rejected.
- Folding y into the particle assignment: first divergence target 0x0E (the target spills r3, while the candidate does not); candidate ended at 0x100. Rejected.
- Folding z into the particle assignment: first divergence target 0x0E (the target spills r3, while the candidate does not); candidate ended at 0x100. Rejected.
- Replacing table/tableBase with one alias: first divergence target 0x0A (`sub sp,#8` versus `sub sp,#4`); candidate ended at 0xFC. Rejected.
- Removing the no-op `table += 0`: the one-build candidate retained the same no-op-free source shape and produced no useful instruction; the statement is removed. Retained removal.

Unproven E594 parked-draft layout assumptions: Particle is assumed to contain a SpriteEntry* at offset 0, 32-bit trajectory accumulators at offsets 4, 8, and C, signed 16-bit position components at offsets 10, 12, and 14, signed 16-bit camera offsets at 16 and 18, a signed 16-bit lifetime at 1A, and 16-bit animation fields at 1C, 1E, 20, and 22. These fields are not present in the committed Particle placeholder because no non-parked function has established the complete layout. The frame table declaration is limited to the two halfwords and six-byte stride proven by the target.

sub_804EA88 (0x0804EA88): parked. The target loads the particle trajectory fields and system coordinates into separate live values; its first divergence from the best semantic draft is target 0x12 (`ldr r3, [r0,#8]`) while the draft loads that value into r2.

sub_804EA88 retry table (each row was one temporary activation build):

- Folding the single-use z local into the projected-coordinate expression: first divergence remained target 0x12 (`ldr r3, [r0,#8]` versus the candidate's `ldr r2, [r0,#8]`); candidate literal pool began at 0x158 versus the target's literal pool at 0x15C. Rejected.

Unproven EA88 parked-draft layout assumptions: Particle is assumed to use 32-bit trajectory accumulators at offsets 4, 8, and C; 16-bit screen offsets at 16 and 18; a 16-bit lifetime at 1A; and 16-bit animation fields at 1C, 1E, 20, and 22. ParticleSystem.unkC is treated as an Actor* for the camera fields at Actor offsets 40 and 44. These assumptions remain confined to the parked draft and are not added to the shared Particle layout.
