# geometry matching notes — 2026-09-17

## sub_805E804 (0x0805E804)

- The inferred signature is `GeometryLine* (LevelGeometryAddresses*, unk8, unk16)`; the function walks 0x20-byte `GeometryLine` records from `startIndex` and compares `unkF`.
- A signed `s32 count` is required for the target `bge`/`blt` branches. Assigning `count` inside the initial `startIndex < addresses->unk0->lineCount` guard reproduces the target load into `r0` followed by `mov r3, r0`; assigning it before the guard instead allocates the cached count directly in `r3`.
- The `count` local is byte-required to retain one cached loop bound; direct repeated field access reloads the bound and diverges at 0x00000002 (`mov r3, r0` appears before the expected argument shift).
- Moving `count = addresses->unk0->lineCount` before the initial guard also diverges, first at 0x00000012 (`ldr r3, [r0, #8]` instead of the target `ldr r0, [r0, #8]`); the in-guard assignment is required.
- `bun run tools/diff/diff.ts sub_805E804` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805BA7C (0x0805BA7C)

- The matched signature is `LineMetadata* (LevelGeometryAddresses*, GeometryLine*)`; it searches `unkC` for the target line and returns the corresponding `unk114` metadata entry.
- Keeping the current line pointer initialized before the metadata-null check, then assigning the line count after that check, reproduces the target address and loop setup exactly.
- `bun run tools/diff/diff.ts sub_805BA7C` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805C444 (0x0805C444)

- The matched signature is `unk32 (LevelGeometryAddresses*, Actor*, unk32)`; it finds one spline connection for a point index, rejects splines with one or fewer points, adjusts the endpoint position, and calls `sub_805C3BC`.
- A `SplineConnection` local provides the target 20-byte stack record at `sp+4`; the fifth `sub_805DBF0` argument occupies the outgoing slot at `sp+0`.
- The result expression must be `sub_805DBF0(...) << 16` before the shared result test; assigning the unshifted result to an `unk16` local adds a compensating `lsr`, and direct early returns branch to the zero arm instead of the target common epilogue.
- `bun run tools/diff/diff.ts sub_805C444` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
- A `unk16 found` local with natural result assignments preserved the call's `lsl #16`, but its first divergence was an extra result initialization before the spline-point load and a merged `ble` zero arm; the whole-ROM hash did not match. Changing `sub_805DBF0` to return `unk16` instead added `lsr #16` in this caller and also failed the whole-ROM hash.

## sub_805E7C0 (0x0805E7C0)

- The matched signature is `GeometryLine* (LevelGeometryAddresses*, unk8, unk16)`; it scans 0x20-byte lines from index zero and returns the first line whose `unkF` and `unk14` match the two arguments.
- `GeometryLine.unk14` is a proven halfword field from the target `ldrh [r3, #20]`; the existing `unkF` byte field is reused.
- Writing the match as an explicit mismatch arm (`if (line->unkF != type || line->unk14 != id) { advance; } else { return line; }`) reproduces the target `beq` success branch and separate `mov r0, r3`.
- The initial `index < lineCount` guard and the same condition on the `do` loop preserve the target initial count load and per-iteration count reload; the byte and halfword index normalizations are supplied by `unk8` and `unk16` locals/parameters.
- `bun run tools/diff/diff.ts sub_805E7C0` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805D430 (0x0805D430)

- The matched signature is `void (Actor*, LevelGeometryAddresses*)`; it skips actors with `unk84 == -1` false, computes the actor origin from `x/y` plus `unk9A/unk9C << 8`, adds the velocity and margins, and forwards the two endpoint pairs to `sub_805D488`.
- The typed `Actor` fields already prove the target `ldrsh` and word-load widths; a local `s32 positions[2]` preserves the two outgoing stack arguments at `sp` and `sp+4`.
- `bun run tools/diff/diff.ts sub_805D430` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805D548 (0x0805D548)

- The matched signature is `void (Actor*, LevelGeometryAddresses*, QuadTreeNode*, s32, s32, s32, s32)`; it tests the actor state, scans the node's spline entries, computes an intersection with `sub_805E320`, updates the actor, and invokes the optional spline callback.
- A 24-byte `GeometrySplineIntersection` local preserves the helper output block at `sp+0x10`; its `unkC` and `unk10` fields reproduce the position and line-index loads, while the remaining bytes are padding.
- `QuadTreeSplineEntry.unk4` and `.unk6` are signed halfwords, proven by the target `ldrsh` loads. The callback's first function pointer and intersection-result pointer are proven by the target indirect call and its arguments.
- `bun run tools/diff/diff.ts sub_805D548` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805E474 (0x0805E474)

- The matched signature is `unk32 (s32 x0, s32 y0, s32 x1, s32 y1, s32 x2, s32 y2, s32 x3, s32 y3)`; it rejects non-intersecting endpoint pairs using two signed cross-product tests and returns 1 or 2 according to the first cross-product sign. The argument order is confirmed by `sub_805E18C` passing `(x0, y0, x1, y1, x2, y2, x3, y3)` to the helper.
- The return type must remain wide `unk32`; using `unk8` adds a final `lsl/lsr #24` normalization that is absent from the target.
- Each rejection test must be written as a single compound condition, `(cross0 <= 0 && cross1 < 0) || (cross0 >= 0 && cross1 > 0)`, to preserve the target's repeated sign comparison and register lifetime. Separate sequential `if` statements reload the saved cross product and diverge at the second sign check.
- Separate `cross1`/`cross3` locals change the second cross-product instruction sequence. A single naturally reused `cross1` local is required for the target; it holds the second cross product for each side test.
- The second temporary cross product must use `dx2 * (y1 - y3)` rather than the commuted `(y1 - y3) * dx2`; the source operand order controls the target's `mov r1, r2` before `mul`.
- `bun run tools/diff/diff.ts sub_805E474` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_805E0D8 (0x0805E0D8)

- The parked draft was discarded; the first unresolved divergence was the natural `pointIndices` cursor setup, whose `add`/`lsl` ordering differed from the target.

## sub_805D488 (0x0805D488)

- The matched signature is `void (Actor*, LevelGeometryAddresses*, s32, s32, s32, s32)`; it scans the spline table, tests each spline with `sub_805E18C`, updates the actor through `sub_805C3BC`, and invokes the optional spline callback.
- The successful-intersection path returns immediately after the callback check. Omitting this return makes the callback path fall through to the next spline and changes the target's terminating branch.
- A 24-byte `GeometrySplineIntersection` local at `sp+0xC` preserves the helper output and callback result pointer. The declaration order with the loop index after the pointer/result locals yields the target's geometry, spline, and index register allocation and the 36-byte frame.
- The canonical `for (i = 0; i < geometry->unk0->count.splineCountWord; i++)` loop form is byte-identical to the target; the equivalent explicit `while` form was not retained.
- `bun run tools/diff/diff.ts sub_805D488` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
