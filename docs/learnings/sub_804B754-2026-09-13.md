# sub_804B754

Matched at 0x0804B754 in src/rider.c. The function is reachable from mainLoop through sub_804B4FC; the only C call site is inside that function's disabled draft and passes a discarded VCOUNT value, while the target has no entry normalization and the established declaration remains void(void). No header or layout changes were needed.

The target first snapshots `_gameData` into `state`, checks `state->unk1638`, and uses `&state->base` for the first RiderBase argument. The rider array is accessed as the typed `RiderBase` array element `&_gameData->unk42C[i]`; this naturally produces the target's separate scaled cursor and loop-count registers. The previous byte-offset local and pointer arithmetic were removed. The compiler-generated stride is 0x428, matching `sizeof(RiderBase)`.

The target reloads `_gameData` for the rider-array base, loop bound, and `unk1638` write/readback. Keeping those expressions direct while retaining the initial `state` snapshot reproduces the target's global reloads and r7/r8 allocation. The success path calls `sub_804FE14`, tests `_unk3005DA0 & 0x200`, clears `unk1638`, forwards the byte field to `sub_804F800`, and calls `sub_805568C`; the failure path calls `sub_804FE30` and continues until the count is reached. The `s32 i` declaration is supported by the signed loop branches: `bge` at `0x0804B784` for the initial bound check and `blt` at `0x0804B7F0` for the loop-back check.

Measured source changes:

| Source shape | Result |
| --- | --- |
| Compiled trial `&_gameData->unk42C[0] + offset` with `offset += sizeof(RiderBase)` | Emits a 0x114640 stride literal because `offset` is added to a `RiderBase*` and is therefore scaled by 0x428; rejected. The earlier `(unk8*)_gameData->unk42C + offset` spelling was not compiled successfully. |
| Typed `&_gameData->unk42C[i]`, with the separate offset local removed | Exact instructions, including the target 0x428 cursor increment and 0xA8 function size. |
| `state` passed directly as the first helper argument | Compiler diagnostic; helper requires RiderBase*. |
| `&state->base` as the first helper argument | Exact instructions. |
| Fold `GameData* state` into direct `_gameData` accesses | First divergence at function offset `+0x2`: target `mov r7, r8` / saved state pointer is absent; folded body is 0xA0 bytes versus target 0xA8. Retain `state`. |

No raw offset dereferences, casts on fields, artificial control flow, volatile/register tricks, or speculative header fields remain. After clang-format, the instruction diff is exact and the full-ROM compare passes SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5.
