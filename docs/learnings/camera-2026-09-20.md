# Camera matching learnings — 2026-09-20

## sub_805ED60 (0x0805ED60)

- The function takes `CameraState* camera` and `Actor* actor`. The caller `sub_805EE78` stores the actor in `camera->unk224` and calls `sub_805ED60(camera, actor)` when no alternate callback is installed.
- `actorPosition[3]` is a stack local passed to `sub_8058754(actor, actorPosition)`. The target reserves 12 bytes after saving r4-r7 and lr.
- `camera->unk354 |= 1` is emitted as a byte read/OR/store at camera offset `0x354`.
- The `i` loop value is kept as `s16`. The target normalizes the loop value with `lsl #16` followed by `asr #16` before using it in the layer index calculation.
- `scale` is kept as `s16`. The target loads the layer scale with `ldrh` and then sign-normalizes it with `lsl #16`/`asr #16` before the multiply and `asr #5`.
- `state = nullsub_12(camera)` is retained as a distinct source value. The target keeps the incoming camera pointer in r7 and the nullsub result in r5; replacing all `state->records` accesses with `camera->records`, removing the state local, and discarding the call result diverged first at offset `0x04`: target `mov r7, r0`, candidate `mov r5, r0`. The candidate then omitted the target `mov r5, r0` at offset `0x0C` and shortened the function.
- Explicit `actorY = actor->unkA2 + 0x50` is byte-required. Folding it into the vertical expression diverged first at offset `0x3E`: target `ldrsh r0, [r0, r1]`, folded candidate `ldrsh r2, [r0, r1]`; the target then emits `add r0, #80`.
- Explicit `right` staging is byte-required. Folding `state->records[0].field_40 + 0xF000` into the right-bound store diverged first at offset `0x24`: target `ldrb r6, [r4, #16]`, folded candidate `ldrb r0, [r4, #16]`. The folded form also removed the target staged right-bound sequence `mov r1, #240; lsl r1, #8; add r0, r4, r1; sub r0, r2, r0`.
- Explicit `bottom` staging is byte-required. Folding `state->records[0].field_44 + 0xA000` into the bottom-bound store preserved the early instructions but diverged at offset `0x8A`: target `ldr r0, [r5, #24]`, folded candidate `ldr r1, [r5, #24]`. The folded form also shortened the function by the two-instruction `mov r6, #160; lsl r6, #8` staging sequence and changed subsequent register allocation and literal-pool placement.
- Loop form `for (i = 0; i < 4; i++)` matches the target exactly.
- Loop form `for (i = 0; i <= 3; i++)` also matches exactly; the `< 4` form is retained as the clearer equivalent.
- The final matched source retains the distinct `state` alias, `actorY`, `right`, and `bottom` temporaries, and uses `for (i = 0; i < 4; i++)`.
- `actorPosition` is `s32[3]`: `unk32[3]` changes the ROM (measured at merge); the two sibling users (sub_805EBCC, gameloop) use the same type. The `record != &state->records[0]` test is the target's `cmp r3, r5` pointer compare.
