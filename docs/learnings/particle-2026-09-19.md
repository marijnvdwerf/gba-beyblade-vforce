# Particle matching — 2026-09-19

## sub_804E910 (0x0804E910)

- Matched the explicitly requested function. No C/assembly callers were found, and the current mainLoop callgraph does not list it; signature recovery uses its entry conversions and existing ParticleSystem/Particle layouts.
- Signature: void (ParticleSystem*, s16 scale, unk16 velocity, unk16 spread, unk16 lifetime, unk16 timer, s16 sequence). The target normalizes velocity, spread and lifetime unsigned at entry, then takes signed halfword views for arithmetic in the loop. The scale and sequence have signed normalization. No shared types or headers changed.
- A signed countdown uses the system's signed halfword count. Each iteration consumes three signed table values for velocity and three for position, masking the table index between triples. The lifetime addition takes an unsigned halfword view of the current table entry, then the position multiplication reads the same entry signed: target ldrh at function offset 0xD8 and ldrsh at 0xE2.
- The lifetime sum needs an s32 staging local. Folding it into the halfword store first introduces an extra stack store at offset 0x22 and removes the target signed lifetime conversion. Without the unsigned table view, the first lifetime read becomes ldrsh and CSE merges it with the next multiplication's read.
- `particle->unk20 = particle->unk22 = timer` recovers the two timer stack slots and stores without a duplicate source alias. Table-first multiplication for the three velocity components places the table-base literal load at offset 0x7C; multiplier-first spelling moves that load to 0x84, with all other instructions equal.
- Fold tests retained dx/dy/dz (each fold adds a mov into ip at offset 0xC), x/y/z (each changes r9 to ip at offset 0x1C), the frame pointer (fold changes stack allocation from 36 to 44 bytes at offset 0xA), and sprite (fold replaces the r9 move with a stack store at offset 0x1C). Each probe was reverted independently.
- All instructions match. Formatted source passes US SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5 and EU SHA1 6d20a586e7bb52b4a9352bbeaec9d7be65e93369.
- No raw-offset accesses, field casts, invented layout, volatile, goto, or assembly matching constructs are used. Explicit halfword conversions are byte-required as described above.
- An explicit `unk16 tableValue` assigned from `word_8074D64[index]` matches every instruction and replaces the table-read cast naturally. Removing the signed lifetime cast (`tableValue + lifetime`) first diverges at offset 0x22 with an extra `str r4, [sp, #0]`; the `(s16)lifetime` view remains. Narrowing the sum staging local from s32 to s16 also first diverges at offset 0x22 with an extra stack store.
- Replacing the three signed input casts with s16 locals diverges at offset 0x0E (extra parameter move and changed frame/stack layout); an alternate declaration order diverges at the same offset. The casts remain for velocity, spread, and lifetime.
