# festate 2026-09-16

## sub_8047494 (0x08047494)

- The direct typed implementation matches every instruction and the US ROM SHA1.
- `uploadPalette` declared as `unk8` produces the target entry normalization (`lsl #24; lsr #24`) and keeps both later tests in the target registers.
- The record accesses at offsets 0x00 and 0x14 are five-element pointer arrays; the existing palette and sprite-sheet members remain at 0x28 and 0x2C.
- `SpriteEntry.x` at 0x08 supplies the third `LoadSpriteSheet` argument; `frame.word` at 0x18 and `y` at 0x0C supply the three reset and three final stores.
- No named local temporary or artificial control-flow shape is required.
- There was no first divergent instruction; `bun run tools/diff/diff.ts sub_8047494` reports all instructions equal and the full US compare passes.
