# Beyblade matching notes — 2026-09-07

- `sub_805703C` (`0x0805703C`): `return _807a53c[arg0]` matched the target `ldr`/`ldrb` sequence exactly; no byte-required temporary or signedness adjustment.
- `sub_8057068` (`0x08057068`): writing the nonzero collection branch first, followed by `return Pal_836067C`, matched the target branch layout exactly; no byte-required temporary or signedness adjustment.
- `sub_8057094` (`0x08057094`): the same nonzero collection-branch shape with `CollectionSpriteSheets` and `SpriteSheet_836087C` matched exactly; no byte-required temporary or signedness adjustment.
