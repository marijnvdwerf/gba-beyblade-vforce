# profile

- `profile` matches at `0x804323c`.
- `timer` must be an `s32` receiving an explicit `unk16` conversion from `REG_TM2CNT_L`: the target emits `lsl #16; lsr #16` and then calls `__floatsisf` directly on the normalized value. An `unk16` local caused sign-extension; an `unk32` local caused unsigned-to-float conversion. The measured signed-wide local plus narrow assignment is the matching source shape.
- `scale` must be `unk32`: the target's `cmp #0; blt` and negative-value correction sequence around both `__floatsisf` conversions is the unsigned-integer-to-float path. The scale values are `1000000` and `1000`.
- The initial `scale = 0` is byte-required for the timer reset store to use the same `r5` value as the target. Removing it while keeping a literal reset first diverged at `0x0A`: the target has `mov r5, #0`, while the alternative began with the timer literal-pool load. Keeping `scale = 0` while changing the reset store to literal `0` remained byte-identical.
- Moving the timer read/reset after the mode switch first diverged at `0x0A`: the target has `mov r5, #0`, while the reordered form began with the mode global load.
- The initial floating operation must be written as one expression, `elapsed = (float)timer * frameRate`, followed by `elapsed /= scale`; staging the conversion and multiplication emitted an extra move before `__mulsf3`.
- The fractional result must be assigned directly from the product, `fraction = (unk16)((elapsed - milliseconds) * scale)`. Assigning the product back to `elapsed` first emitted an extra move before `__fixunssfsi`.
- `milliseconds`, `fraction`, `hundreds`, `tenths`, and `ones` are `unk16`; the target's halfword normalization and decimal extraction match these widths.
- Replacing each `250 << 2` with `1000` at offsets `0x36`, `0x42`, and `0x4E` was byte-identical; the source now uses `1000`.
- Switching directly on `_unk3000150[0]` was byte-identical.
- The timer reset must remain a 32-bit store: `*(vu16*)REG_TM2CNT_L = 0` emitted `strh` at `0x14`, while the target emits `str`. A 32-bit volatile read plus `(unk16)` conversion is also required: a 16-bit read emitted `ldrh` at `0x0E`, while the target emits `ldr` followed by `lsl #16` and `lsr #16` at `0x10` and `0x12`.
- Removing the casts on `milliseconds`, `fraction`, `hundreds`, `tenths`, and `ones` was byte-identical. The source retains no explicit narrowing casts for these `unk16` assignments.
- Replacing `0x30` with `'0'` in all three digit arguments was byte-identical; the target still emits `add #48` at `0x10C`, `0x10E`, and `0x112`.
- Folding `hundreds`, `tenths`, and `ones` into the `printf` arguments diverged at `0xD2`: the target moves the fractional result into `r5`, while the folded form uses `r4` and adds an extra literal load and stack store. The named `unk16` locals are retained.
- Replacing `timer = (unk16) * (vu32*)REG_TM2CNT_L` with `timer = *(vu32*)REG_TM2CNT_L & 0xFFFF` diverged at `0x0E`: the target emits `ldr r1`, `lsl r1, #16`, and `lsr r1, #16`; the mask form emits `ldr r2`, loads literal `0xFFFF`, and uses `and r2, r1`. The explicit `(unk16)` cast is retained.
