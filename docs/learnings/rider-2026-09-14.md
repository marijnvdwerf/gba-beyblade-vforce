# sub_804B7FC (0x0804B7FC, src/rider.c)

Nearest eligible rider to `rider`. Matched.
- Prototype: `RiderBase* sub_804B7FC(RiderBase*)`; the caller (effects.c sub_805568C) passes `&gameData->base` instead of the GameData pointer. The header previously declared `struct GameData*`, which conflicted with the draft's definition.
- Byte-required: a plain `for (i = 0; i < _gameData->unk430 + 1; i++)`. The parked `if (...) do { } while` form keeps `&_gameData` in a callee-saved register and moves the argument off the stack (39 rows).
- Byte-required: `bestDistance = 0x07FFFFFF` after the x/y/z loads (before them: 5 rows, literal load order).
- Byte-required: the `dx`/`dy`/`dz` locals.
- Folded, byte-neutral: the `Actor* coord` alias, for both the rider and the candidate; `RiderHasFlag(...) << 24` written as a plain `== 0` (the unk8 return supplies the shift); `best = NULL`. `distance` kept because it has two uses; inlining it duplicates the sum.

# sub_804B624 (0x0804B624, src/rider.c)

Matched. It has two dead-store "debugging leftovers", and both are byte-required.

- Frame: a function-scope VLA `RiderBase* riders[_gameData->unk430 + 1]` after `s32 i = 1;`. There is no bare block.
- Loop 1 is an indexed `for (j = 0; j < n + 1; j++)`. agbcc strength-reduces `riders[j] = ...` to the target's `stmia` cursor. The selection goes into `candidate` (ternary and if/else are byte-identical).
- Leftover 1: `rider = candidate;` in loop 1. It is dead because `rider` is reassigned in loop 2 before any read, but it makes the target build the selection in r0 and copy it to r6 at the join (`mov r6, r0`). Without it, the arms load straight into r6 (8 rows). Using `rider` for the selection, a scoped candidate, an inline getter (19–35 rows), ADDRESSOF via `(void)&rider`, or an embedded assignment all failed to reproduce the copy.
- Leftover 2: `count = _gameData->unk430 + 1;` just before loop 2. The dead load moves the top-test `mov r2, #0` after the bound loads (the address computation is CSE'd with it), so final cross-jumping can no longer merge the duplicated entry test into the bottom test. Without it, the entry becomes `mov r2, #0; b <bottom test>` (14 rows). `count = _gameData->unk430` (no +1) got the order partly right (12 rows). Using count as the VLA size, or placing it before loop 1, breaks.
- The outer loop must be a plain `for (j = 0; j < n; j++)` with `rider = riders[j]` at the top. The .loop dump finds no biv there, so `riders[j]` stays indexed (`lsl; add sl; ldr`) as in the target. `while` with an early `j++` gets a verified biv and reduces it to an `ldmia` cursor.
- The inner loop must be an indexed `for (k = i; k < n + 1; k++) { other = riders[k]; ... }`. A pointer-cursor inner loop leaves the outer loop small enough that its `riders[j]` also gets reduced.

Parked-draft step table:

| Change from the parked best draft | First divergence | Emitted extent / size delta |
| --- | --- | --- |
| Parked best draft: omit both `rider = candidate;` and `count = _gameData->unk430 + 1;` | `0x1C`: target `mov r6, #134`, trial `mov r1, #134` | trial `0x120` vs target `0x130` (`-16` bytes) |
| Add only `rider = candidate;` | `0x9C`: target `ldr r3, [pc, #140]`, trial `mov r2, #0` | trial `0x124` vs target `0x130` (`-12` bytes) |
| Add only `count = _gameData->unk430 + 1;` | `0x1C`: target `mov r6, #134`, trial `mov r1, #134` | trial `0x12C` vs target `0x130` (`-4` bytes) |

Both leftovers are retained in the matched source; each changes the allocation or loop-entry schedule, and together they close the residual.

# initRider (0x0804B07C, src/rider.c)

Matched (the only differing diff row is the `.word convert3DCoordsto2DCoords` Thumb-bit relocation display; ROM SHA1 passes).
- Key fix is outside the function: `actor_80585F8(Actor*, s16, s16, s16, s16)`, separate commit a533f596. With `unk16` parameters the `-1` arguments lower to `0xFFFF` literals, and there are 79 differing rows. With `s16`, cse shares one `-1` pseudo, which is the target's r8, across `actor_8057C58`, `actor_80585F8` and the second `actor_8057C58`. That extra callee-saved pseudo also pushes `arg5` back onto its stack slot, which the target reloads six times. The stored fields `Actor.unkA8..unkAE` are `s16`.
- Debugging leftover, byte-required: the discarded `getBeybladeData0(arg6);` call.
- Byte-required locals: `actor`, `shadow` and `config` (folding any one breaks).
- Byte-neutral cleanups: `rider->unk3C4 = NULL` in the arg5 == 0 arm (was `(SpriteEntry*)arg5`), and `VRAM + 0x10000 + (...)` for `unk3D4`.
- Layout: the `getBeybladeActorData(arg6)->unk4` byte goes to `RiderBase.unk3CF` (the old draft wrote `unk2FC.unk39`), and `sub_804E584` receives `z` (the old draft passed arg5). `RiderTileState` gained a real `unk32 unk8` (was `pad8[4]`) to replace the draft's scratch struct. `SpriteSheet_86FAEAC` is declared in rider.c as the `ActorConfig` it is passed as. The `initRider` prototype takes `RiderBase*`.
