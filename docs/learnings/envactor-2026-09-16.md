# envactor 2026-09-16

## sub_80550B8 (0x080550B8)

- The callback is invoked with `(Actor*, LevelGeometryAddresses*, GeometrySpline*, s32, s32)`; the fifth argument is the spline point index and the third argument supplies `pointCount` at offset 0.
- The callback updates the environment object for the first or final spline point, copying actor velocity fields 0x40/0x44/0x48 to object fields 0x2C/0x30/0x34, copying object halfword 0x3A to 0x38, then clearing the actor velocity fields.
- The body matches every instruction with the existing Actor, GeometrySpline, and EnvironmentObject layouts. No local temporary or artificial control-flow shape is required.
- An `unk8` return declaration is required for the target epilogue (`pop {r1}; bx r1`); a `void` declaration instead emits `pop {r0}; bx r0`.
- `bun run tools/diff/diff.ts sub_80550B8` reports all instructions equal and the full US compare passes.
