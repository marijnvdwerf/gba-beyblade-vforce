# Geometry matching — 2026-09-19

## sub_805E18C (0x0805E18C)

- Matched the user-selected function with typed spline/point indexing, signed movement bounds, byte outcodes, a word overlap mask and found flag, and an early exit after the first intersection. The existing C caller `sub_805D488` supplies the seven-argument signature and tests the byte result against 1.
- The current `mainLoop` callgraph does not list this function; it was selected explicitly by the user rather than from the reachable worklist.
- The TU-local 24-byte `GeometrySplineIntersection` replaces one padding byte with `unk14`. The target `strb [r0, #0x14]` proves this new field width; all preceding fields and the total extent remain unchanged.
- Signed coordinate comparisons and the `blt` loop bound support the existing signed point coordinates and signed index/bounds. The byte intersection local supplies the target normalization of the wide `sub_805E474` result; the byte-return prototype for `sub_805E0D8` supplies its caller's low-byte zero test.
- The initial natural C implementation matched every instruction. Folding the cached `pointIndices` pointer into `spline->pointIndices` first diverged at function offset 0xA: stack allocation changed from 48 to 44 bytes, and the spline/index register roles changed. Retained that pointer.
- Folding the `segment = i - 1` temporary into the call and output store preserved every instruction, so the temporary was removed. The remaining point pointers, bounds, flags and intersection result each retain distinct algorithmic state or repeated uses.
- No artificial control flow, raw-offset access, field casts or other matching levers were required.
- Combining the intersection assignment with the `found` test, and directly branching on the intersection without the second test, first diverged at offset 0x28 (`b 0x184` target versus `b 0x178` probe); the separate `found == 1` test is retained.
- Formatted C and header pass both enforced ROM comparisons: US `cd527c8c24e20e33913fc45199e64b3e6138a6e5`, EU `6d20a586e7bb52b4a9352bbeaec9d7be65e93369`.

## sub_805E320 (0x0805E320)

- Matched the explicitly requested single-segment intersection test with the existing geometry and result types. Its real C caller is sub_805D548; the current mainLoop callgraph does not list this function.
- The final segment index is a wide s32 parameter: the target loads the complete stack word for indexing, helper forwarding and the result store. Updated the local prototype from s16 to s32; sub_805D548 still matches every instruction, including the signed load from QuadTreeSplineEntry.unk4. No header or layout changes.
- Direct `pointIndices[index]` and `pointIndices[index + 1]` accesses reproduce the target allocation. Advancing the pointIndices pointer first changes stack allocation from 28 to 32 bytes at offset 0xA.
- Reuse the first endpoint byte flag for the intersection result. A separate byte result local adds a move at offset 0x104 after normalizing into r0 rather than directly into r4.
- The cached pointIndices base is byte-required: folding it into spline->pointIndices first changes the load at offset 0x18 and removes the early base-address setup. Signed bounds and endpoint coordinates retain the target signed comparisons; the remaining locals represent separately reused endpoints, bounds, flags and result state.
- Every instruction matches, including the existing caller. The direct positive intersection branch first diverged at offset 0xDE (`bne 0x140` target versus `bne 0x13A` probe); the `found` flag and separate test are retained. Removing `found` for an early-return form changed the frame allocation at offset 0x0A from 28 to 24 bytes. A separate `unk8 intersection` local first diverged at offset 0x102 (`lsr r4, r0, #24` target versus `lsr r0, #24` followed by `mov r4, r0`), so the first endpoint flag remains the result source.
- Formatted C passes US SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5 and EU SHA1 6d20a586e7bb52b4a9352bbeaec9d7be65e93369. No artificial matching constructs or rule-breaking source shapes.

## sub_805DF04 (0x0805DF04)

- Matched the explicitly requested spline-angle interpolation. No C/assembly call sites were found. The unused first parameter is retained as LevelGeometryAddresses* consistently with neighboring helpers; the used arguments are GeometrySpline* and signed position, with a signed halfword return proven by the final asr #16.
- Uses the existing trailing GeometrySplineLine array after pointIndices. No shared field types or headers changed. Signed halfword snapshots of the neighboring angles produce the target sign extensions and equality comparisons without field casts.
- A halfword fraction is byte-required: changing s32 fraction to s16 recovers the target prologue and mask allocation. Signed delta and amount follow the target sign tests and asr operations. An explicit if/else for the absolute value matches; a ternary removes the target mov at offset 0x62 and simplifies subsequent halfword normalizations.
- Assigning the cached count inside the clamp comparison preserves the target shift followed by count copy. An earlier assignment reverses those operations. Folding count adds an initial mov at offset 0x2; folding the lines pointer adds saved high-register setup at offset 0x2. Retained both.
- The currentEnd halfword snapshot was also fold-tested; direct field comparison changes the signed comparison semantics and emitted instructions, so the typed snapshot remains. The other angle snapshots and arithmetic locals carry distinct values across multiple operations or branches.
- Folding `fraction` into `(position & 0x3FF)` changed the first instruction at offset 0x02 from `ldr r3, [r1, #0]` to `mov r4, r2`; the signed halfword fraction local is retained.
- Replacing `currentEnd` and `nextEnd` with direct field/control-flow tests changed the prologue at offset 0x02 and moved the literal pool; the cached endpoint locals are retained. The target's terminal `nextEnd = next` behavior is therefore expressed by the natural snapshot assignments.
- Every instruction matches. Formatted source passes US SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5 and EU SHA1 6d20a586e7bb52b4a9352bbeaec9d7be65e93369. No artificial matching constructs.
