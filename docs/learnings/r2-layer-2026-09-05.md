#Layer round 2 — 2026 - 09 - 05

## `sub_8059934` (`0x08059934`)

Matched with a `u8` loop index and a separate zero temporary. Reusing the zero temporary for the two initial byte stores reproduces the target's single `mov #0`; the `do`/`while (i <= 3)` loop reproduces the byte normalization after increment. The full-ROM compare passed.

## `sub_8059C18` (`0x08059C18`)

Parked with the strongest natural draft. The recovered operation is a byte read-modify-write for each BG control register: preserve all bits except bits 0-1 and OR the corresponding argument masked with `3`. The draft matches the prologue, byte normalization, calls, clear-mask setup, later register roles, and all instructions after the first mask use. The first divergence is at offset `0x34`: target emits `mov r1, sl` followed by `and r4, r1`, while the draft uses `and r4, r2`; this shifts the remainder by two bytes. Natural probes covering pointer lifetime, explicit result accumulation, temporary declaration/type changes, expression forms, and parameter width did not remove this copy without introducing other differences. The dump is retained for the ASM fallback.

## `sub_8058968` (`0x08058968`)

Matched using the existing `BGLayer` layout and the natural sequence of tile-coordinate setup, `sub_8058AA8`, hardware offsets, tile bounds, fixed-point positions, and conditional `sub_8059310` call. The historical semantic draft reproduced all 95 instructions exactly, including the stack argument reloads and field widths. The dump was removed after the function diff matched.
