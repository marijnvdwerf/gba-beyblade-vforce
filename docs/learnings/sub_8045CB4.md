# sub_8045CB4

Address: 0x08045CB4

Status: parked near-match; the ROM continues to use `asm/dump/8040d18/8045cb4.s`.

The best source draft is retained in `src/festate.c` immediately above the `INCLUDE_ASM` line inside `#if 0`.

## First divergence

With the best direct-global draft, the first differing instruction in case 1 is at function offset `0x2C6`:

- target: `ldr r5, [r5, #0]`, then `ldr r1, [pc, #168]`
- current: `ldr r1, [pc, #168]`, then `ldr r5, [r5, #0]`

The corresponding pair in the second animation block differs at offsets `0x310` and `0x312` in the same order. All instructions before the first pair and the remaining arithmetic shape match, aside from relocation-display and literal-pool address presentation.

## Controlled experiments

| Change | Result |
| --- | --- |
| Distinct case-1 locals `value`, `value2`, `scroll`, and `offset` | Matched the target prologue, state register (`r9`), font registers, scroll logic, animation arithmetic, and case 2. Kept in the draft. |
| Change music helper parameters from `u16` to `unk32` | Removed unwanted caller normalization (`lsl #21` / `lsr #16`) and matched the target shifted calls. Reverted while parking because the function remains asm. |
| Separate `phase` local assigned from `sub_8057C40()` | Added a move and substantially changed register allocation and instruction layout. Reverted. |
| Reuse `offset` for the `sub_8057C40()` return before computing the animation offset | Added/reshuffled instructions and changed register allocation. Reverted. |
| Case-local and nested `SpriteEntry*` aliases | Changed dispatch and pointer allocation; nested form shortened/reshuffled the block without matching. Reverted. |
| Reuse the existing `sprite` local as a pointer alias | Produced an extra move in the first block and did not fix the second block. Reverted. |
| Preload the existing pointer before the null test | Changed the null-test and downstream block layout. Reverted. |
| Reverse the commutative addition operands | Changed the arithmetic tree and generated extra instructions. Reverted. |

No artificial alias, volatile qualifier, raw offset expression, or compiler-specific lever was retained. The remaining mismatch is an instruction scheduling difference between the global pointer dereference and the independent scalar-global load.

The parked draft uses the proven typed fields and globals, including the packed two-byte `SpriteEntry.frame` whole/parts representation already present in the shared sprite layout.
