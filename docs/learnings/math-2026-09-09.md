# sub_8059FA0 (math.c, 0x8059FA0) — matched; unparks Leaf K

Target: `lsl/asr #16` both params, `mul`, `mov r1, r0`, `cmp r0, #0`, `bge`,
`add r1, #255`, `lsl r0, r1, #8`, `asr r0, #16`, `bx lr`.

Matching source:

```c
s16 sub_8059FA0(s16 a, s16 b)
{
    s32 p = a * b;

    p /= 256;
    return p;
}
```

Retracts `docs/learnings/processed/leaves-k.md` ("every natural form
coalesces the copy"): the copy survives when the dividend and the quotient are
**the same source object assigned in a separate statement**. agbcc's
`expand_divmod` branch variant emits `quotient = op0; if (op0 >= 0) goto lab;
quotient += 255; lab: quotient >>= 8`, and CSE rewrites the compare back onto
`op0`. Writing the quotient into a *different* pseudo (`q = p / 256`) or
letting the division be part of the return expression lets the allocator
coalesce the copy away.

Step table (identical prologue/epilogue in all rows; only the copy at +0xA and
the branch shape differ):

| form | result |
|---|---|
| `return a * b / 256;` | copy coalesced (12 bytes short of target) |
| `(s16)(a * b / 256)` | coalesced |
| `p = a*b; q = p; if (p < 0) q += 255; return q >> 8;` | coalesced |
| `p = a*b; q = p; if (p < 0) q = p + 255; return q >> 8;` | coalesced |
| `p = a*b; q = p / 256; return q;` | coalesced |
| `p = a*b; q = p + 255; if (p >= 0) q = p; return q >> 8;` | copy kept, but `add #255` hoisted before `cmp`, `blt`, second copy in arm |
| `(p + ((p >> 31) & 255)) >> 8` | branchless `asr #31; and; add` |
| **`p = a*b; p /= 256; return p;`** | **match** |
| `p = a*b; p = p / 256; return p;` | match (identical bytes) |

Return width: the `lsl #8; asr #16` pair is the `s16` return narrowing folded
into the `>> 8`; an `s32` return produces a second compare (already recorded in
leaves-k).
