# sub_8061F3C — 2026-09-18

Matched the formatter at 0x08061F3C in src/spritetext.c. US and EU full-ROM SHA1 comparisons pass.

The function allocates and clears 0x800 bytes, parses a format string using va_list/va_arg, appends ordinary characters and formatted conversions, calls sub_8061660, then frees the allocation. The two source switches reproduce both target jump tables. Modifier cases increment the cursor and continue parsing; unknown modifiers preserve the target's additional cursor increment.

## Interfaces

- Read the sole C caller, variadic wrapper sub_80622D0. Both functions match with void returns; the prior unk32 forwarding declaration was unnecessary. The wrapper now uses va_start, calls the formatter, then va_end. No other source callers use the wrapper's return.
- The formatter accepts va_list directly instead of an untyped word-array interpretation. Character arguments use a promoted word and the byte parameter of sub_8061E94 supplies the narrowing load; signed decimal uses s32 and string conversions use const unk8*.
- sub_8061E9C has seven target call arguments: the seventh stack slot is 4 for signed decimal and 0 for the other numeric cases. Added the unused unk32 arg6 parameter to preserve this call shape without undefined calls or hidden prototypes.
- Its zero-padding and uppercase parameters are wide unk32 arguments converted to unk8 locals inside the helper. Keeping the old narrow formals adds unwanted byte-normalization pairs in the formatter. Widening only zeroPad moves its conversion after uppercase; widening both and assigning zeroPad first restores the helper's original instruction order. The helper remains byte-identical.
- These measured interfaces supersede the earlier return-type and six-argument helper descriptions in spritetext-2026-09-17.md.

## Source-shape probes

- Initial plain while-loop draft had only a load-placement residual after the helper ABI and flag stack slots were corrected: the target loads the character into r1 at +0x4E, while direct repeated *cursor expressions reuse the loop-test value for the comparison and reload only in the append arm. A branch-local unk8 character snapshot used by both the comparison and append call exactly matches the target.
- Keep zeroPad, width, plus, alternate in that declaration order to reproduce the target flag stack slots: zeroPad +0x18, plus +0x1C, alternate +0x20.
- Moving all cursor advancement to one for-loop increment changes cursor/argument register roles and loop layout. Retain the per-path increments and continues.
- Folding cursor into the format parameter changes the frame from 0x24 to 0x20 bytes at +0x0A and changes saved-register roles. The separate advancing cursor is byte-required.
- The local constant size = 0x800 folds into the two allocation/clear arguments without changing instructions and was removed.
- Width accumulation retains `width *= 10; width += *cursor - '0';`, matching the target expression order.

No new layouts, header changes, volatile qualifiers, raw fixed-offset accesses, gotos, or fake return values were needed. sub_8061E9C and sub_80622D0 both have exact function diffs. The shared expected symlink already contains the original matching bytes and was not rewritten outside this worktree.
