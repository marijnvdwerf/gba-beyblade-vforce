# Palette matching notes

## sub_8063454 (0x08063454)

- The exact match uses `Palette *` and `unk16 *` parameters. `Palette::source` is an `unk16*` field; all nine palette functions using that field were re-diffed after the retype with no instruction differences. `Palette::unk8` is read with `ldsh`; the destination is `Palette::unkC.half`.
- The matched source keeps distinct `source`, `target`, and `destination` halfword cursors, plus `nextRow` and `nextIntensity` temporaries. The target stack slots are source `0x14`, target `0x08`, next row `0x0C`, and next intensity `0x10`.
- Declaration order is byte-required for the spill layout: the three pointers precede `nextRow` and `nextIntensity`, and `nextRow` precedes `nextIntensity`. This produces the target stores at `[sp,#0x10]` for next intensity and `[sp,#0x0C]` for next row.
- The target pointer is typed directly as `target = targetPalette + palette->unk4`; the cast-free spelling changed 0 instructions. The direct cached-target fold changed 38 instructions.
- Mask-before-shift RGB555 extraction is byte-required for the target sequence: green uses `(color & 0x3E0) >> 5` and blue uses `(color & 0x7C00) >> 0xA` for both source and target colors.
- The matched function uses a top-tested outer `do` loop and an inner bottom-tested loop. It interpolates each channel by `(target - source) * intensity >> 0xA`, clamps positive results above `0x1F`, and writes the packed halfword.
- The target color is intentionally constant across columns: the assembly loads the target cursor from `[sp,#0x08]` and performs `ldrh` at `0x5A` on every inner-loop iteration, while only the source cursor at `[sp,#0x14]` advances by `0xC0`-`0xC4` and the destination advances at `0xBE`.
- Signedness evidence: `Palette::unk8` uses `ldsh` at dump offsets `0x14` and `0xD8`; `step` is produced by the signed `__divsi3` call at `0x1A`; channel results use `asr #0xA` at `0x8C`, `0x94`, and `0x9C`; outer and inner counters use signed `bge` at `0x2A`/`0x4C` and `blt` at `0xCE`/`0xDC`.
- Fold tests: folding `step` changed 81 instructions, folding the `source` load/increment into `*source++` changed 16, folding the cached `target` changed 38, folding `nextRow` changed 4 (including the `[sp,#0x0C]`/`[sp,#0x10]` swap), and folding `nextIntensity` changed 24. These shapes are retained as byte-required. Folding `red2`, `green2`, and `blue2` changed 79, 27, and 36 instructions respectively; those temporaries are retained. Folding the destination update into `*destination++ = ...` changed 0 instructions and is the final form.
- `bun run tools/diff/diff.ts sub_8063454` reports no differing instructions, and the US ROM SHA1 comparison passes with `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
