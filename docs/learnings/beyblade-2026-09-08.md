# Beyblade table getter matching notes

## sub_80573C8 (0x080573C8)

Matched in `src/beyblade.c`, body size 0x10. The target computes an index times
12 and returns the address of that fixed-size record in `_807b858`. Declaring the
table as `extern const unk8 _807b858[][12]` and returning `_807b858[arg0]`
produces the target `mov r1,r0; lsl/add/lsl` sequence and the same literal-pool
relocation.

No local temporary is byte-required. The parameter remains `unk32`; the target
uses a word-sized index and shows no signedness evidence. No casts, raw offset
dereferences, or artificial control-flow shapes are present.

`cmake --build build --target compare` passed, and the assembly dump was
removed after the exact function diff matched.

## sub_80573DC (0x080573DC)

Matched in `src/beyblade.c`, body size 0x20. The target saves the record index
across `getLanguage`, indexes the five-entry `_807BFD8` language table, then
indexes that language's pointer row. The source expression
`_807BFD8[getLanguage()][arg0]` emits the target prologue, two scaled additions,
and final load.

No local temporary beyond the ABI-preserved index is byte-required. Both the
language result and record index are word-sized; the target provides no signedness
evidence. No casts, raw offset dereferences, or artificial control-flow shapes
are present.

`cmake --build build --target compare` passed, and the assembly dump was removed
after the exact function diff matched.

## sub_80573FC (0x080573FC)

Matched in `src/beyblade.c`, body size 0x20. This has the same language-row
lookup shape as `sub_80573DC`, using the distinct five-entry `_807C49C` table.
The source expression `_807C49C[getLanguage()][arg0]` reproduces the target
prologue, indexing, final load, and literal-pool relocation.

No local temporary beyond the ABI-preserved index is byte-required. The language
result and record index are word-sized, with no signedness evidence in the
instructions or matched call sites. No casts, raw offset dereferences, or
artificial control-flow shapes are present.

`cmake --build build --target compare` passed, and the assembly dump was removed
after the exact function diff matched.
