# sub_805F47C — 2026-09-18

Matched 0x0805F47C in src/anim.c. The 0x2B4-byte function advances animation frames, handles direction changes and repeat limits, and invokes the frame callback. US and EU ROM SHA1 checks pass.

## Source shape and measured probes

- Compute the current AnimFrameEntry pointer and signed speed sum before the resource guards. Folding the entry pointer changes the prologue from saving r4–r6 to saving r4–r5. Folding speed removes the target's early loads at +0x0E onward.
- Direction changes use a combined sign-change test followed by a direction-specific update. Separate outer direction arms place the +2 adjustment before the +1 adjustment; the first branch difference is +0xB0 and the preceding literal placement differs at +0x94. The combined predicate reproduces the target branch order and literal pool exactly.
- Keep the duplicated unk2E assignment in the two direction arms. The retained source has no goto, raw offset access, field cast, volatile, or artificial scope.
- The repeat-counter saturation is `unk3C |= -1`. Using `|= 0xFFFF` instead collapses the operation to a constant store, removing the target's literal load and OR at +0x194 and the equivalent operation in the reverse arm.
- The reverse wrap expression uses `unk2A + entry->unk2 - 1`; swapping the operands changes the add at +0x240.
- The byte `advance` local starts at 1 and is overwritten by an optional callback. Folding it into a short-circuit condition changes the callback arm and literal placement (first displayed difference +0x18E). Retain the local.

## Proven layout and types

Animation layouts now have a single declaration in src/anim.h, moved from src/anim.c. The layout is unchanged, with padding split only for newly accessed fields.

- `unk2A`, `unk2C`, `unk3A`, `unk3C`, `unk40`, and `unk44` are s16, proven by target signed halfword loads and signed arithmetic/comparisons. `unk38` and `unk58` are newly exposed s16 fields with signed halfword loads and sign tests. Remaining padding and later offsets are preserved.
- The callback returns unk8: both event-1 calls normalize r0 with lsl/lsr by 24 before testing the result. The event-2 return is discarded.
- Signed halfwords can still use ldrh for arithmetic whose result is stored back to a halfword; this function demonstrates both views without a union or cast.

## Existing users

Read the sole C caller, sub_805F400; it passes one state pointer and discards the void result. The user explicitly selected this function.

Diffed sub_805F1E4, sub_805F730, sub_805F784, sub_805F7FC, and sub_805F848 after the type changes. All remain exact after adjusting sub_805F730:

- Preserve its existing unk16 `frame` and add an unk16 `current` comparison snapshot of the now-signed unk2C. Without this conversion local, the first difference is +0x14, where the compiler reloads unk2E instead of loading unk2C and then changes register allocation. The later helper call still uses the signed field directly, matching its ldrsh. The target comparison itself uses ldrh.

Both final full-ROM comparisons pass. The expected snapshot already contains the original matching bytes; its shared symlink was not rewritten outside this worktree.
