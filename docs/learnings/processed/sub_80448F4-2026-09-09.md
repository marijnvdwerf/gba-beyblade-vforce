# sub_80448F4

Matched at 0x080448F4 in src/festate.c. Reachable through the frontend handler table; the existing dispatcher passes FrontendState* and an event word. No rule-breaking shapes.

The existing Packet fields recover the packet type 8 and response operations. Assigning packet.unk2_0 = 8 followed by packet.unk2_4 = 0 emits the target mov #0x10, neg, byte read, mask, or #8, and #0xF sequence. The disabled draft's explicit mask temporary and manual bit arithmetic are unnecessary.

The target loads signed packet byte 4 into r1 before comparing it with menu.selection and retains it for sub_8050DF8. Passing record->packet.unk4[0] as the second argument corrects the disabled draft's missing argument.

RAM 030001F0 and 030001F4 are sprite pointers. RAM 030001F8 and 030001FC are signed positions, proven by subtraction and asr #3. The byte flag at 03000200 uses its proven byte width while preserving the original four-byte storage extent. Existing menu descriptor and packet declarations suffice; no new packet layouts or casts.

Full-ROM fold trials for data, packetState, record, and the sprite allocation result all changed bytes, so those locals remain. The data alias preserves one _gameData load across three byte clears. The explicit sprite local preserves allocation-result staging before the global store. Direct menu member expressions and inline timer calls match without extra locals.

After clang-format, the instruction diff is exact and cmake --build build --target compare passes. ROM SHA1: cd527c8c24e20e33913fc45199e64b3e6138a6e5.
