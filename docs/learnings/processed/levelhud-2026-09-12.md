# levelhud: sub_804F878 (0x0804F878)

Matched. `src/levelhud.c`, 316 B. Measured against `expected/CMakeFiles/rom.dir/src/levelhud.c.o`.

## LevelDescription language tables at 0x68/0x7C

The target computes `add r1, r6, #0x68` and `add r1, r6, #0x7C` from the same
`description` pointer, each followed by `getLanguage() * 4`. `src/common.h` had
`const unk8* unk68[6]; unk8 pad80[0x10];`. Split into `unk68[5]` + `unk7C[5]`:
same total extent (0x68..0x90), `sizeof(LevelDescription) == 0xD0` unchanged,
and it fits the neighbouring 5-entry language tables at 0x40/0x54/0x94/0xA8.
Only other reader is `src/festate.c:2923` (`unk68[getLanguage()]`, index < 5);
full ROM compare unchanged after the split.

## Base-pointer roles

r7 = `state + 0xF50` covers text0 (+0), text0.unk14, motion0 (+0xC0),
status (+0xF0) and state (+0x10A), all as `mov rN, r7; add rN, #imm`
=> `LevelHudData* hud = &state->levelHud;` (byte-required temp).
The else-arm's text1 (0xF80), text1.unk14 (0xF94) and motion1 (0x1028) are
materialized from r5 with pool literals, so those stay `state->levelHud.*`.
Writing every access as `state->levelHud.*` emits `ldr rN,[pc]; add rN,r5,rN`
for motion0/status/state instead of the r7 form.

## The dead `mov r4, #0` at 0x24

Target emits a never-read `mov r4, #0` between `mov r0,#2` and `ldrh r2,[r1]`
of `flags |= 2`. It comes for free from direct `state->levelHud.flags |= 2;`
(agbcc CSEs `state + 0x105E` into r8 and keeps it across every call); a
`u16* flags = &state->levelHud.flags;` alias produces the identical body minus
that instruction.

Step table (change -> first divergence / size delta):

| # | change | result |
|---|--------|--------|
| 1 | `u16* flags` alias + `SpriteTextCleanup* hud = &state->levelHud.text0` | all instructions present, r7/r4 roles swapped (r7 held flags); motion0/status/state from `state` + pool literals |
| 2 | `LevelHudData* hud = &state->levelHud;` for the r7-derived accesses | only diff: missing `mov r4,#0` at 0x24, function 2 B short |
| 3 | `motion = NULL;` before `flags \|= 2` | flow deletes the dead init, no change |
| 4 | `unk32 showText1` (0, set 1 in else arm, `if (showText1) flags \|= 4;`) | keeps `mov r4,#0` but adds `mov r4,#1` + `cmp r4,#0; beq` (+6 B); also shows the init is emitted in source order w.r.t. `flags \|= 2` |
| 5 | drop the alias, direct `state->levelHud.flags \|= 2;` / `\|= 4;` | exact match |

## getLanguage() duplicated in both arms

The `_currentGameState->unk6A4 == 2` arms end with `add r1,r6,#0x94` /
`add r1,r6,#0xA8` and share the tail from `add r1,r1,r0` onwards: cross-jumped
identical tails, so the source has a complete `sub_8061660(...)` call in each
arm, not a shared `table` temp assigned before one call.

Byte-required temps: `hud`. `motion` is multi-use in both branches.

## Retained measurements

- sub_8050C18 (0x08050C18): first divergence is the frame-toggle block, where the draft is two instructions short.
- newIconMenu (0x08050A7C): first divergence is the final selected-item address computation, with only register allocation differing.
- Passing `x >> 8` to an `s16` parameter is byte-identical to `(x << 8) >> 16`; the parameter conversion supplies the `asr #16`.
