# sub_804B4FC

Matched at 0x0804B4FC in `src/rider.c`. The function is called from `mainLoop` in `src/gameloop.c`; no leaf functions were changed. The required forward declaration for the later same-TU `sub_804B624` definition was added to `src/rider.h`.

The target keeps the incoming `target` pointer in r8 and the incoming `item` pointer in r5 across the initial `sub_804B8F0` call. Direct `_gameData` accesses preserve the address of `_gameData` in r4 and copy it to r7 for the rider loop. The first `sub_804B8F0` uses `&_gameData->base`, which is emitted as the loaded `_gameData` pointer because `base` is at offset zero.

When `_gameData->unk1618` is zero, `s32 i` is initialized and guarded by a signed comparison, then a do/while walks `&_gameData->unk42C[i]`. The typed RiderBase indexing emits the target's separate byte-stride induction in r6 (`0x428`) and count in r5, with the array base reloaded from `_gameData` each iteration. The rider predicate and conditional `sub_804B8F0` call match exactly. The VCOUNT reads are preserved as discarded `*(vu16*)REG_VCOUNT` expressions. In the other branch, `sub_80501F8(item, _gameData->unk42C)` is called for side effects; `sub_804B754` takes no source argument and is called directly afterward.

The final source emits the target function exactly, including its 0xC0-byte body and trailing literal pool. No struct or global layout changes were needed, and no raw offset arithmetic, artificial temporaries, casts on fields, volatile locals, or comments were added.

Measured source-shape result:

| Shape | Result |
| --- | --- |
| `&_gameData->unk42C[i]` with `s32 i` and a guarded do/while | Exact instructions, including the target 0x428 induction stride and register allocation. |
| Explicit byte-offset cursor and cast from `(unk8*)_gameData->unk42C` | Replaced by typed indexing; the final source uses the fixed-stride RiderBase array shape. |
| Assigning the `sub_80501F8` result to a value and passing it to `sub_804B754` | Not used; current declarations show `sub_80501F8` and `sub_804B754` are void, and the target call sequence matches side-effect call followed by `sub_804B754()`. |

`cmake --build build --target compare` passed with the baseline SHA1 check.
