# sub_804712C

- The matched function is `sub_804712C` at `0x0804712C` in `src/festate.c`.
- `FrontendBladeState::unk34` and `unk35` are read as signed bytes. Keeping the saved index in an `s32` local preserves the target's single `ldrb; lsl #24; asr #24` normalization and its long-lived `r9` value. A narrow index local introduced an extra byte reload before `getBeybladeData0`.
- `_806E240` is an 11-row by 5-language pointer table and `_806E31C` is a 3-row by 5-language pointer table. The source shape `_806E240[row][language]` / `_806E31C[row][language]` matches the target's one language scale plus fixed row-byte offsets.
- The `hasSprites` flag remains a wide local and the returned `sub_80573C8` pointer remains a wide pointer local. These lifetimes reproduce the target's `sl` flag and `r8` pointer across the switch and later calls.
- The three frame updates require conditional expressions of the form `values[n] != 0 ? values[n] - 1 : 0`. Direct `if/else` field assignments were optimized into a branch-to-common-store form: the first divergence was at target offset `0x2E2`, with the target's literal pool at `0x314` and its explicit branch over the zero arm. Converting the first, second, and third assignments to conditional expressions successively removed divergences at `0x2E2`, `0x32E`, and `0x34C`.
- The final `bun run tools/diff/diff.ts sub_804712C` output has no differing instructions, and `cmake --build build --target compare` passes.
- The coordinator's fold test was byte-identical: removing the `s8 category` local and switching directly on `state->unk35` passed `cmake --build build --target compare`. The direct switch form is retained.
