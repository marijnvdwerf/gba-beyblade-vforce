# sub_804257C — matched

Frontend callback reached through the mainLoop handler tables. The dispatcher
uses void (*)(FrontendState*, unk32); the old disabled three-argument stub had
no supporting call-site evidence. The active definition follows the dispatcher.

ROM SHA1: cd527c8c24e20e33913fc45199e64b3e6138a6e5.

## Types and layout

- _unk30000DC and _unk30000DD are byte flags, proven by ldrb/strb.
  The old halfword placeholder named _unk30000DD actually aligned to 0x030000DE.
  Changing it to unk8 puts the symbol at the target 0x030000DD; automatic padding
  keeps the following word at 0x030000E0. The data bytes remain all zero.
- _unk30000E0 is s32: case 2 uses bgt for the comparison against 4.
- _unk30000E4/_unk30000E8 are s32: their subtraction feeds asr #3 and E4 also
  feeds asr #8/#9. Unsigned arithmetic would change the shift semantics.
- _unk30000EC/_unk30000F0 are s32: case 8 uses signed bge for their ordering.
- _unk30000F4 is unk32: only word arithmetic and equality tests are proven.
- All active C uses of these globals were searched; only this new callback
  consumes them. Their canonical declarations are in transition.h and their
  existing definitions were upgraded in ram.c.
- _80687F0 contains five 8-byte image/palette records. TransitionImage has a
  TileMapHeader* at +0 and a palette pointer at +4, matching the two loads and
  the typed layer/palette APIs. The table declaration is local to transition.c.
- Added the existing sub_8058A28 definition's prototype to layer.h; no function
  ABI or struct layout was changed.

## Source shape and fold tests

Explicit no-op cases 3 through 7 are needed to recover the 0..8 jump table.
Case-body order is 0, 1, 8, 2, matching the target assembly.

The layer subobject pointer and image-table pointer are initialized before
hardware blanking and sub_8059934. Both are byte-required:

| Fold | First non-relocation divergence |
| --- | --- |
| Inline layer subobject address in sub_8058A28 call | +0x8: incoming state moves to r8 instead of r6 |
| Inline image-table reference | +0x1F2: table address load disappears and moves after sub_8059934 |
| Remove captured done byte; use direct flag test and literal zeroes | ROM remains byte-identical; local removed |

The final implementation uses no value-less returns, raw field offsets, scalar
pointer aliases, register pinning, volatile matching tricks, or padding hacks.
Only hardware accesses use volatile. m2c supplied the semantic draft; local
diff then reported only raw-address versus _unk30000DD relocation displays.
Final full-ROM SHA1 passed after formatting and removing the assembly dump.
