# sub_8063F84 (0x08063F84, src/teletype.c) — matched 2026-09-10

Teletype update loop. Previously parked as a TeletypeStateUpdateDraft
`#if 0` block (never measured in this session's shape).

Step table (non-.word differing rows):
- natural draft on TeletypeState, `switch (*cursor)`, direct `state->unkBA`
  (no alias), `(x & 0xFFF7) | 4` masks → 45
- switch head `current = state->unk0; code = current[0]; cursor = current;`
  → 12 (reproduces target `mov r2,r1` code copy + `mov r3,r0` cursor copy).
  Each of the three is byte-required: dropping any one → 36.
- `s32 unkA4/unkAC/unkB4` (header) → 11. Evidence: target `ble` on
  `unkAC + unkB4 > unkA4` (0xF2); all three must be signed (s32 + unk32
  promotes to unsigned → `bls`). Other users (sub_8063AD8, B44, BA8, CD0,
  CF4, E18) re-diffed: 0 rows each.
- `unk8 sub_8063B44` return (definition) → caller's `lsl r0,#24; cmp`
  appears (0x146); B44 itself still 0 rows. Replaces the draft's
  `(sub_8063B44(state) << 24) != 0`.
- `state->unkC0 + state->unk12` (not `unk12 + unkC0`) → 8: target computes
  &unkC0 first, then loads unk12, unkC0, `add r0,r1,r0`. Swapping → 3.
- Remaining 8 rows were ONLY the prologue/epilogue saving an unused r9
  (`push {r6,r7}` with r8+r9) plus pool offsets shifted by 2 bytes.
  Neutral (all stay 8): `unk16 *loopFlags` alias in loop (draft shape),
  alias over whole function (34), alias from loop to end (10), `~8` vs
  0xFFF7 spellings, `x = x | 2` vs `|=`, `s32 process`, declaration order,
  `unk32 code` (34), `cursor = current = state->unk0` (10).
- FIX (user suggestion): flag updates as two statements,
  `state->unkBA &= ~8; state->unkBA |= 4;` → 0. flow deletes the first
  (dead) strh so the output still has a single ldrh/and/orr/strh, but the
  extra intermediate pseudo pushes global allocation onto r9, which later
  vanishes — leaving the target's saved-but-unused r9. Splitting either
  in-loop update alone (the `| 4` or the `| 0x10` one) is enough → 0;
  splitting only the post-loop `&= ~0x10; |= 8` → 8. All three kept split
  for consistency.
  Generic lesson: a callee-saved high register pushed but never used in the
  body can come from a split read-modify-write whose first store flow
  deletes; try `&=` / `|=` on separate lines before chasing the loop pass.

Follow-up (same day): the matched siblings also accept the split style,
now used throughout teletype.c:
- sub_8064140: `unkBA &= ~0x18; unkBA |= 4;` → 0. Three statements
  (`&= ~8; &= ~0x10; |= 4`, either order) → 3.
- sub_806415C: `unkBA &= ~4; unkBA |= 0x10;` → 0.

Measured shape tests (2026-09-10):
- Test 1, reverted: changed `TeletypeState.unkBA` to a 16-bit bitfield with `unkBA_1`..`unkBA_4` for masks 2, 4, 8, and 0x10, and rewrote every `rg unkBA src` user as bit assignments/tests. Every user was re-diffed. First divergences were sub_8063E18 +0x0A (target `mov r4,r0`, trial `mov r5,r0`), sub_8063F84 +0x02 (target `mov r7,r9`, trial `mov r7,r8`; bitfield access also became byte loads), sub_8064140 +0x04 (target pool `ldr`, trial `mov r1,#9`), sub_806415C +0x0A (target pool `ldr`, trial `mov r0,#5`), sub_806417C +0x02 (target `ldrh`, trial `ldrb`), and sub_8064188 +0x02 (target `ldrh`, trial `ldrb`).
- Test 2, reverted: hoisted the shared `state->unkBA &= ~8` above the default-case `if/else`. First divergence in sub_8063F84 was +0x02 (target `mov r7,r9`, trial `mov r7,r8`); compare failed.
- Test 3, reverted: removed the local callback value and passed the parser out-parameter through `state->unkA8` (`sub_8063D38(cursor, &state->unkA8)` / callback with `state->unkA8`). First divergence in sub_8063F84 was +0x02 (target `mov r7,r9`, trial `mov r7,r10`); compare failed.
