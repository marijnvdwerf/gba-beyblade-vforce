# sub_804F05C

At 0x0804F05C, hoisting `SpriteTextCleanup* text2` to function scope and assigning it immediately before the first `sub_8061844` call removes the bare lifetime block. The final `sub_8061C48` call remains a direct `&state->text2` access. The block-free form produced an instruction-identical `sub_804F05C`; the full ROM compare passed. ROM SHA1: `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
