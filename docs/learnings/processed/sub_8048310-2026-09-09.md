# sub_8048310

Matched at 0x08048310 in src/festate.c. Reachable through the frontend handler table in asm/data12.s. The dispatcher passes FrontendState* and an event word. No rule-breaking shapes.

Uses the existing Packet type for packet type 5, its high-nibble response, and signed payload byte 4. The target retains that signed byte in r1 for sub_8050DF8; the old disabled draft omitted this second argument.

RAM 030005C0 and 030005C4 are sprite pointers. RAM 030005C8 and 030005CC are signed positions, with subtraction followed by asr #3 in the target. Definitions preserve their four-byte extents. The menu descriptor is declared as const FrontendMenuData.

The target reloads _currentGameState after each byte store. Removing the old draft's cached currentGameState alias recovers those loads. In the selection-zero arm, storing literal zero to unk6A9 reuses the tested value in r3 and matches. Reloading state->menu.selection after the unk6A4 store first diverges at function offset 0x2B0 and introduces another selection load. No additional selection temporary is necessary.

As in sub_804868C, the connected case 2 branch precedes the offline else arm, and packet sends in cases 0, 1, and connected case 2 merge to the target shared call site. Direct menu member expressions and inline sub_8057C40 calls suffice.

Each remaining temporary was fold-tested against the full ROM SHA1: packetState, record, and the sprite allocation result all change bytes when folded, so they remain.

After clang-format, the instruction diff is exact and cmake --build build --target compare passes. ROM SHA1: cd527c8c24e20e33913fc45199e64b3e6138a6e5.
