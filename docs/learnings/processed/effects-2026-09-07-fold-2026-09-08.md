# effects matching notes (2026-09-07)

## sub_8055B64 (0x08055B64)

The matched source uses `EffectSprites *` and an `unk8` selector directly in the condition. The narrow parameter emits the target `lsl #24` before the compare without a cast. The two branch-local stores and the `EffectSprites` fields produce the target branch layout and no extra temporaries.

## sub_8055C4C (0x08055C4C)

The four zero stores match with a typed `DecompressorState *` and direct field assignments in offset order. The state layout is 16 bytes: `AllocatedBlock *block`, `unk8 *source`, `unk8 *data`, and `unk32 size`. `_unk3000140` was upgraded in place from the 16-byte RAM placeholder to this type; its size and address are unchanged.

## sub_8055CA0 (0x08055CA0)

The matched source tests and passes `state->block` directly, then clears that field after `deallocateBlock`. This preserves the target's loaded block pointer across the call, `r4` state alias, and post-call zero store without an artificial local.

## getDecompressorData (0x08055C58)

The matched source keeps a separate `unk8 *data` initialized to `NULL`, frees an existing block before updating source and size, and writes `state->data` after either the decompression call or allocation-failure print. A mutable `unk8 *source` parameter and `state->source` field allow the direct `LZ77UnCompWram(source, data)` pointer argument; this cast-removal attempt is byte-identical. Reading the compressed header through `*(const unk32 *)source` emits the target word load and `lsr #8`. The local data lifetime is required for the target `r6` zero initialization, block-address result, failure path, final store, and return. The error string is referenced as `Str_8729780`; the literal pool and all instructions match.
