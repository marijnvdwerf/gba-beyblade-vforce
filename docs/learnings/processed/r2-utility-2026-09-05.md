# Round 2 utility — 2026-09-05

## `renderActor2` (`0x08058778`)

Matched with the direct wrapper `renderActor(actor, 0)`. The typed prototype
`void renderActor(Actor*, unk32)` produces the target's `push {lr}`, zero in
`r1`, direct `bl renderActor`, and `pop {r0}; bx r0` sequence.

## `sub_8055274` (`0x08055274`)

Matched by caching the embedded `ProjectileSystem` record before reading
`unk2C`:
`ProjectileSystem* projectileSystem = &_gameData->projectileSystem;` followed
by `return projectileSystem->unk2C;`. The record pointer preserves the target's
separate `_gameData + 0x1084` address formation and `ldr #0x2C`.
Fold experiment verdict: folding changed the load to `[r0, #0]` and the pool literal to `0x10B0`, so the cached record pointer is retained.

## `sub_8055288` (`0x08055288`)

Matched with the same cached `ProjectileSystem*` record shape as
`sub_8055274`, returning `projectileSystem->unk30`. This preserves the target's
separate `_gameData + 0x1084` formation and `ldr #0x30`.
Fold experiment verdict: folding changed the load to `[r0, #0]` and the pool literal to `0x10B4`, so the cached record pointer is retained.

## `sub_8060070` (`0x08060070`)

Matched with a direct whole-word flag test on `_unk3005DC4->unk14`:
return `1` when bit `0x2` is set and `0` otherwise. The unsigned `unk32`
status field yields the target's `ldr`, `and #2`, compare, and two return arms.

## `sub_8061C48` (`0x08061C48`)

Matched as a byte-returning wrapper around `sub_8061228` and `printTime`:
`sub_8061228(arg0); return printTime(arg0, arg1, arg2);`. The `unk8` argument
and `u8` return prototypes account for the target's explicit byte
normalization before the call and after the return.
