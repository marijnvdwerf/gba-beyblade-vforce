# sub_806014C

Matched in src/multiplayer.c; ROM SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5.
No header or layout changes. Main checkout, uncommitted.

The previous disabled draft used the wrong registers for the timer setup:
0x0400010C and 0x0400010E are REG_TM3CNT_L and REG_TM3CNT_H.
The final flags mask is ~0x20 (target mov #0x21; neg), not 0xFFFFFFDE.
Connected processing precedes the handshake branch in the emitted code.

Byte-required state: player snapshots unk3 before calls; replacing it with
field reads changes the prologue at +0xA (stack allocation disappears).
localFlag carries the branch result across calls; matchCount is the incremented
halfword handshake count, with lsl/lsr #16 normalization. The cached flags
local folds into direct reads with identical ROM bytes and was removed.
Counter staging was removed in favor of direct field increments/stores.

Two value-less returns in the non-void function preserve target r0:
- At 0x806021E, the unset 0x200 test branches straight to the epilogue.
- After sub_8060404 at 0x8060370, the target likewise branches to the epilogue.
These follow the agbcc skill's measured undefined-return exception. They do not
supply a defined C return value. Replacing the first with return 0 or returning
a local containing the tested mask merges the early returns: the first branch
at function +0x2A becomes bne to +0x6C rather than beq to +0x34. The resulting
function is four bytes shorter and fails SHA1. Retained return width unk8 agrees
with all three C callers and the existing public declaration.

The callgraph confirms mainLoop reachability through the three existing callers.
m2c was run as a semantic cross-check; it treats sub_8060404 as returning a value,
whereas the established C definition is void. The requested residual-analysis
file was absent from docs/learnings.

Mask spelling probes in sub_806014C were run independently, retaining each
because the complete ROM compare stayed byte-identical:

- `0xFFFFFBBF` to `~0x440`: retained, no divergence.
- `0xFFFFFF7F` to `~0x80`: retained, no divergence.
- `0xFFFFFDFF` to `~0x200`: retained, no divergence.
- `0xFFFFFFDF` to `~0x20`: retained, no divergence.
