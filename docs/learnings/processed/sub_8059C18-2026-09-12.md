# sub_8059C18

Matched at 0x08059C18 in src/layer.c by assigning each argument directly to
BGControl.unk0_0 through GetBGLayerCntPtr(0..3). The existing two-bit field
produces every target instruction, including the shared mask registers and
byte read-modify-write operations. No temporary locals remain.

The entry normalizes all four arguments with lsl/lsr #24. The definition and
header use unk8 parameters. The sole C call is in a disabled camera.c draft;
the corresponding active assembly caller is 805e8d8.s. The mainLoop callgraph
includes sub_8059C18.

No struct layout or shared field type changes were needed. The pointer casts
select the existing hardware-register bitfield type, as in sub_8059CB4;
there are no casts on struct-field values or raw offset accesses.

Instruction diff was exact and the full ROM SHA1 comparison passed after
formatting and rebuilding the header's consumers.
