# sub_8063220

The matched target has a 40-byte local frame. The matching source shape uses `nextHeight` and `nextIntensity` locals assigned at the top of each outer-row iteration and copied back to `height` and `intensity` after the inner loop. These locals produce the target stack slots at `0x18` and `0x1C`; the source and destination pointers then occupy `0x10` and `0x14`, and the clear-size temporary occupies `0x20`.

The initial matching source used six distinct signed delta locals (`deltaRed`, `deltaGreen`, `deltaBlue`, `deltaRed2`, `deltaGreen2`, `deltaBlue2`), while a single reused `delta` local produced a 36-byte frame and kept the delta in registers. Each individual delta fold was byte-neutral, and applying all six folds together also preserved the target 40-byte frame, reused spill slot at `0x24`, and exact `str`/copy-before-multiply sequence. The final source therefore inlines all six signed delta expressions.

Measured type evidence:

- `palette->unk8` emits `ldrsh`; `height` is `s32`.
- Width and column comparisons emit unsigned `bcs`/`bcc` without narrowing normalization; `width` and `col` are `unk32`.
- Packed source words use `lsr` extraction; `color` is `unk32`.
- Target color arithmetic and interpolation use signed `asr`; component values and deltas are `s32`.
- The three target-color parameters are `unk32` in the final matched definition and header. The earlier `s32` formal version and this `unk32` formal version produce identical instructions, so incoming parameter signedness is not byte-required. The signed `s32` locals remain required for the interpolation operations.

Caller bookkeeping:

- The real caller is `src/envactor.c:466`: `sub_8063220(palette2, 0x1F, 8, 8);`.
- The caller uses positive constants. Changing the prototype from `s32` formals to `unk32` formals produced no caller instruction change and retained a zero-instruction function diff.

Independent temporary folds, each started from the matched `unk32`-formal baseline and verified with `cmake --build build --target compare` plus `bun run tools/diff/diff.ts sub_8063220`:

- `nextHeight`: folding the staged value into the height update failed compare. The frame stayed 40 bytes, but the first divergence was at function offset `0x14`; the candidate had 62 divergent instructions. The target begins the row setup with `mov r0, #8`, while the folded candidate begins with `mov r1, #8`. `nextHeight` is byte-required.
- `nextIntensity`: folding the staged value into the intensity update failed compare. The frame stayed 40 bytes, but the first divergence was at function offset `0x60`; the target stores at `[sp, #28]` while the candidate stores at `[sp, #24]`, with 8 divergent instructions. `nextIntensity` is byte-required.
- `deltaRed`: inlining `redTarget - redValue` into the interpolation expression passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaRed` is not byte-required.
- `deltaGreen`: inlining `greenTarget - greenValue` passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaGreen` is not byte-required.
- `deltaBlue`: inlining `blueTarget - blueValue` passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaBlue` is not byte-required.
- `deltaRed2`: inlining `redTarget - red2` passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaRed2` is not byte-required.
- `deltaGreen2`: inlining `greenTarget - green2` passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaGreen2` is not byte-required.
- `deltaBlue2`: inlining `blueTarget - blue2` passed compare with zero divergent instructions. The frame stayed 40 bytes and the target spill behavior was unchanged. `deltaBlue2` is not byte-required.
- Both `if (col < width)` guards changed independently together to `if (width > 0)`. Compare failed with candidate SHA1 `ddb71fbc53e934b0a0b5bf3c5cba19450162bdfd` versus expected `cd527c8c24e20e33913fc45199e64b3e6138a6e5`. The frame stayed 40 bytes; the first divergence was at function offset `0x68`, where the target has `cmp r9, r10` and the candidate has `cmp r6, #0`, with 32 divergent instructions. The existing `col < width` guards are byte-required.

The matched final source retains both staged row temporaries and inlines all six delta expressions. Applying all six independently byte-neutral folds together also passed compare with zero divergent instructions, so none of the six delta locals is retained. There is no standalone bare `{ ... }` scope block in the matched source: the braces around the `while` and `do` bodies are required compound statements, not an extra lifetime-shaping block. No bare-block removal probe is applicable.

Near-match measurements from the original matching pass:

- Function-scope component variables with direct interpolation first diverged at `0x0000000A` (`sub sp, #36` versus target `sub sp, #40`); setup and all non-interpolation instructions otherwise matched after declaration reordering.
- Adding explicit next-row state changed the setup to the target pointer/register roles but retained a 36-byte frame and kept interpolation deltas in registers.
- Replacing one reused delta with six per-component deltas removed all instruction differences.
