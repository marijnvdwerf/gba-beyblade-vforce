# sub_80526C8 — parked

The improved typed draft remains under #if 0 in src/gameloop.c, immediately
above its original INCLUDE_ASM. No layout or active header changes are needed.
The mainLoop callgraph confirms reachability; gameLoop passes _gameData,
object->unkB8, and &_gameData->base.unk2FC. m2c was used as a semantic draft.
The requested residual-analysis-2026-08-28.md file is absent.

## Assembly-proven corrections to the previous draft

- Count, actor cursor, geometry, and nullsub_12's argument come from _gameData;
  the main actor and final effect updates use the incoming GameData pointer.
- The line pointer is selected first, then separately tested for NULL.
- The rectangle test uses the saved unexpanded left/top against upper limits,
  and the width/height-expanded right/bottom against lower limits. Each sprite
  needs distinct bounds snapshots across calls.
- Main actor x > minimum x AND y > minimum y selects the lower-frame arm;
  the z comparison also selects that arm.
- Frame changes use each overlapping sprite's var22, gated by signed frame
  comparisons, rather than repeatedly adding/subtracting three from frame.
- The countdown tests the old counter at the loop bottom after an initial
  decrement. The target sprite is updated only if the main frame changes.

## Residual

Target length 0x294; retained draft length 0x2A6 (18 extra bytes).
Both allocate 0x34 bytes of stack. Spill slots match: incoming pointers at 0/4,
count at 8, bounds at C/10, minima at 14/18/1C, main actor at 20, target sprite
at 24, geometry at 28, callback data at 2C, second top at 30.
First differing instruction is the literal load at +0x12 (pool +0x90 instead
of +0x88). First non-pool divergence is +0x28: target ldr r7,[r1], draft
ldr r1,[r1] followed by mov r8,r1. The actor cursor remains in r8 rather than
r7, adding moves throughout the loop. This is not a matching implementation.
The asm-annotated .lreg/.greg outputs were inspected; the cursor has STACK_REG
preference and the x bound occupies r7. No register pinning, volatile, raw
field offsets, flags changes, or artificial assembly were used.

## Measured experiments

| Change | Result |
| --- | --- |
| Correct semantics, separate scalar snapshots | 0x38-byte frame; first difference +0xA |
| Scope second rectangle's locals separately | Frame shrinks to target 0x34 |
| Order persistent declarations by target spill lifetime | Target stack offsets recovered; cursor still r8 at +0x28 |
| Snapshot each endpoint coordinate before minimum selection | Corrects point0/point1 load order; cursor residual persists |
| Scope geometry and sprite locals inside active-actor arm | Same first cursor divergence and +18 bytes |
| Separate coordinate temporaries for each axis | Same cursor divergence |
| Hoist lineSprite NULL initialization before geometry | Changes sprite/frame high registers at +0xE; rejected |
| Move lineSprite NULL initialization immediately before bounds | Cursor still r8 |
| Hoist object NULL initialization | Cursor still r8 |
| Positive outer count guard instead of early return | Cursor residual unchanged |
| Post-decrement in initial count guard | Adds decrement before test and compares against -1; rejected |
| Early continue plus cursor increment in loop condition | Cursor residual unchanged |
| Signed count instead of unk32 | Cursor residual unchanged; kept unk32 |
| Byte overlap flags instead of word flags | Cursor residual unchanged; kept unk32 |
| Boolean assignment instead of conditional flag store | Cursor residual unchanged |
| Cache rectangle limit coordinates | Cursor residual unchanged |
| Reuse first x local for second sprite's saved left | Cursor residual unchanged |
| Stage width call result through x | Cursor residual unchanged |
| Cache global GameData pointer | Cursor residual unchanged; removed |
| Separate initial actor pointer from cursor | Cursor residual unchanged; removed |
| Per-iteration actor alias | Adds spill and changes high registers at +0xE; rejected |
| Load currentSprite before its NULL guard | Cursor residual persists; shifts guard allocation |
| Fold point0 pointer alias | Changes initial register setup and geometry load order; rejected |
| Group bounds/minima into arrays | Frame 0x38 and changed spill placement; rejected |
| Preinitialize line to NULL instead of conditional selection | Moves initial literal pool to +0xDC; rejected |

Retained locals capture bounds across calls, endpoint values before shifts,
minimum coordinates, and long-lived actor/sprite/geometry state. The first
rectangle overlap value and second rectangle overlap value feed a separate
found variable to retain the target's result-copy shape. Signed coordinates
and frame are supported by blt/bge/bgt/ble and z's asr. No speculative fields
were introduced. Helper prototypes exist only inside the disabled draft.
