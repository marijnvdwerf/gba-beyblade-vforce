# sub_806123C

Matched at 0x0806123C in src/spritetext.c. Full ROM SHA1 passes.

## Types and layout

The target reads the frame index with ldrh at +0x18, so use the existing frame.word union member, not frame.b[0]. SpriteTextCleanup.unk2A is s8: the line advance at 0x08061438 uses ldsb, followed by lsl #8. Its existing writers allocFont and sub_80611EC remain instruction-identical. No layout changes or new unions were needed.

Width-only offset, advance, and position locals match. Signed scale and adjustment are required for asr #8 in coordinate scaling; unsigned trials changed the instruction at function offset 0x1A8 to lsr. Signed line_shift is required for asr #1 at +0x244. Making count unsigned shrank the frame from 0x2C to 0x28 and diverged at +0xA.

## Source shape

Keep separate current and cursor pointers for scanning and positioning. Read advance before testing the marked-space flag. Use direct text->unk8 expressions: agbcc caches and spills that field as a halfword itself. Likewise, reading the list head again for the final pass produces the target hoisted load; an explicit saved_first alias changed its order relative to the decrement.

The width expression is text->unk29 plus a conditional glyph width. Staging those operands in delta locals altered allocation and branch placement. Both alignment cases need their position accumulation in two statements. adjustment also needs its offset subtraction as a separate compound assignment before scaling.

The final switch clears line_shift in case 0, halves it in case 0x10, and retains case 0x20. Replacing the clear with an early return produced an otherwise exact function with opposite branch polarity at +0x23E and +0x240. Clearing the source state recovered those final two instructions.

## Fold checks

| Trial | Measured result |
| --- | --- |
| Fold child pointer | First difference +0x3A, changes pointer load order |
| Fold line-spacing delta | Byte-identical; removed |
| Fold advance into condition | First difference +0x94, changes field-load allocation |
| Fold widths pointer | Frame grows from 0x2C to 0x30 at +0xA |
| Fold char_width | First difference +0x14, changes load order |
| Fold font_width | Removes target ldrh at +0x22 |
| Merge position assignments | First changed instruction operand at +0xAA; pool placement also changes at +0x96 |
| Fold adjustment subtraction into scaling | Changes sub destination at +0x1A0 and removes mov at +0x1A4 |
| Remove redundant unsigned cast in wrap test | Byte-identical; removed |

Final instruction diffs are exact for sub_806123C, allocFont, sub_80611EC, showString, sub_8061824, and sub_8061844. Formatted source and full ROM compare pass. No rule-breaking shapes retained.

## Review measurements (2026-09-10)

### Signedness sweep

`SpriteTextCleanup.x` and `.y` are explicitly `s32` fields in `src/common.h`
(at offsets `0x00` and `0x04`). Changing either local independently from
`s32` to `unk32` was byte-identical, but the final declarations remain `s32`
to reflect the proven field types. The one-at-a-time trials were:

| Local trial | Measured result |
| --- | --- |
| `x: s32` -> `unk32` | Byte-identical; retain `s32` because `SpriteTextCleanup.x` is `s32`. |
| `y: s32` -> `unk32` | Byte-identical; retain `s32` because `SpriteTextCleanup.y` is `s32`. |
| `count: s32` -> `unk32` | ROM changed; first difference `+0x0A`: target `sub sp, #44`, trial `sub sp, #40`. The unsigned declaration changes the frame and is not merely a frame-size observation: it changes the generated function prologue. Retain `s32`. |
| `scale: s32` -> `unk32` | ROM changed; first difference `+0x1A8`: target `asr r0, #8`, trial `lsr r0, #8` (a later difference also occurs at `+0x1CC`). Retain `s32`. |
| `line_shift: s32` -> `unk32` | ROM changed; first difference `+0x244`: target `asr r3, #1`, trial `lsr r3, #1`. Retain `s32`. |
| `adjustment: s32` -> `unk32` | ROM changed; first difference `+0x1A8`: target `asr r0, #8`, trial `lsr r0, #8`. Retain `s32`. |

The final source uses `s32` for `x`, `y`, `count`, `scale`, `line_shift`, and
`adjustment`; `font_width`, `offset`, `advance`, and `position` remain `unk32`
where the unsigned trial does not establish signedness.

### Fold trials

| Trial | Measured result |
| --- | --- |
| Fold `advance` into the line-break condition | Kept `advance`. First difference `+0x94`: target `ldrh r1, [r5, #30]`; trial `ldrh r2, [r5, #30]`. |
| Introduce a named `width` local for the readable line-break expression | Kept the inline expression. First difference `+0x1E`: target `ldr r3, [r1, #4]`; trial `ldr r7, [r1, #4]`. |
| First `while (count-- != 0)` loop -> `while (count != 0)` with decrement in body | Kept postfix decrement. First difference `+0x0A`: target `sub sp, #44`; trial `sub sp, #40`. |
| Second `while (count-- != 0)` loop -> explicit entry test with decrement in body | Kept postfix decrement. First difference `+0x0A`: target `sub sp, #44`; trial `sub sp, #48`. |
| Fold `position` into alignment-case-1 `adjustment` | Kept `position`. First difference `+0x11E`: target `ldr r0, [pc, #44] (->0x14C)`; trial `ldr r0, [sp, #40]`. |
| Fold `position` into alignment-case-2 `adjustment` | Kept `position`. First difference `+0x15A`: target `ldr r0, [pc, #44] (->0x188)`; trial `ldr r1, [sp, #40]`. |
| Factor the duplicated position accumulation before the switch | Kept the duplicated case-local accumulation. First difference `+0x18`: target `mov r10, r0`; trial `str r0, [sp, #4]`. |

The final `sub_806123C` instruction diff is exact after restoring every trial.
