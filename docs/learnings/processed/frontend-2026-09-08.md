# Frontend matching notes (2026-09-08)

## sub_8048FCC (0x08048FCC)

The function is a direct accessor for `_unk3000650`; `return &_unk3000650` emits the target literal-pool `ldr` followed by `bx lr` exactly. The caller `sub_8043604` uses the result as a `FrontendState*`, confirming the existing prototype and struct type.
