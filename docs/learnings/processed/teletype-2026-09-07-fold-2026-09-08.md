# Teletype small functions

- `sub_8063CD0`: direct `TeletypeState` fields `unkAC`, `unkB0`, `unkE`; `unkE` is a byte because the matched sequence is `ldrb`, `lsl #8`, add, and word store. `unkBE` is a byte because the increment matches `ldrb`, add, `strb`.
- `sub_8063DAC`: the countdown form with separate `count` and `temp` locals, a `SpriteEntry *sprite` cursor, and the trailing `sub_8063AB4` call matched the complete target. The target stores `0xA000` through `SpriteEntry::y` and advances through `SpriteEntry::next`.
- `sub_8063DAC` natural-loop sweep: `while (count-- != 0)` first diverged at `0x02` (`mov r3, r0` versus `mov r2, r0`); `for (count = state->unkEC.count; count != 0; count--)` first diverged at `0x00` (target saved `r4`, candidate did not); guarded `do/while` with `if (count-- != 0)` first diverged at `0x02` (`mov r3, r0` versus `mov r2, r0`). The separate `count`/`temp` loop was retained.
- `sub_8063DAC` alias/allocation sweep: candidate A (`SpriteTextBlock *block = &state->unkEC` with `while`) first diverged at `0x02` (`mov r3, r0` versus `mov r4, r0`); candidate B (A plus hoisted `unk32 y = 0xA000`) first diverged at `0x04` (target `add r0, #236`, candidate `mov r4, #160`); candidate C (A with `for (; count-- != 0; sprite = sprite->next)`) first diverged at `0x02` (`mov r3, r0` versus `mov r4, r0`). None matched, so the direct field accesses and separate `count`/`temp` loop remain.
- `sub_8063DF0`: `FontStyle` is a 12-byte record whose first word is a `const SpriteSheet*` and whose second word is a `const unk8*`; bytes at offsets `+8` through `+B` remain padding because matched code does not access them. The target uses `ldr r2, [r1, #0]`, then `ldrb` through `r2 + 4` and `r2 + 5`; it uses `ldr r1, [r1, #4]` and stores that word into state offset 8. The matched `sub_8063BA8` assembly later does `ldr r0, [r7, #0x8]`, adds an indexed byte-table offset, and dereferences with `ldrb`, proving `FontStyle::unk4` and `TeletypeState::unk8` are `const unk8*`. The first pointer targets the proven six-byte `SpriteSheet` header, with `unk4` and `unk5` read as bytes for state offsets `C1` and `C2`; the typed `dialogue.c` call passes `&FontStyle_80688B8`, preserving the original address value. `TeletypeState` fields at offsets 4, 8, C, D, E, C1, and C2 were added at their proven widths; the complete target matched without casts.
- `sub_8064140`: direct halfword mask/merge on `unkBA` and pointer increment on `unk0` matched the literal-pool target, including the `0xFFE7` word.
- `nullsub_16`, `nullsub_21`, and `nullsub_22`: callback-compatible parameter lists all compile to the target `bx lr`.
- `TeletypeState` bytes from offsets `0x0F` through `0xAB` are one padding region; only offsets proven by matched functions remain named.
- The matched functions above required no artificial cast, volatile qualifier, or raw offset expression. `sub_8063DAC` retains its measured transcribed loop shape, and `sub_8063D38` is parked below.

- `teletypeDefaultUserCodeHandler` (0x228-byte target span, offsets `0x000` through `0x227`): the initial natural draft differed only at the two argument moves before `sub_8055B64` at offsets `0x160` and `0x162`; the target is `mov r0, r5` then `mov r1, r4`, while that draft reversed them. The final source implementation matches the complete target and has no parked draft or assembly inclusion.
- `teletypeDefaultUserCodeHandler`: replacing the B64 second argument `sideA` with the direct expression `code == 0x61` changed the prologue to save `r7`, changed the dispatch value register, and grew the function from `0x220` to `0x228`; replacing the first argument `effects` with `_unk3000EE8` inserted a literal-load pair at the B64 block and shifted the remainder by 6 bytes.
- `teletypeDefaultUserCodeHandler`: swapping declarations of `effects`/`sideA` and independently swapping `sideA`/`sideTemp` left the same first divergence at `0x160`; scoping `sideA` inside a case block also left the same two-instruction divergence. The only exact shared-tail form in that early draft used a `goto`; it was rejected as an artificial source shape and is recorded only as history.
- `teletypeDefaultUserCodeHandler`: the ordinary switch fall-through shared-tail form emitted a second contiguous `sub_80640F8(state)` at `0x11A`, where the target branches to `0x208`; the if/else-if shared-tail form first diverged at `0x0C` because its conditional chain moved the literal pool and dispatch layout. Both natural forms were rejected.
- `teletypeDefaultUserCodeHandler`: placing the complete two-call `sub_80640F8(state)` arm before the complete one-call `sub_80640F8(state)` arm, with the remaining command arms between them, produced the target cross-jump with no divergent instruction or literal-pool entry.
- `teletypeDefaultUserCodeHandler`: changing `sub_8055C18` to return `unk8` and testing it directly with `!= 0` preserved the target's `lsl #24` return normalization and matched the complete function.
- `teletypeDefaultUserCodeHandler`: the corrected `TalkingHead` fields are `unk18`/`unk20` as `const unk8*` sprite-sheet pointers and `unk1C`/`unk24` as `const unk16*` palette pointers. The selected `right`/`top` values and the fourth `sub_8055C04` parameter use the corresponding pointee types; removing field-read casts matched the complete function.
- `teletypeDefaultUserCodeHandler`: folding `right` and `top` directly into the `sub_8055C04` call with two conditional expressions matched the complete function. A single if/else selecting both fields first diverged at `0x02` (`mov r4, r0` target versus `mov r5, r0` candidate) and was rejected.
- `teletypeDefaultUserCodeHandler`: removing the redundant `(unk8)` cast from `pushTeletypeStringPalette(state, value)` matched the complete function.
- `teletypeDefaultUserCodeHandler`: changing `command` to `s32` changed `0x0A` from target `lsr r2, r0, #24` to `asr r2, r0, #24`; restoring `unk32 command` and using `command & 0x80000000` matched the complete function.
- `teletypeDefaultUserCodeHandler`: replacing `value = 0 - value` with `value = -value` matched the complete function.
- `teletypeDefaultUserCodeHandler`: replacing the `sideA` zero/conditional/one temporary sequence with `sideA = code == 0x61` matched the complete function.
- `teletypeDefaultUserCodeHandler`: folding the single-use selectors into `sub_8055B7C(effects, code == 0x62)`, `sub_8055BA0(effects, code == 0x76, value)`, `sub_8055BC0(effects, code == 0x71, 1)`, and `sub_8055BC0(effects, code == 0x75, 0)` each matched the complete function.
- `teletypeDefaultUserCodeHandler`: changing the selector formal of `sub_8055B7C` from `unk32` to `unk8` matched the complete function.
- `teletypeDefaultUserCodeHandler`: changing the selector formal of `sub_8055BA0` from `unk32` to `unk8` matched the complete function.
- `teletypeDefaultUserCodeHandler`: changing the selector formal of `sub_8055BC0` from `unk32` to `unk8` matched the complete function.
- `teletypeDefaultUserCodeHandler`: changing the selector formal of `sub_8055C18` from `unk32` to `unk8` matched the complete function.
- `teletypeDefaultUserCodeHandler`: replacing the scalar `_unk3000EEC.unk0` updates with a layout-preserving union and bitfields first diverged in the prologue, changing `push {r4, r5, r6, lr}` to `push {r4, r5, lr}` and the command-code register from `r2` to `r3`; it also emitted byte accesses instead of the target word mask/merge operations. The scalar mask/shift expressions were retained.

- `sub_8063D38` (0x8063D38, parked): the typed parser draft preserves the target semantics (skip `{`, optional `/`, optional command byte, decimal accumulation, `-` sign bit, stop after `}`, store and return the cursor), but the natural draft did not match register allocation. Its first divergence was target `mov r6, r1` at `0x02` versus candidate `mov r7, r1`; the candidate then used `r1` for the cursor, `r4` for sign, `ip` for code, `r5` for value, and `r6` for the loop flag instead of target cursor `r3`, sign `r4`, code `r7`, value `r1`, and loop flag `r5`. The target and natural candidate spans were both `0x72` bytes.
- `sub_8063D38` near-miss sweep: adding an `output = command` alias retained the first divergence at `0x02` and introduced the target-independent code pseudo in `ip`; using the input parameter itself as the cursor changed `0x02` to target `mov r6, r1` versus candidate `mov r4, r0`; initialized declarations, scalar declaration permutations, pointer-first declarations, an output-array parameter, a return-pointer alias, and pointer conversion did not recover the target allocation; post-incrementing body reads moved the cursor to `r3` but added a separate load/increment shape and still left `0x02` as target `mov r6, r1` versus candidate `mov r7, r1`. Plain `while` versus guarded `do/while` did not remove the allocation divergence. No casts or volatile qualifiers were retained in the parked draft.
- `sub_8063BA8` (0x8063BA8, 0x128-byte target span): the final source uses a measured `process` flag initialized to 1, direct zero stores to `unkB4`, `unk12`, and `unkBF`, an output-buffer alias, a zero local initialized after the buffer alias, direct `state->unkB4` updates, and a `while (process != 0)` switch. A prior `widthCursor = &state->unkB4` candidate was byte-identical but was removed as an unnecessary scalar-field pointer alias. The terminal set is `0`, `9`, `10`, `12`, `0x20`, `0x7B`, `0xFE`, and `0xFF`; `0x3C`/`0x3E` only advance `unk0`; the remaining glyph cases update width and append, with the special path adding a terminator and returning.
- `sub_8063BA8`: `buffer[state->unk12++] = character` is required for the target byte-index normalization (`lsl #24; lsr #24`) and exact old-count/increment register roles. The equivalent separate `index = state->unk12; state->unk12 = index + 1; buffer[index] = character` form omitted the normalization and differed from the target.
- `sub_8063BA8`: initializing the zero local before the three state resets first assigned zero to `r6` and produced a different initialization sequence. Writing the resets with literal zero and initializing `zero` after `buffer = state->unk1C` produced the target `r9` zero role and exact prologue/init sequence. Folding the single-use `width` alias into `widthCursor = &state->unkB4` remained byte-identical.
- `sub_8063BA8`: retaining a local `table = byte_807D980` produced `add r1, r6, r3` in both glyph paths, while direct `byte_807D980[character]` produced the target `add r1, r3, r6` without any other instruction difference. The final direct-global source matches the complete function; no cast, volatile qualifier, raw offset expression, or artificial control-flow shape is used.
- `sub_8063BA8` lever sweep: replacing the never-modified `process` flag and `do/while (process != 0)` with `for (;;)` removed the target's `mov r0, #1`/`mov r8, r0` loop state and changed the prologue at `0x02` (`mov r7, r9` target versus `mov r7, r8`); the source retained `process`.
- `sub_8063BA8` lever sweep: replacing the `zero` temporary with literal `0` in both terminator stores changed the prologue at `0x02` (`mov r7, r9` target versus `mov r7, r8`) and removed the target's saved zero register role; the source retained `zero`.
- `sub_8063BA8` lever sweep: folding `widthCursor = &state->unkB4` into direct `state->unkB4 += ...` was byte-identical; no instruction changed. The pointer alias was not byte-required and was removed.
- `sub_8063BA8` lever sweep: replacing `state->unk0[0]` with `*state->unk0` was byte-identical; no instruction changed. The direct dereference was retained.
- `sub_8063BA8` process-realization sweep: changing both return arms to `process = 0; break;` while retaining `do { ... } while (process != 0)` did not match. The first divergence was at `0x02` (`mov r7, r9` target versus `mov r7, r8` candidate); the candidate also materialized `mov r6, #0` where the target later uses `mov r9, r0`, and its switch/epilogue layout shifted. The return arms and measured `process` lifetime remain unchanged.


- `sub_8063AB4` (0x8063AB4, 0x24-byte target span): the direct-field sprite cursor plus separate `count` and `temp` countdown loop matched completely. Folding the old-value test into `if (count-- == 0)` first diverged at `0x00` because the candidate saved `r6` (`push {r4, r5, r6, lr}` instead of `push {r4, r5, lr}`) and grew from `0x24` to `0x2C` bytes.
- `sub_8063AD8` (0x8063AD8, 0x6C-byte target span): the staged `normalizedPalette`, `data`, `x`, `y`, and `currentPalette` locals matched completely in the original source form. Folding `normalizedPalette` inline first diverged at `0x0A` (`lsl r5, r1, #24` target versus `mov r6, r1`), and folding `data` first diverged at `0x02`; both candidates shrank to `0x60` bytes. Folding `x` first diverged at `0x20` (`add r0, #172` target versus `add r0, #176`), while folding `y` and `currentPalette` individually was byte-identical; the final source keeps only those two direct folds.
- `sub_80640F8` (0x80640F8, 0x0C-byte target span): the direct `sub_8063CD0(state)` wrapper matched the complete target without a temporary or alternate control-flow form.
- `pushTeletypeStringPalette` (0x2C-byte target span): the direct bounded stack increment/store and overflow `printf` path matched the complete target; the `unk8` palette parameter and indexed `unk14` store required no casts or staging workaround.
- `sub_8064130` (0x10-byte target span): the direct conditional `unk13` decrement matched the complete target; no temporary or alternate branch form was retained.

| `sub_8063D38` parked-parser change | First divergence | Size result |
| --- | --- | --- |
| Natural typed semantic draft | `0x02`: target `mov r6, r1`, draft `mov r7, r1` | `0x72` versus `0x72` (`+0x00`) |
| Add `output = command` alias | `0x02` remained `mov r6, r1` versus `mov r7, r1`; code pseudo also entered `ip` | No match; candidate span remained `0x72` |
| Use input parameter itself as cursor | `0x02`: target `mov r6, r1`, candidate `mov r4, r0` | No match; candidate span remained `0x72` |
| Initialized declarations, declaration permutations, pointer-first declarations, output-array parameter, return-pointer alias, pointer conversion | Allocation divergence remained at the entry register roles; none recovered target cursor `r3`, sign `r4`, code `r7`, value `r1`, and flag `r5` | No match; no size change was retained |
| Post-increment body reads | Cursor moved toward `r3`, but the separate load/increment shape remained; `0x02`: target `mov r6, r1`, candidate `mov r7, r1` | No match; candidate span remained `0x72` |
| Plain `while` versus guarded `do/while` | Entry allocation remained divergent at `0x02` | No match; candidate span remained `0x72` |

- `sub_8063BA8` shared-glyph-path experiment: routing the special punctuation labels through one default width/append/advance body and conditionally storing the terminator produced a first divergence at `0x02` (`mov r7, r9` target versus `mov r7, r8` candidate), changed the switch allocation, and shrank the function from `0x128` to `0x0EC` bytes (`-0x3C`). The duplicated special/default bodies are therefore retained.
- `sub_8063BA8` loop sweep: `do { ... } while (1)` first diverged at `0x02` (`mov r7, r9` target versus `mov r7, r8`) and was rejected. The top-tested `while (process != 0)` form was byte-identical and is the final loop spelling; the process flag remains required by the target allocation.
