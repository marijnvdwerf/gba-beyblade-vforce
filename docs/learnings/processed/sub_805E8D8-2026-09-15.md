# sub_805E8D8 (0x0805E8D8, src/camera.c) — MATCHED

Previously parked (processed/geometry-camera-2026-09-05.md) with bare-scope
coordinate aliases and a one-add residual that only `(layerBase - (index * -1))`
fixed. Neither of those is in the matching source.

Two things recovered the source:

1. **The "layer 0" test is a pointer compare, not `index != 0`.** Target:
   `str r5, [sp,#0x18]` (copy of camera) … `add r0, r5, index*0x88; str [sp,#0x24];
   cmp r0, [sp,#0x18]`. Source: `first = &camera->records[0];` before the loop and
   `&camera->records[index] != first`. `index != 0` = 150 rows; comparing directly
   against `camera->records` = 17 rows (the separate spill slot disappears).

2. **`offsets` points to a struct with an embedded pair array.** x/y loads are
   `add r0, r4, i*8; ldr` and `add r0, r4, #0; add #4; add i*8; ldr`. Both
   `s32* offsets` with `[index*2]`/`[index*2 + 1]` and a plain `CameraLayerOffset*`
   with `offsets[index].y` fold the +4 into `ldr [r0,#4]` (40 rows each).
   `CameraLayerOffsets { CameraLayerOffset layers[4]; }` accessed as
   `offsets->layers[index].y` emits the separate add, and with it the whole
   function matches (246/246). This is the same embedded-array shape as
   `ScreenLayout.layers[i].unk14` in sub_805EBCC.

With (1) and (2), the old draft's scoped aliases (`xBase`, `yBase`, `layer`,
`layerBase`) are unnecessary: direct `level->layers[index].unk0` in the test and in
both calls matches.

Fold tests: `record` local folds (0 rows, removed); required are `first`
(17 rows), `origin` (107), `x`/`y` loaded at loop top before the presence test
(114). Keeping `record` for the else-arm call as well: 118 rows.

Signedness: `s8 index` (loop `asr #24`), `s8 flags` (final `lsl #24; asr #16`),
`unk16 mode` (entry `lsl/lsr #16`).

Header change: `CameraLayerOffset`/`CameraLayerOffsets` in camera.h;
sub_805E8D8/sub_805E8A0 take `CameraLayerOffsets*`. The only caller, sub_80539E8
(gameinit.c), is still asm; its parked `#if 0` draft passes `(unk32*)&values[3]`
and needs the new type when it is unparked.

## Fold-test pass (2026-09-15)

| Candidate | Result | First divergence |
| --- | --- | --- |
| `first` direct `&camera->records[0]` comparison | kept | `0x278` |
| `origin` direct `level->layers[0]` reads | kept | `0x278` |
| `x` inline fallback offset read | kept | `0x5E8E2` |
| `y` inline fallback offset read | kept | `0x5E8E2` |

All four probes were isolated and restored after compare. `first`, `origin`, and
both staged coordinate values are byte-required.
