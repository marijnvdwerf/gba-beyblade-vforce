# Layer matching notes — 2026-09-08

## sub_80594FC (0x080594FC)

The function is parked with a typed `#if 0` draft immediately above the retained dump include. The draft uses the seven-argument order proven by the target entry and the `LayerCopyFunc` callback type: `BGLayer *`, `x`, `y`, `srcX`, `srcY`, `width`, and `height`. No live header field or second layout type was added for the draft; every accessed member already exists in `BGLayer`.

The best draft reproduces the target `{r4, r5, r6, r7, lr}` prologue, saved `sl`/`r9`/`r8`, 20-byte local frame, stack slots at `[sp]`, `[sp, #4]`, `[sp, #8]`, `[sp, #0xC]`, and `[sp, #0x10]`, map-address arithmetic, DMA count calculations, wrapped and nonwrapped copies, DMA control readback, and loop arithmetic. Its first persistent divergence is at offset `0x10`: the target has `mov r9, r2`, while the draft has `mov r5, r3`. Consequently, the target uses `r9` for the `y * columnCount` multiply while the draft uses `r2`.

The target has an initial row-bound check before the DMA setup block. The natural top-tested loop draft places invariant DMA setup before that check, while the guarded `do/while` spelling moved the setup but increased the frame to 32 bytes and introduced additional stack temporaries. The 20-byte top-tested form was retained as the closest natural source shape.

| Source change | Measured result |
| --- | --- |
| Initial natural draft | 32-byte stack frame instead of the target 20-byte frame. |
| One shared `vu32 *dma` with indexed register writes | 20-byte frame; one DMA literal and target stack-slot layout recovered. |
| Keep a separate `secondCount` local | Preserved the closest allocation; inlining it worsened the diff. |
| Top-tested `while` loop | Kept the 20-byte frame and the persistent first divergence at offset `0x10`. |
| Guarded `do/while` loop | Moved DMA setup after the entry guard, but produced a 32-byte frame and substantial allocation differences. |
| `yValue` temporary placement and `unk32`/`s32` variants | Did not move the incoming `y` value to target `r9`; the first divergence remained at offset `0x10`. The parked draft uses the default wide `unk32` form. |
| Signed `y` parameter | Did not change the relevant allocation; not retained because signedness was not proven. |
| Move `mapAddress` initialization after mask setup | Deferred the map load past the target's offset `0x16` load and worsened ordering; the parked draft restores the early initialization. |

The draft's signed normalization of `fullCount` is retained because it generates the target `lsr #31`, `add`, `asr #1` sequence. The DMA control readback is represented by `(void)dma[2]`; `dma` points at the SDK DMA register address with a `vu32` pointee, preserving the target load after each control write.

## Review measurements

The parked draft reads `mapAddr` directly as a `void *` field and computes screen memory with the SDK `VRAM` macro; no cast is used on the `mapAddr` field read.

Replacing the indexed DMA alias with direct SDK register accesses through `REG_DMA3SAD`, `REG_DMA3DAD`, and `REG_DMA3CNT` produced a 28-byte stack frame instead of the target 20-byte frame. The first divergence is at offset `0x0A`, where the target emits `sub sp, #20` and the direct-register candidate emits `sub sp, #28`. The indexed `vu32 *dma` form is retained as the only tested spelling preserving the closest target frame and layout.

The natural signed-division spelling `((s32)width / 2) | DMA_ENABLE` for `fullCount` preserved a 20-byte frame but did not match. Its first divergence remained at offset `0x10` (`mov r9, r2` in the target versus `mov r5, r3`); the generated signed divide sequence was also moved earlier than the target's sequence at offsets `0x90` through `0x96`. The explicit normalization expression is retained.

Folding `yValue` into direct uses preserved a 20-byte frame but did not move the persistent first divergence at offset `0x10`; the `yValue` temporary is retained.

Folding `rowMask` into the row-offset expression reduced the frame to 16 bytes. The first divergence is at offset `0x0A` (`sub sp, #20` target versus `sub sp, #16` candidate); `rowMask` is retained.

Folding `horizontalMask` into the `srcX` mask preserved a 20-byte frame but changed setup and later mask arithmetic, with the persistent first divergence at offset `0x10`; `horizontalMask` is retained.

Folding `firstCount` into the wrapped DMA write preserved a 20-byte frame but changed count setup and allocation, with the persistent first divergence at offset `0x10`; `firstCount` is retained.

Folding `secondCount` into the wrapped DMA write preserved a 20-byte frame but changed count setup and allocation, with the persistent first divergence at offset `0x10`; `secondCount` is retained.

Moving the DMA control readback `(void)dma[2]` to a common tail after the wrapped/nonwrapped branches did not match. The existing offset `0x10` allocation divergence remained, and the shared-tail shape removed the wrapped-path `ldr [r2, #8]` at target offset `0xBE`, changing the branch layout. The branch-local readbacks are retained.

Hoisting the first two DMA writes before the wrapped/nonwrapped test did not match. The first divergence is at offset `0x0A`, where the candidate has a 28-byte frame versus the target's 20-byte frame; subsequent allocation and control-flow differences follow. The duplicated branch-local DMA writes are retained.
