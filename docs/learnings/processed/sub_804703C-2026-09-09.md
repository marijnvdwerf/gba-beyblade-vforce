# sub_804703C

Matched at 0x0804703C in src/festate.c. Reachable from mainLoop through sub_804712C. The caller and existing prototype pass FrontendBladeState*.

The asm/data12.s label previously named _80699DC was actually at 0x080699D8, on the preceding pointer word. Renamed that local label to _80699D8 and exported _80699DC at the real table start. The table ends at 0x08069CDC and has size 0x300: six rows of eight 16-byte records. Added a .size directive and a shared FrontendBladeAsset declaration in festate.h. No ROM data bytes changed.

Each record contains three accessed words at offsets 0, 4, 8 and a halfword at C; E–F remain padding. The source word at offset 8 is narrowed when stored to text->unkC. The existing signed state->unk35 matches ldsb and the signed loop counter matches ble. Other source record fields retain unk types because the shifts and plain loads do not prove signedness.

The existing do/while draft matches after correcting and exporting the table address. Folding the asset pointer into repeated table expressions fails the ROM SHA1, so it remains. The text local holds one sub_804A0E0 result for all four stores; substituting repeated calls would change behavior. No rule-breaking shapes.

After clang-format, instructions match exactly; the literal row differs only by symbolic relocation display. cmake --build build --target compare passes with ROM SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5.
