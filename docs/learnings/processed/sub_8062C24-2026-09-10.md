# sub_8062C24 — 2026-09-10

Matched at 0x08062C24 in src/sound.c. The mainLoop callgraph reaches it through sub_80627F0; the assembly caller passes no arguments and ignores the result. The existing disabled C caller agrees.

## Types and layout

- `_unk3005E08` is signed: target `ble` at 0x08062C3E and `bgt` at 0x08062E46 test its word value against zero. Its RAM placeholder is now s32, still four bytes. `_unk3005E10` retains unknown signedness and word width.
- The event cursor is an unk8 pointer. The first opcode and both note-on bytes use postfix cursor increments; separated reads/increments changed address lifetimes and instruction operands.
- SoundStructG adds only the accessed byte at +1 (`ldrb` at 0x08062CCE, `strb` at 0x08062D6E). Existing fields stay at +0 and +4; its stride remains eight bytes. SoundStructF's first word is a SoundStructE pointer forwarded to Sound_80629F0. Its four embedded channel records preserve the 0x24-byte stride.
- SfxTable.data is an array of SoundStructE pointers, proven by the +0xC table load in program change and the established Sound_8062BFC caller. Shared types reside in sound.h. Pointer globals retain four-byte storage.
- The note-off loop requires s32 count: changing only this declaration to unk32 replaces the old-value zero comparison at function +0x9A with a decrement/negative-one sentinel sequence. The two note-on searches use unk32 counters and naturally produce that sentinel sequence.

## Source shape and measured simplifications

- Use the high-bit branch with the switch first and delay processing in its else arm. An early continue with delay processing before the switch emits blocks in the wrong order.
- Folding the event-record alias to direct indexed fields removes unnecessary address temporaries. Case-local channel pointers remain the actual moving cursors.
- The note-on sample local is required: replacing it with repeated indexed reads introduces an r8 save at function +2. Both original null guards remain.
- The delay local is required: folding it into the timing update changes the first cursor address register at +0x1E from r3 to r6.
- The program-change byte local is required: folding its postincrement read into the table index changes the program-change block and its address from +0x1D0 to +0x1CC.
- The note bytes must survive cursor advancement and channel searches; the velocity byte must also survive Sound_80629F0 before Sound_8062AD4 consumes it.
- Reading the first two tempo bytes with postfix increments inside the shift expressions yields the exact target, including store ordering. The temporary assignments back to opcode both fold away byte-identically and were removed. The following two byte reads retain separate cursor increments.
- No raw field offsets, field casts, goto, volatile, inline assembly, or artificial scopes were added.

## Verification

Full ROM SHA1 passes after formatting. Instruction diffs have no differing rows for sub_8062C24, Sound_8062BA8, Sound_8062BFC, Sound_8062B2C, and allocateSoundTables. The target dump is removed. tools/update-expected correctly declines to update the shared expected symlink in this worktree.

## Measured fold tests (2026-09-10)

- Case 8 note-off `value8`: kept the local. Folding the comparison to the cursor read (while preserving the two-byte advance) diverged at function offset `+0x02`: target has no instruction there before the first literal load, while the trial added `mov r7, r8` as a prologue save. The direct-index and post-loop-advance variants both failed compare.
- Case 9 staged `found`: kept the flag. Replacing it with `if (i != 0) break;` first diverged at `+0xDC`: target has `mov r12, r3`, while the trial had no corresponding instruction and reached `cmp r2, #0` at `+0xDC`.
- Case 9 redundant `sample != NULL`: kept the guard. Removing it first changed the note-byte load at target `+0xC4` (`ldrb r7, [r0]`); the trial emitted `ldrb r1, [r0]` at `+0xC8` followed by `mov r8, r1` at `+0xCA`.

All three folds changed the ROM, so the original source was restored. Final `cmake --build build --target compare` passes.
