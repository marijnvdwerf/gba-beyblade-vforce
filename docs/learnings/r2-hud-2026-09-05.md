# HUD helper matching — round 2 — 2026-09-05

## sub_804F794 (0x0804F794)

Matched exactly with a `LevelHudData*` parameter. The four cleanup checks use `unkF4`, `unkF8`, `unkFC`, and `unk100` in order, then clear those fields in reverse order before masking bit 3 from `flags`. The four slots use the canonical `SpriteEntry*` type shared by the sprite APIs, with direct field arguments and no casts. The generated function is 0x6C bytes including the literal pool, with no instruction or ROM-byte differences.

## sub_804F37C (0x0804F37C)

Matched exactly with a `LevelHudData*` parameter. The function lazily allocates the four `SpriteEntry*` slots, then loads each corresponding sheet in order and sets bit 3 of `flags` with a halfword read-modify-write. The four sheets are `SpriteSheet_8225414`, `SpriteSheet_82266B4`, `SpriteSheet_8227954`, and `SpriteSheet_8227C24`, with x values `0x4A00`, `0x8A00`, `0xA00`, and `0x4200`, respectively; all other sheet arguments are zero except y `-0x2000`. The generated function is 0xFC bytes including the literal pool, with no instruction or ROM-byte differences.

## Review shape experiments

Folding the reused `SpriteEntry*` temporary in `sub_804F37C` matched byte-for-byte, so the direct field arguments are retained.

Replacing the four allocation/load arms in `sub_804F37C` with pointer-array/table iteration changed the prologue, frame size, control flow, and literal-pool layout; the explicit named-field form is retained.

Replacing the four cleanup arms and reverse clears in `sub_804F794` with pointer-array iteration changed the prologue, loop control flow, and store order; the explicit named-field form is retained.
