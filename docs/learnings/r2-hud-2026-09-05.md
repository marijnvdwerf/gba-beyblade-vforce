# HUD helper matching — round 2 — 2026-09-05

## sub_804F794 (0x0804F794)

Matched exactly with a `LevelHudData*` parameter. The four cleanup checks use `unkF4`, `unkF8`, `unkFC`, and `unk100` in order, then clear those fields in reverse order before masking bit 3 from `flags`. The fields retain their `LevelHudObject*` declarations used by the already-matched HUD animation helpers; each cleanup call uses a `SpriteEntry*` pointer conversion required by the shared sprite cleanup prototype. The generated function is 0x6C bytes including the literal pool, with no instruction or ROM-byte differences.

## sub_804F37C (0x0804F37C)

Matched exactly with a `LevelHudData*` parameter and one reused `SpriteEntry* sprite` local. The function lazily allocates the four `LevelHudObject*` slots, reloads each slot into the local before its corresponding `LoadSpriteSheet` call, and sets bit 3 of `flags` with a halfword read-modify-write. The four sheets are `SpriteSheet_8225414`, `SpriteSheet_82266B4`, `SpriteSheet_8227954`, and `SpriteSheet_8227C24`, with x values `0x4A00`, `0x8A00`, `0xA00`, and `0x4200`, respectively; all other sheet arguments are zero except y `-0x2000`. The `LevelHudObject*` slots require conversion to the shared `SpriteEntry*` call parameter. The generated function is 0xFC bytes including the literal pool, with no instruction or ROM-byte differences.
