# sub_80526C8

The semantic C body is parked in `src/gameloop.c` above the active assembly include.

## First divergence

The first divergent instruction was the stack allocation at function offset `0x0A`.
The target allocates 52 bytes, while the closest natural typed draft allocated 40 bytes.
The target also keeps the second and third incoming pointers in the target's expected
register/stack arrangement (`r9` for the sprite and a stack slot for the actor), while
the draft coalesced several values and kept the sprite in a lower register.

## Step table

| Change | Result |
| --- | --- |
| Initial semantic draft with opaque fixed-stride environment and geometry overlays | Buildable after adding local declarations; frame was 32 bytes and several accesses had incorrect semantic widths/locations. |
| Replaced geometry records with fixed-stride typed `GeometryLine` and `GeometryPoint` accesses | Corrected the 0x20-byte line stride and 0x10-byte point stride; frame grew to 40 bytes. |
| Added explicit environment x/y/z fields and separate line sprite from final target sprite | Corrected the no-line fallback and final sprite behavior; frame remained 40 bytes and register roles still differed from the target. |
| Used separate saved callback, count, geometry, main actor, transformed coordinates, and secondary-coordinate locals | Reduced semantic ambiguity, but the target's 52-byte frame and exact lifetime shape were not reproduced. |
| Restored the assembly include and reverted draft-only `Actor.unk3C` header change and declarations | Full ROM compare restored to 100%. |

The draft uses local fixed-layout overlays for offsets proven by the target, but it remains
disabled because ordinary natural-source variants did not reproduce the target frame or
register allocation. No draft-only fields remain in shared headers.
