# Effects round 2

## sub_80558B8 (0x080558B8)

Matched with a local `ProjectileSystem *` alias to `_gameData->projectileSystem`; direct member access folded the global and field offset into one literal (`_gameData + 0x10AC`), while the target materializes `_gameData + 0x1084` and stores at field offset `0x28`.

## sub_80558D0 (0x080558D0)

Matched with the same local `ProjectileSystem *` alias as `sub_80558B8`; the target stores `0x20` at `projectileSystem + 0x28`.

## sub_80558E8 (0x080558E8)

Matched with direct typed field assignments in each switch arm. A shared `unk16 *` field temporary produced the selector in `r1`; assigning `projectileSystem.unk72` and `projectileSystem.unk70` directly preserves the target selector in `r2` and matches the full function.

## sub_8055734 (0x08055734)

Matched with `Actor *` parameters and typed `Actor` coordinate fields. The eight resource records at `0x08078A08` through `0x08078DF8` were exported from `asm/data9.s` as real data symbols so C could reference them; this changes symbol visibility only and leaves the ROM bytes unchanged. The generated instruction stream matches the target; diff relocation names are display-only differences.

## Review shape checks (2026-09-06)

- `sub_80558B8`: kept the record alias; direct member folding changed the target store from `+0x28` to offset zero.
- `sub_80558D0`: kept the record alias; direct member folding changed the target store from `+0x28` to offset zero.
- `sub_8055734` first coordinate source: kept split arms; selecting one source alias changed the prologue, dispatch registers, and branch layout.
- `sub_8055734` second coordinate source: kept split arms; selecting one source alias changed the null-test branch shape and shortened the function.
