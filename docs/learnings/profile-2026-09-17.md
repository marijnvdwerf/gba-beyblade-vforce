# profile

## sub_80431E8 (0x080431E8)

- The `unk8` parameter naturally produced the target entry normalization (`lsl #24` / `lsr #24`).
- A `switch` with explicit `case 0` and `default` produced the target comparison tree; omitting `case 0` produced a balanced `case 1/2/3` tree instead.
- No local temporary was required; direct timer writes and `_unk3000150[0]` matched exactly.
