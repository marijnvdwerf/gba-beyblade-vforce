# def_94_8_collision_8055F2C — 2026-09-18

Matched the collision filter at 0x08055F2C in src/collision.c. The registered ActorCollisionFilter signature supplies the existing unk8 return and actor, geometry, line, and unk16 mask parameters.

## Behavior and interfaces

The filter selects the lower and upper line endpoints by z. Types 0x8C and 0x9A apply signed height bounds and dispatch metadata object type 6. Type 0x8C additionally checks RiderHasFlag before accepting the line. Type 0x8D applies an unsigned distance threshold before the default collision path. The default path either adjusts the actor height and rider->unk88 or calls one of the two collision-result helpers.

Added the existing processMetadata_6 signature to event.h and included that header in collision.c. No structure layouts or shared field types changed. The original assembly inclusion was replaced in place and its dump removed.

## Measured source shape

- Endpoint ternaries match. Initializing lower to point1 and then conditionally overwriting it moves `mov r10, r4` from +0x2E to +0x2A, before the endpoint z loads.
- Each event case needs its own signed height local. Sharing one local changes `mov r4, r0` at +0xCA into `mov r2, r0` and prevents the target tail sharing. Case-local declarations with the same name preserve exact bytes.
- The target normalizes these signed heights with `lsl #16` / `asr #16` and uses signed positive/negative tests. Type 0x8D instead loads the halfword with `ldrh` and immediately shifts it left three bits. Keep its unk16 distance local: folding the signed line->unk16 field into the expression replaces the +0x190 `ldrh` with signed-load setup and changes five diff rows.
- Nested metadata success guards duplicate the event tail, growing the function by 0x78 bytes. Its first instruction divergence is +0x12C: the target branches directly to the shared zero return, while the draft needs an inverted conditional plus a long branch. Early returns for an invalid line index, missing metadata, and missing event recover the exact backward-shared tail naturally.
- Changing switch failures and terminal returns to breaks can merge the cases too early, moving the 0x9A case before the shared height tests. Separate signed height locals and early returns match without goto or forced control flow.
- Combining the two positive-range failure tests, introducing scaled-height/lower-z snapshots, and nesting the outer acceptance guards did not recover the target sharing. The metadata early-return shape was decisive.
- Folding rider removes the initial actor +0xB4 address calculation at +0x16 and changes subsequent accesses. Folding either endpoint or either selected endpoint increases the stack frame from 0x30 to 0x34 at +0x0A. Retain these record aliases.
- The unreachable return after the exhaustive switch folds away byte-identically and was removed.

No raw offsets, casts, volatile qualifiers, fake guards, or assembly barriers were needed. US and EU full-ROM SHA1 checks pass. The expected symlink already contains the original matching baseline and was not rewritten outside the worktree.
