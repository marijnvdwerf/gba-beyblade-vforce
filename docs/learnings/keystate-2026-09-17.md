# keystate

## sub_805AB6C (0x805AB6C)

- The target loads `_unk3005DB8` with `ldrh`; the loop decrement and zero test are at `+0x30` (`mov r0,r4`, `sub r4,#1`, `cmp r0,#0`).
- Changing `count` from `s32` to `unk32` in `while (count--)` first diverges at `+0x02` and compares the decremented value against `-1`; `unk16 count` first diverges at `+0x00` and adds setup instructions.
- The natural `s32 count` with `while (count--)` matches; changing only `count` to `unk32` first diverges at `+0x02` and compares the decremented value against `-1`, which is signedness evidence.
- `if (++lineCount == 30)` and the split increment/compare both match under the final loop shape; the folded form is retained.
- Equivalent `for` loop spellings also matched in isolation; no byte requirement justified replacing the natural `while (count--)` form.
- `if (lineCount++ == 29)` also matches; the requested pre-increment spelling is retained.
