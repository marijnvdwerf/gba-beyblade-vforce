# frontend matching notes — 2026-09-17

## sub_8049240 (0x08049240)

- The function returns `_unk3000650.unkC` with signature `unk32 sub_8049240(void)`.
- The existing `FrontendState::unkC` `unk32` field emits the target word load at offset `0xC`; no temporary is required.
- `bun run tools/diff/diff.ts sub_8049240` reports every instruction equal, and the US ROM SHA1 compare passes.

## sub_80491C4 (0x080491C4)

- The function has no callers; its first two ABI parameters are unused and the third is a `SpriteTextCleanup*`.
- `text->unk8 |= 8` emits the target halfword read-modify-write at offset `0x8`; the existing `unk16` field is sufficient.
- Calling `sub_8061684(text, 0x140, 0x120)` emits the target `0x140` and `0x120` argument construction and call.
- `bun run tools/diff/diff.ts sub_80491C4` reports every instruction equal.

## sub_8049210 (0x08049210)

- The function takes one `unk32` index, clamps it against `_unk3000650.unkB4->unk4->unk1C` with an unsigned comparison, adds one, and stores the byte in `_unk3000650.unk7D`.
- `FrontendSubobject::unk1C` replaces the former four-byte padding at offset `0x1C`; the `unk32` field preserves the existing layout and emits the target word load.
- The direct local `count` and source statement order reproduce the target address materialization, clamp, and byte store without casts or artificial control flow.
- `bun run tools/diff/diff.ts sub_8049210` reports every instruction equal, and the US ROM SHA1 compare passes.
- Review probe: folding `count` into both clamp expressions was byte-identical; the local is not required.

## sub_8048F80 (0x08048F80)

- The function parses consecutive decimal digits from an `unk8*`, returning `-1` when the first byte is outside `0` through `9`; `unk32` is sufficient for both the return and accumulator.
- An `unk8 digit` temporary assigned from `*cursor - 0x30` is byte-required for the target's `lsl #24`/`lsr #24` normalization before each unsigned digit-range comparison; expression casts are not needed.
- The loop shape `result *= 10; result += *cursor - 0x30; cursor++; digit = *cursor - 0x30;` is byte-required: it emits the target product update, separate product-minus-`0x30` temporary, `ldrb` into `r3`, and three-operand add. Combining the accumulator expression or splitting the subtraction changes the first divergent instruction.
- Folding the cursor alias into the `string` parameter is byte-identical; the alias is not required.
- Spelling the digit base as `'0'` instead of `0x30` is byte-identical.
- `bun run tools/diff/diff.ts sub_8048F80` reports every instruction equal, and the US ROM SHA1 compare passes.

## Review probe: sub_80491C4 (0x080491C4)

- Omitting the names of the first two ABI parameters is rejected by the compiler (`parameter name omitted`), so the named `arg0`/`arg1` form remains the only accepted declaration tested.

## sub_8049074 (0x08049074)

- The function takes a `BGLayer*`, advances `_unk3000650.unk84` and `unk88` toward `unk8C` and `unk90` through `sub_80491E0(delta, 0x80) << 5`, then stores each value shifted right by eight into the layer's signed halfword fields at offsets `0x48` and `0x4A`.
- `FrontendState` fields `unk84`, `unk88`, `unk8C`, and `unk90` must be `s32`: the target emits `asr #8` for both stores, while the all-`unk32` trial emitted `lsr #8`. Retyping these fields left `sub_8049018` and `sub_804903C` byte-identical.
- Direct compound assignments and direct field stores match the target with no temporary aliases or casts. `bun run tools/diff/diff.ts sub_8049074` reports every instruction equal.
