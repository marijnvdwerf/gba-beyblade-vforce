# actor matching notes — 2026-09-17

## sub_8058478 (0x08058478)

- The matched signature is `void (Actor*, unk32)`; it compares the current frame offset against the destination sequence frame count, then calls `ActorSetFrame` or `ActorSetFrameSequence`.
- Keeping separate `currentSequence` and `sequenceData` pointers makes both `GetSpriteSheetStructA` calls occur before the frame subtraction and matches the target register allocation and instruction order.
- `ActorSetFrame` needs an `unk16` third-parameter declaration: the target caller emits `lsl #16`/`lsr #16` before the call. The existing sprite-string caller remains byte-identical with this shared prototype.
- `bun run tools/diff/diff.ts sub_8058478` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_80582D0 (0x080582D0)

- The matched signature is `void (Actor*, unk16, unk16, unk16)`; the three halfword parameters are normalized at entry and used as sequence, frame, and callback sequence.
- A separate `frames` pointer assigned from `entry->frames` is byte-required for the target frame lookup address order; folding it into `entry->frames[frame]` changes the first differing instructions.
- `bun run tools/diff/diff.ts sub_80582D0` reports every instruction equal.

## sub_8057FDC (0x08057FDC)

- The matched signature is `s32 (Actor*, s32)`; signed `index` and a signed local copy of `SpriteSheet->unk8` are required for the target `blt` bounds check.
- Hoisting `frameOffset = sequence->unk0` before the bounds check is byte-required; the target loads the halfword before branching while forming the frame cursor only after the checks.
- `bun run tools/diff/diff.ts sub_8057FDC` reports every instruction equal.

## ActorSetFrame (0x080583DC)

- The matched signature remains `void (Actor*, unk32, unk16)`; the `unk16` frame argument produces the target entry `lsl #16`/`lsr #16` normalization.
- Loading `flags` inside the frame-count bounds branch is byte-required; placing it before the branch adds an extra `ldrb` on the out-of-range path.
- The `frameData != NULL` guard is byte-required. Removing it changes the frame-data pointer register allocation and removes the target's null-test branch.
- `bun run tools/diff/diff.ts ActorSetFrame` reports every instruction equal.

## sub_8058794 (0x08058794)

- Separate `entries`, `cursor`, and selected `entry` pointers are byte-required. The cursor/index initialization order produces the target `r2` index and `r1` cursor allocation; reusing one pointer changes the register roles and first divergence.
- `ActorTimerCallback` is shared by `ActorTimerEntry`, `sub_8058794`, and the two spritestring forwarding functions; typing the callback parameter removes the previous function-pointer cast without changing code generation.
- `bun run tools/diff/diff.ts sub_8058794`, `sub_806570C`, and `sub_8065760` report every instruction equal.
