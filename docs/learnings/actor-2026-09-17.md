# actor matching notes — 2026-09-17

## sub_8058478 (0x08058478)

- The matched signature is `void (Actor*, unk32)`; it compares the current frame offset against the destination sequence frame count, then calls `ActorSetFrame` or `ActorSetFrameSequence`.
- Keeping separate `currentSequence` and `sequenceData` pointers makes both `GetSpriteSheetStructA` calls occur before the frame subtraction and matches the target register allocation and instruction order.
- `ActorSetFrame` needs an `unk16` third-parameter declaration: the target caller emits `lsl #16`/`lsr #16` before the call. The existing sprite-string caller remains byte-identical with this shared prototype.
- `bun run tools/diff/diff.ts sub_8058478` reports every instruction equal, and the US ROM compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## sub_80582D0 (0x080582D0)

- The matched signature is `void (Actor*, unk16, unk16, unk16)`; the three halfword parameters are normalized at entry and used as sequence, frame, and callback sequence.
- A separate `frames` pointer assigned from `entry->frames` is byte-required for the target frame lookup address order; folding it into `entry->frames[frame]` changes the first differing instructions.
- `bun run tools/diff/diff.ts sub_80582D0` reports every instruction equal.
