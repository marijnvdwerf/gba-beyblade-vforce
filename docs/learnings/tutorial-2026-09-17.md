# tutorial matching notes — 2026-09-17

## sub_804A7F8 (0x0804A7F8)

- A source-wide search found no existing record that embeds a `BGLayer` followed by a `BGLayer*` at offset `0x88`; the only such declaration was the review-blocked `TutorialLayer`.
- The neighboring `804a744.s` dump independently reads the same pointer at `arg + 0x88` and then reads its `characterBaseBlock` at `+0x5D`, confirming the pointer field's offset and `BGLayer*` type without establishing an owning C record.
- `functions.csv` identifies the function as `GPT::UploadBackgroundAssetToVram`; the record is therefore named `BackgroundAsset` and models bytes before the pointer as `unk8 pad0[0x88]`.
- Changing only the record name and replacing the overlay with padding leaves all target instructions equal in `bun run tools/diff/diff.ts sub_804A7F8`; US compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

## unref_804A744 (0x0804A744)

- The signature is `unk32 (BackgroundAsset*, unk8, unk8, FrontendFontData*)`; the target's non-void epilogue requires the `unk32` return type even though the source has no return statement.
- `BackgroundAsset` is a `BGLayer` at offset 0 followed by a `TilemapTextRenderer` at offset `0x88`; the target writes the renderer through `sub_805B244` and later reads its `unk0` pointer. Retyping the record this way leaves `sub_804A7F8` byte-identical.
- The `unk16 value` temporary is reused for every tile-map iteration; direct typed fields and the `unk16` temporary emit the target's byte normalization, tile loop, and `strh` sequence without field-access casts or extra aliases.
- `bun run tools/diff/diff.ts unref_804A744`, `sub_804A7F8`, `sub_805B244`, and `sub_805B260` report every instruction equal. US compare passes with SHA1 `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.
