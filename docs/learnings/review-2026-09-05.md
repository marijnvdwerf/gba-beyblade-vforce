# Review: worktree-agent-a9511d69e5a1f22c3 — 2026-09-05

## BLOCKING

- `src/tutorial.c:111-115` — `timer`, `index`, `done`, and `counter` are all declared `s32`, but the learning note only calls the language-count comparison signed; the target's signed `bgt`/`bge` branches support `timer` and `index`, while equality/bitmask use does not prove signedness for `done` or `counter` — signed types require measured evidence and `unk32` is the default for locals — cite the timer/index branch instructions in `docs/learnings/tutorial-hud-2026-09-05.md`, and use `unk32` for the unproven `done` and `counter` (then recheck the match).
- `src/hud.c:78-80,98` and `src/ram.h:137` — the new `s32 maxY`, `mode`, `mode2`, and `status` declarations are not justified by signedness evidence in the learning note; the target has `asr` evidence for the coordinate/mode expressions and a signed `bgt` for status, but `mode2` is only equality-tested — signed types require cited `asr`/signed-branch/call-site evidence and unproven locals must remain `unk32` — add the measured dump citations for `maxY`, `mode`, and `status`, and change `mode2` to `unk32` unless a signed use is demonstrated.

## QUESTIONS

- `src/tutorial.c:130-173` — the `done` flag is initialized to zero immediately before an `if (done == 0) { do { ... } while (done == 0); }` wrapper — is a plain `while (done == 0)` the source shape a person would write while preserving the match?
- `src/hud.c:114-138` — cases 2 and 5 duplicate the complete mode/color-selection block — can the shared computation be expressed once without changing the matched instruction shape?
- `src/hud.c:114-117,131-134` — the repeated `>> 4`, `& 7`, `& 3`, and `>> 2` choreography looks like extraction of packed mode bits — should this be represented with a named bitfield or other typed representation, provided it remains byte-identical?

## NITS

- `docs/learnings/tutorial-hud-2026-09-05.md:44` — the note says the layout is rooted at `GameData.levelHud0`, but the reviewed branch now uses the nested `GameData.levelHud` member — update the stale field name.

# Review: worktree-agent-a60eb05dea4107e39 (festate) — 2026-09-05

## BLOCKING (resolved in f9e882d4 before merge)

- `src/festate.c:22-24` — local prototypes for `sub_8043DB8`, `sub_8043F40`, `sub_8048A74` → moved to new `src/festate.h`.
- `src/ram.h` — `FrontendFadeProgress` duplicated the 8-byte `FrontendScalarWithPad` → reuse for `_unk3000178`.

## QUESTIONS

- `((sub_8057C40() >> 4) & 3) == 0` vs `(sub_8057C40() & 0x30) == 0` — tested: the mask form changes bytes; shift form retained.

# Review: worktree-agent-ac51b9c4ccd73abf5 (geometry/camera) — 2026-09-05

## BLOCKING

- `src/common.h:669-673` — `LevelDesign.unk0`…`unk10` are accessed by no committed C nor the parked draft → `unk8 pad0[0x14]`.
- `src/geometry.c:17-19` — `allocateDynamicBoundingAreas`, `initQuadTreeNode`, `sub_805BDBC` declared locally → `src/geometry.h`.
- `src/camera.c:46` — parked draft uses raw `0x04000050` → `REG_BLDCNT`.
- `src/camera.c:73` — `(DisplayRecord*)state` overlay cast used to shape the match → typed comparison (`index != 0` / `&camera->records[index] != &camera->records[0]`).
- `src/camera.c:214`, `src/camera.h:45` — `sub_805EEE0` casts the typed `geometry` field to `unk32*` → return `LevelGeometryAddresses*` / `&arg0->geometry`.
- `src/geometry.c:275` — matched allocQuadTree uses `(unk8*)nodes + nodeBytes` → typed `&nodes[arg4]`.
- `src/layer.h:52`, `src/common.h:661` — tile-map pointer erased to `void*` → share `TileMapHeader*`.
- `src/camera.c:185` — parked sub_805EBCC draft accesses `camera->unk220->entries[i].unk14` but `unk220` is now `LevelDesign*` (no `entries`) → fix the draft's access path without speculative header fields.

## QUESTIONS

- `src/camera.c:75` — `(*((layerBase + index))).unk0` → `layerBase[index].unk0` / existing `layer` alias?
- `src/camera.c:88-89` — repeated shift pairs on `LevelDesign.unk74` look like packed fields → test a bitfield.

## NITS

None. Superseded `(layerBase - (index * -1))` form is absent; allocQuadTree dump deleted; camera dump directly below its draft.
