# Tutorial matching notes — 2026-09-07

## sub_804A378 (0x0804A378)

- The function takes an `unk32` table index and returns the corresponding entry from `_806A77C`; the table entries point to arrays of five `const unk8 *` strings.
- Declaring `_806A77C` as `const unk8 * const * []` and returning `_806A77C[arg0]` reproduces the target's literal load, scaled index, table load, and `bx lr` exactly.
- The prototype is declared in `src/tutorial.h` so the definition can remain after `sub_804A364`, preserving the target function emission order; the former local extern declaration was removed.
- No local temporary is required.
