# AI matching — 2026-09-21

## initAiManagement (0x080575F4): matched

Activated the typed draft, replacing the dump in its original position. The
canonical LevelDescription now exposes LevelGeometryTable* unk38 and
LineMetadata** unk3C instead of eight padding bytes; its 0xD0 size is unchanged.
GeometryLine.unk16 remains s16 for its established signed consumers. This
function needs an explicit unk16 conversion at each of its five reads: the
original uses ldrh and an unsigned range comparison. The m2c semantic draft was
regenerated using the locally cached tool. No live C caller was found; the
existing void(void) interface is now declared in ai.h.

The prior draft's two-register swap is resolved by copying ai into a second
AiState* local, management, immediately before initQuadTree. Setup uses ai;
the later line walk uses management. Moving that copy before geometry setup or
after initQuadTree fails. Removing it or reassigning ai itself restores the
register swap at +0x06 (target ldr r6, candidate ldr r7).

This duplicate pointer is a matching compromise under the user's permission for
light rule breaking, and is marked TODO: fakematch? above the function. The
per-read unsigned casts are another retained exception. No register keywords,
inline assembly, volatile tricks, or compiler changes are used.

### Fold pass

Each candidate was checked with the decomp-fold-temps runner against the full
US SHA1 test; every valid experiment below failed and was restored. Size deltas
include trailing alignment relative to the target's 0xF8-byte allocation. These
locals are required by these tested forms, not proven impossible to remove.

| Fold | Size delta | First divergence |
| --- | ---: | --- |
| gameData into _gameData | +8 | +0x04: literal-base load uses r5 instead of r0 |
| ai into gameData->ai | 0 | +0x06: game-data load uses r5 instead of r6 |
| management into ai | 0 | +0x06: game-data load uses r7 instead of r6 |
| Replace pointer copy with ai reassignment | 0 | Same register swap at +0x06 |
| level pointer into indexed LevelDescriptions accesses, retaining level ID | +24 | +0x02: additional high-register save setup |
| line cursor into geometry.unkC[i] | +4 | +0x50: additional mov r5, r7 |
| Remove all five unsigned conversions | +8 | +0xAA: signed-load address setup replaces ldrh |
| Remove range-check conversion only | +4 | +0xAA: mov r1, #22 replaces ldrh |
| Remove first printf conversion only | +4 | +0xB0: mov r2, #22 replaces ldrh |
| Remove lookup-index conversion only | +8 | +0x50: address-load register changes |
| Remove second printf conversion only | +4 | +0x50: address-load register changes |
| Remove store-index conversion only | +8 | +0xC0: ldrh destination changes r2 to r1 |

The cursor fold was rerun after correcting an invalid intermediate candidate;
the compiler-error trial is not evidence. The signed loop counter and output
array walk remain separate because the target uses blt and a 0x20-byte cursor
increment.

Other measured trials included an else-wrapped body, redundant table guard,
while/do-while loop forms, continue-based error handling, subobject aliases,
individual direct gameData->ai accesses, cached line IDs, and pointer-slot
aliases. None recovered the match without the phase-separated pointer copy.

Final formatted source passes both enforced US and EU ROM SHA1 tests. The
fn_aiStubFunc1 versus fn_aiStubFunc difference printed by objdiff is relocation
naming noise; the complete ROMs are byte-identical.

### Follow-up: canonical unsigned field

Retyped GeometryLine.unk16 from s16 to unk16 and removed all five conversions
from initAiManagement. The original mixed-use declaration is better represented
as unsigned storage with two explicit signed interpretations in sub_80561EC:
the comparison against rider->unk1C0 and the case-0x8C lower-height calculation.
Without these conversions, the comparison changes from ldrsh/bgt to ldrh/bhi
at +0x164, and the height read changes from ldrsh to ldrh at original +0x2C4.
The other signed height consumers already assign the field to s16 locals.

All compiled GeometryLine.unk16 consumers were checked: initAiManagement,
sub_80561EC, def_94_8_collision_8055F2C, initRiders, and sub_805E77C retain their
instructions. Full US and EU SHA1 checks pass. This supersedes the earlier
choice to keep the canonical field signed; the duplicate AI pointer remains.

### Follow-up: typed temporary in init

Tested restoring the signed field and removing the collision casts, then reading
it into a local in initAiManagement. An unk16 local supplies the desired implicit
unsigned conversion, but differs in six instructions starting at +0xAA (ldrh r2
instead of r0), as the compiler reuses the loaded ID. Explicit reload assignments
and distinct check/print/index temporaries retain those six differences. Using
unk32, s16, or s32 locals instead gives eleven differing instructions, starting
with signed-load setup at +0xAA. These trials were restored; the unsigned field
and two signed collision conversions remain the verified form.

### Follow-up: collision arithmetic and signed loads

Tested replacing the case-0x8C height expression with unsigned multiplication:
`(lowerC->z - line->unk16 * 8) * 32`, addition of `line->unk16 * -8`, and
`lowerC->z * 32 - line->unk16 * 256`. All fail at +0x2C4, where the original
requires signed-load setup. Multiplication with `(s16)line->unk16`, or assigning
into an s16 local before multiplication, passes the US SHA1 check; both still
emit the original signed load. Removing the signed comparison conversion also
fails (first allocation difference +0x152; ldrsh/bgt becomes ldrh/bhi). Loading
an s16 comparison temporary before the preceding condition instead adds an
early load at +0x152 and fails. Original source restored after these trials.

### Final choice

At the user's preference, restored GeometryLine.unk16 to s16, retained the five
explicit unk16 conversions in initAiManagement, and removed the two experimental
signed conversions in sub_80561EC. This restores the committed source shape;
the unsigned-field and arithmetic experiments above remain diagnostic history.
Both US and EU SHA1 comparisons pass with this final choice.
