# sub_8041344

Matched at 0x08041344 in src/levelselect.c. The function forwards its second
argument to sub_8061E58 for state->rows[1], then state->rows[0]. Existing
LevelSelectState fields suffice. MainLoop reachability and every C call site
were inspected; those callers pass 0 or 1.

The definition and header now take unk16 rather than unk32. Direct forwarding
to sub_8061E58's byte parameter reproduces every instruction, including the
row load before lsl/lsr #24. No local, cast, or layout change is needed.

| Source shape | First difference |
| --- | --- |
| Previous draft: unk32 argument, unk8 local | +0x4: conversion precedes row load |
| unk32 argument forwarded directly | +0x4: extra mov r4,r1 |
| Assignment to narrow or wide color inside first call | +0x4: extra mov r4,r1 |
| First row pointer staged before color conversion | +0x4: extra mov r4,r1 |
| unk8 formal forwarded directly | +0x4: conversion precedes row load |
| unk16 formal forwarded directly | Exact instructions and full ROM SHA1 |

This is measured parameter-width evidence; the #24 pair alone did not prove
a byte formal. Callers remain byte-identical after the header change.
After clang-format, compare passes SHA1
cd527c8c24e20e33913fc45199e64b3e6138a6e5. No rule-breaking shapes remain.

## Parameter-width measurement

The definition and header were compiled at each candidate width against the
existing target object. `sub_8061E58` itself takes an `u8` color parameter.
All five call sites in `sub_80413FC` pass constants 0 or 1 and remained
byte-identical for every candidate.

| Second parameter | Function first divergence | Caller changes |
| --- | --- | --- |
| `unk32` | +0x4 | none |
| `s32` | +0x4 | none |
| `unk16` | exact | none |
| `s16` | exact | none |
| `u16` | exact | none |

The widest matching width is 16 bits; with no signedness evidence, the
canonical `unk16` declaration is retained.
