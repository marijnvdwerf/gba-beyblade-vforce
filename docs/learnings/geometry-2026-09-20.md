# Geometry matching notes — 2026-09-20

## sub_805E77C (0x0805E77C)

The direct line-record search with the established `s16 GeometryLine::unk16` emits `ldrsh` for the identifier comparison. A masked `(line->unk16 & 0xFFFF)` trial also failed and introduced a literal pool. The exact ordinary form uses an `unk16 identifier` local in the short-circuit RHS, `(identifier = line->unk16) != id`, preserving the target's type-first branch and emitting `ldrh`; removing the local changes the load to `ldrsh`. The comma spelling without the assignment parentheses is byte-identical. Assigning `identifier` as a standalone statement at loop top adds `r6` to the push mask and diverges at the type-load sequence. The tested `for` loop diverges at `0x0805E77E` (`mov r4,r0` is delayed) and changes the loop CFG. The local is byte-required and no artificial cast, union, attribute, volatile, register pinning, or raw offset is used. The previously tested packed signed/unsigned union also matched but was rejected and is not retained.

## sub_805E0D8 (0x0805E0D8)

The matching source keeps a typed `unk32* pointIndices` base, assigns it after the null guard, and indexes it for the two endpoint records. This yields the target address sequence `mov r0,r1; add #0x20; lsl segment,#2; add`. A direct `spline->pointIndices + segment` assignment was hoisted before the null test; direct indexed expressions computed the scaled index before adding the spline base.

A signed `s32 yDelta` is byte-identical to the prior `unk32 yDelta` form; no unsigned instruction or branch evidence requires `unk32`, so the signed declaration is retained. The target register roles and stores require `s32 xDelta`, `s32 lineXDelta`, `s32 lineYDelta`, `s32 denominator`, and `s32 fraction`, plus `s32 intersectionX` and `s32 intersectionY` temporaries. Folding the latter directly into the two result stores first diverges at the y-delta loads around `0x0805E108` and changes the interpolation register allocation, so the temporaries remain. With those natural lifetimes, the function matches exactly. No artificial casts or register-steering constructs are needed.

## sub_805E528 (0x0805E528)

The semantic draft uses typed GeometryPoint pointers for the input endpoints and reuses them for each candidate line, a GeometryLine cursor, normalized endpoint flags, a narrow loop index, signed line count, and cached axis flags. It remains parked because the first divergence is the prologue frame: target `sub sp,#16`, `mov r5,r0`, normalized flags in `r9`, point base at `[sp,#4]`, and line count at `[sp,#8]`; the best natural draft emits `sub sp,#20`, assigns the address argument to `r6`, spills normalized flags, and uses different pointer registers. Direct candidate indexing, separate candidate pointer aliases, reuse of endpoint pointers, signed and unsigned line-count locals, and a wide flags argument with a narrow local were tested. The 20-byte frame and register/address-materialization divergence persisted. No prohibited casts, volatile, register pinning, or raw offsets were used.

## sub_805E648 (0x0805E648)

The semantic draft adds the fifth stack argument as an `unk8` side selector and filters candidates by bit 1 of `GeometryLine::unk11` before applying the same endpoint-mask logic as `sub_805E528`. The dump's target prologue has a 20-byte frame and exact normalized side-argument shifts; the natural C draft diverges first at the prologue with a 24-byte frame and different register/stack placement. The target extracts the side bit with `lsl #30` followed by `lsr #31`, while the draft's direct shift-and-mask expression emits `lsr #1` and `and #1`. The draft remains parked above the asm include. No prohibited constructs were used.
