# sub_80596AC — 2026-09-12

Matched at 0x080596AC in src/layer.c (0x258 bytes). Main checkout, branch raw-decomp-7.

The union approach below was superseded by the follow-up byte-local experiment in `sub_80596AC-byte-local-2026-09-12.md`. The final layout uses a plain halfword field and a byte local.

## Semantics and types

The routine advances fixed-point background coordinates, copies newly exposed tile strips, wraps map bounds, and updates hardware offsets. All real C callers pass BGLayer pointers; signed scrolling deltas are explicit in collection, credits, level selection, and frontend callers. The declaration now takes BGLayer*, s32, s32.

Struct3000CA0 keeps all six members as s32. Direct signed branch evidence in sub_80596AC is per-field:

| Field | Signed instruction evidence |
| --- | --- |
| var00 | 0x0805973C: `428C cmp r4, r1`; 0x0805973E: `DA07 bge.n` |
| var04 | 0x0805976C: `42A6 cmp r6, r4`; 0x0805976E: `DA0A bge.n` |
| var08 | 0x08059728: `4288 cmp r0, r1`; 0x0805972A: `DD06 ble.n` |
| var0C | 0x08059754: `428B cmp r3, r1`; 0x08059756: `DD09 ble.n` |
| var10 | No direct ble/bge in sub_80596AC; signed caller/dataflow evidence is sub_8058968's xTile producer at 0x0805898A (`10C5 asrs r5, r0, #3`) and store at 0x080589B6 (`6125 str r5, [r4, #16]`). |
| var14 | No direct ble/bge in sub_80596AC; signed caller/dataflow evidence is sub_8058968's yTile producer at 0x0805898E (`10CE asrs r6, r1, #3`) and store at 0x080589B8 (`6166 str r6, [r4, #20]`). |

The signed xTile/yTile values are also passed as the signed source-coordinate arguments of sub_8059310. Actual agbcc layout assertions passed for the 24-byte Struct3000CA0 and BGLayer offsets 0x64 and 0x68.

BGLayer.var64 remains a plain unk16 field. In sub_80596AC, the `horizontal` local is unk8 and is assigned `bgLayer->var64 & 1`, producing the target byte load while other users retain the halfword view. The differing-width union probe is historical only: it was tried and removed in favor of this committed layout.

The old disabled draft tested updated xStart/yStart instead of the original viewport origin, passed xBase instead of map->var04 to the horizontal copy, and updated hardware coordinates before logical coordinates. Correcting these recovers the target dataflow. The vertical wrap condition really compares var04 against columnCount while var0C is compared against rowCount; preserved exactly.

## Measured source steps

| Change | Result |
| --- | --- |
| Correct semantics; use four-element signed bounds array | Correct 60-byte frame and instruction count; wrong stack slots and halfword read remained |
| Order declarations by bounds, source offsets, starts, adjustments, mode, exponent pointer, byte extents | Stack offsets match |
| Stage x/y shifted coordinates before assigning bounds[0/1] | Both asr instructions precede the bounds stores |
| Byte union access for mode | Target ldrb at +0x24 |
| Block-local height/width = 32 before each copy | BYTE-REQUIRED; target constant materialization at +0xEE and +0x19A |
| Fresh block-local map aliases after copies | Still first differs +0x106: r2 versus target r1 |
| Direct bgLayer->var8 accesses after copies | Exact instructions and passing ROM SHA1 |
| Fold initial map alias too | Byte-identical; removed |

## Temporary reduction

Each probe was built separately and checked against the matching ROM hash. Rejected probes were restored before the next probe.

- xBase REJECTED first divergence +0x66; folding `xBase` into `bounds[0]` made the second shift occur after the first bounds store
- yBase REJECTED first divergence +0x66; folding `yBase` into `bounds[1]` made the second shift occur after the first bounds store
- height/width literal arguments REJECTED; BYTE-REQUIRED(+0xEE). Replacing both block-local `unk32` temporaries with direct `0x20` arguments changed the first constant materialization; the retained locals still materialize at +0xEE and +0x19A.
- height RETAINED 349  0x000000ee    mov       r1, #32                |                                                     | +
- width RETAINED 349  0x0000019a    mov       r2, #32                |                                                     | +
- bounds-array removal REJECTED; BYTE-REQUIRED(+0x0A). Replacing `bounds[4]` with four scalar limit locals reduced the frame from 60 bytes to 44 bytes.
- bounds[2] = xBase + 0x1E RETAINED byte-identical
- bounds[3] = yBase + 0x14 RETAINED byte-identical
- map REMOVED exact
- horizontal REJECTED first divergence +0x0A; folding the byte local into the final mode test changed the frame from 60 to 52 bytes
- xOffset REJECTED first divergence +0x0A; folding the two branch assignments into the call conditional changed the frame from 60 to 56 bytes
- xStart REJECTED first divergence +0x0A; folding the two branch assignments into the call conditional changed the frame/register setup
- xAdjust REJECTED first divergence +0x0A; folding the magnitude into `yDelta > 0 ? yDelta : -yDelta` changed the frame/register setup
- yOffset REJECTED first divergence +0x0A; folding the two branch assignments into the call conditional changed the frame from 60 to 56 bytes
- yStart REJECTED first divergence +0x0A; folding the two branch assignments into the call conditional changed the frame from 60 to 56 bytes
- yAdjust REJECTED first divergence +0x0A; folding the magnitude into `yDelta > 0 ? yDelta : -yDelta` changed the frame/register setup
- horizontal REJECTED first divergence +0x0A; folding the byte local into the final mode test changed the frame from 60 to 52 bytes
- flags REJECTED first divergence +0x2E; folding the pointer into both mask expressions moved the target `str r4, [sp, #52]` to `[sp, #56]`
- xShift REJECTED first divergence +0x0E; folding the byte shift local changed the initial register allocation and subsequent mask setup
- yShift REJECTED first divergence +0x0A; folding the byte shift local changed the frame from 60 to 56 bytes

The bounds array reproduces four stack stores at +0x68 through +0x76. xBase and yBase stage the signed shifts before these stores. The retained flags pointer points to a struct member, not a scalar global. No volatile, register pinning, goto, raw offset access, or artificial scope is used.

## Verification

After clang-format, diff.ts reports zero differing rows for sub_80596AC, sub_8058968, sub_8058AA8, unref_8058C74, sub_8058EF4, and sub_8048FFC. These cover the active C users of the changed map fields and flag views. Disabled sub_8059310's flag spelling was updated consistently. The complete ROM compare passes: SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5. The assembly dump was removed after the successful compare.
