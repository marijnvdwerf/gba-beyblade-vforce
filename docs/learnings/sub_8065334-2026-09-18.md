# sub_8065334 — 2026-09-18

Matched at 0x08065334 in src/spritestring.c (0x7A bytes). US and EU full-ROM SHA1 comparisons pass.

The matching source uses a plain while loop with direct string->count and string->text accesses, and an Actor pointer computed at the start of each iteration. No volatile, field casts, raw offsets, or artificial scopes are required.

## Measured experiments

| Change | Result |
| --- | --- |
| Activate parked draft, correct total and scaleX signedness | 0x80 bytes; first difference at +0x02, saving r9 as well as r8. Actor-array base is hoisted. |
| Remove cached count only | Same 0x80-byte mismatch. |
| Replace guarded do/while with while, retaining cached text | 0x7A bytes; instruction sequence still differs. |
| Remove cached text from guarded do/while | Exact match. |
| Remove both cached text and count from guarded do/while | 0x72 bytes; prologue differs. |
| Plain while with direct text and count | Exact match; retained as the simpler source. |
| Fold actor local into repeated string->actors[index] accesses | 0x78 bytes; actor address/load scheduling differs. Actor local is byte-required. |
| Fold final total assignment into return expression | 0x78 bytes; first difference at +0x06, string argument retained in r3 instead of r4. Separate total assignment is byte-required. |

## Types and users

The target reads scaleX with ldrsh at 0x0806539C and shifts the product with asr at 0x080653A0. SpriteString.scaleX is therefore s16 and total/return are s32. Actor.unk22 already has the signed halfword declaration needed by the ldrsh at 0x0806537E. Index and textOffset remain unk16, matching their zero-extension pairs.

The other active C users of SpriteString.scaleX are sub_8064F9C and sub_8065088; both function diffs remain exact after the declaration change. Assembly callers in 8065108.s use the result as a width, including a signed right shift. No C call site requires a new prototype.

The mainLoop source callgraph does not list this function in this checkout; the function was explicitly selected by the user. The existing expected snapshot already contains the exact matched bytes and is a shared symlink, so it was not rewritten outside this worktree.
