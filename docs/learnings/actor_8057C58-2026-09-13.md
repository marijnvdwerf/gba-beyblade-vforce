# actor_8057C58

Matched at 0x08057C58 in src/actor.c. The function is reachable from mainLoop through the actor initialization callers. The formal parameters are Actor*, ActorConfig*, BGLayer*, unk32, unk32, unk32, unk32; rider.c, envactor.c, and event.c provide the typed configuration and background-layer call-site evidence. spritestring.c passes its resource pointer through the same configuration parameter.

The canonical Actor and ActorConfig layouts were extended only for fields accessed by this function. ActorConfig fields at 0x4, 0x5, 0x6, 0xC, 0x14 are byte/word loads matching the target, and Actor fields at 0x18, 0x2A, 0x2C, 0x38, 0x3A, 0x60, and 0x64 are represented directly. Actor.unk60 is unk16; no signed evidence required s16 there. The existing ActorCollisionCallbacks field is used for the 0x90 and 0x94 pointer clears. No raw offset dereferences or duplicate layout typedefs were required.

The target's three halfword all-ones operations require read-modify-write expressions (`actor->unk60 |= -1`, `actor->unk1A |= -1`, and `actor->unk2E |= -1`), producing the target ldrh/orr/strh sequences and shared 0xffff literal. Retaining this `|= -1` form follows the coordinator-approved session-8 precedent for s16 fields using `|= -1` when the target has ldrh/orr/strh. Pointer fields are cleared with typed NULL assignments.

The literal-valued temporaries `zero`, `zero8`, `minusOne`, and `scale` were each folded independently to direct literals. None was byte-required: every fold remained instruction-identical for actor_8057C58 and compare-green. The final source has no staged literal-valued temporary.

The byte-required initialization ordering is expressed by writing `actor->unk16 = 0` before the later direct zero-valued stores. This emits the target's `mov r5, #0`, `mov r4, #0` at function offsets 0x24 and 0x26 while preserving the later zero-field stores. Explicit zero-local assignments before the first field store emit the opposite order; declaration-time initialization changes earlier argument-register stores. The final source shape has no artificial control flow or compiler-specific matching lever.

Measured source changes:

| Source shape | Result |
| --- | --- |
| Explicit `zero8 = 0; zero = 0; actor->unk16 = zero` | First divergence at +0x24: emits `mov r4, #0` then `mov r5, #0`; all later instructions match. |
| Reverse assignment order and chained zero assignments | Same +0x24/+0x26 register-order residual; no improvement. |
| Declaration-time initialization of `zero` and `zero8` | Earlier divergence at +0x16, with the wrong argument register stored, followed by changed register allocation. Rejected. |
| `actor->unk16 = 0; zero8 = 0; zero = 0` | Exact function instruction diff and full-ROM compare before literal temporary folding. |
| Fold `zero` with `scale` retained | Exact function instruction diff and compare-green. |
| Fold `scale` after `zero` was folded | Exact function instruction diff and final compare-green. |

After clang-format and removal of the dump, `cmake --build build --target compare` passes. No speculative fields, raw offsets, casts on field accesses, volatile/register tricks, or artificial symbols remain.
