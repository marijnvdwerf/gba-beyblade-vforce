# sub_80627F0 — parked

Target 0x080627F0, 0x120 bytes, src/sound.c. MainLoop reachability confirmed;
all C callers use the existing void(void) signature during frame/wait loops.
The best draft remains under #if 0 above the active assembly inclusion.
No header or shared-layout changes were made.

The routine advances the sound ring buffer from timer 1, updates active
channels, mixes one or two spans, and wraps the write cursor. The old draft
incorrectly reused its cached cursor after the indirect mixer call. The
target reloads _unk3000D90 after that call; the draft now does the same.
The pointer-to-scalar position alias was removed. Direct global operations
produce the target pointer updates; audioCursor folded byte-identically.

## Unresolved sequence

Target +0x2A loads state->var08, adds one, and masks with -2. The result in r0
is discarded by the timer-register load at +0x38. A plain alignedFrameCount
assignment is removed by agbcc, losing the retained state pointer in r5 and
changing the early register assignment and literal pool. No justified source
form tested retained this calculation. No volatile fields, dummy register
accesses, scalar-global pointer aliases or compiler barriers were added.

The retained draft has 30 differing diff rows and is 4 bytes shorter than the
0x120-byte target. First differing encoded instruction is +0xA (literal pool
displacement 68 rather than 72); first register-role difference is +0x1A
(state address in r0 instead of r5). The target-only rounded-state read starts
at +0x2A. The mixer/cursor tail instructions match after correcting reloads.

## Controlled probes

Each probe was compiled separately from the baseline; rejected changes were
restored. Counts include literal-pool displacement and alignment differences.

| Change | Result |
| --- | --- |
| Initial active draft | Missing state calculation, cursor update/reload differences |
| Direct global cursor operations after calls | 36 differing rows |
| Fold audioCursor | Same 36 rows; removed |
| Signed channelCount | Same 36 rows |
| Countdown for or while instead of guarded do/while | Same 36 rows |
| Fold mixLength | 63 rows; first difference +0x2, saved-register set |
| Fold state alias | Same 36 rows |
| Reorder wrap addition to nextTimerPosition + bufferLength + 0xFFFF0000 | 30 rows; retained |
| Stage bufferLength | Same 36 rows from baseline |
| Nested enabled body instead of early return | Same 36 rows |
| Narrow alignedFrameCount to unk16 | 34 rows, still loses state calculation |
| Signed wrapDistance | Same 36 rows |
| Unused min/max against timer after rounding | Same 36 rows |
| Unused signed absolute-value correction after rounding | Same 36 rows |

The reuse-timer-local probe failed to compile due to its unused old local
and supplies no evidence. No conclusion is based on that diagnostic.

The review fold was compiled with the parked draft enabled and compared
against the dump object, not against the full ROM. Folding mixLength into the
mixer call first differs at +0x2 and produces 63 differing rows, so the
separate local is retained. No natural source use was found that preserves the
rounded state read at +0x2A; alignedFrameCount remains the best documented
parked probe rather than gaining a dummy use or alias.
The old referenced residual-analysis-2026-08-28.md is absent at its top-level
path; processed archives were not read.

Final clang-format and `cmake --build build --target compare` pass with the
original assembly active: SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5.
Changes are uncommitted on main in the main checkout.
