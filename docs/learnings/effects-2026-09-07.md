# Effects matching notes — 2026-09-07

## `sub_805599C` (`0x0805599C`)

- The final source uses the existing flat `EffectSprites` record and `SpriteEntry` fields. The pending sheet slots at `0x2C` and `0x34` are `const unk8*`, matching the sprite-sheet data passed to `LoadSpriteSheet`. The pending palette-source slots at `0x30` and `0x38` are `const unk16*`, matching the 16-bit palette data referenced by the talking-head records. The `CopyFn` source parameter was corrected to `const void*` so these read-only sources pass without qualifier diagnostics; the rebuilt callers retain their target instructions.
- The position and velocity fields at `0x08`, `0x0C`, `0x10`, `0x14`, `0x18`, `0x1C`, `0x20`, and `0x24` are `s32`. This produces the target arithmetic interpolation shifts (`asr #2` at function offsets `0x11A` and `0x126`) without casts on field expressions.
- The final bounds source uses `(unk32)(effect->unk8 + 0x4000) > 0x13000` and the corresponding expression for `unk10`. This produces the target unsigned `bls` checks at offsets `0x14` and `0x96` while retaining signed fields for interpolation. The alternative explicit signed-range form also produced an exact instruction match.
- The target palette copy calls use `__fastMemoryCopyARM` with `OBJ_PLTT` and `OBJ_PLTT + 0x20`; the second sprite's OAM flag is `(0x80 << 5)`, which emits the target `mov #0x80; lsl #5` sequence.
- The duplicated frame-update source is required. A two-iteration loop over the two sprites first diverged at function offset `0x62` because the loop shortened the body and moved the following helper address; it ended at `0x1A4` instead of the target `0x1C8` and also changed the frame-update register allocation. The explicit per-sprite blocks are retained.
- After the corrected types and final source shape, `sub_805599C`, `sub_8055914`, `sub_8055C30`, and caller `sub_80420C4` have no differing instructions. The full-ROM comparison passes.

### Source-shape trials

| Trial | First divergence | Observation |
| --- | --- | --- |
| Direct arithmetic with `EffectSprites` position fields as `unk32` | `0x11A` and `0x126`: `lsr #2` instead of target `asr #2`; second OAM flag emitted `lsl #1` | Unsigned fields selected logical interpolation and `0x100` did not preserve target constant materialization. |
| Position fields `s32` with the original unsigned-looking bounds expressions | `0x14` and `0x96`: target `bls`, trial `ble` | Signed fields fixed interpolation but changed the bounds branch conditions. |
| `s32 delta` temporary with position fields `unk32` | `0x118`: target `sub r0,r2`, trial `sub r1,r0,r2`; later second interpolation operands/registers also differed | The temporary changed the direct-expression allocation and was removed. |
| Position fields `s32`, computed-value unsigned checks `(unk32)(position + 0x4000) > 0x13000`, no field-expression casts | No differing instruction | Final bounds and interpolation source shape. |
| Position fields `s32`, explicit checks `position > 0xF000 || position < -0x4000` | No differing instruction | An equally exact alternative; computed-value form retained as the standard unsigned-range idiom. |
| `const unk8*` sheet fields, `const unk16*` palette fields, and `CopyFn` source `const void*` | No differing instruction in the corrected function; affected callers remained exact | Precise pointee types remove the prior `void*` fields and qualifier diagnostics without changing code generation. |
| Two-iteration frame-update loop | `0x62`: the loop moved the helper target because the generated function became shorter; frame-body register differences followed | The target is explicitly duplicated per sprite rather than loop-generated. |
