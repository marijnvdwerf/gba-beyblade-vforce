# Geometry collision matching 2026-09-08

## sub_805CEB8 (0x0805CEB8)

The function remains parked with the typed draft in `src/geometry.c` and the
ROM dump active. `cmake --build build --target compare` passed after restoring
the parked checkpoint.

The first body difference in the direct endpoint draft, excluding frame-size,
stack-offset, and register-name differences, was at target offset `0x4E`.
The target stores the per-line callback-done state to `[sp,#0x4C]`; the direct
draft kept that state in `ip` after the zero initialization (source
initialization near line 746). This accounts for the target's extra four-byte
frame slot. The endpoint load/order difference begins at target offset `0x9E`:
the target shifts each endpoint value before the raw endpoint ordering
comparisons.

Measured investigation steps:

| step | source shape | frame | measured result |
| --- | --- | --- | --- |
| 1 | split x-side bounds from the later full rectangle; raw endpoint locals | 92 | rider allocated to `r8`, line flags to `sl`, callback mask to `r9`; endpoint shifts still followed the comparisons |
| 2 | precomputed scaled endpoint min/max values and swap temporaries | 96 | endpoint values spilled before comparisons; not the target shape |
| 3 | four scaled endpoint temporaries | 96 | endpoint ordering became target-like, but rider moved to `r9` and line flags spilled |
| 4 | removed scaled endpoint temporaries; raw endpoint loads before comparisons | 92 | rider returned to `r8`; compiler still compared before the target shift sequence |
| 5 | four direct scaled endpoint locals with raw ordering checks | 96 | target-like endpoint load/shift order; rider was `r9` and line flags were stack-resident |
| 6 | swapped line-min/line-max declaration order in the four-temporary form | 96 | min/max storage moved opposite the target allocation; rider remained `r9` |
| 7 | direct scaled assignments followed by swap branches | 92 | endpoint loads were grouped, but callback-done remained in `ip` and y endpoint storage differed |
| 8 | branch-ordered endpoint assignments | 92 | compiler compared raw x values before loading the y endpoints, unlike target |
| 9 | branch-ordered endpoint assignments with `unk16 callbackMask` | 92 | no frame or callback-done improvement |
| 10 | direct scaled assignments with `unk16 callbackMask` | 92 | no frame improvement; callback-done remained in `ip` |
| 11 | reordered callback-mask and line-flag declarations | 92 | no frame improvement |
| 12 | narrowed callback-done local | 92 | no frame improvement; compiler still used `ip` |
| 13 | retained a scaled `y0` local | 92 | y endpoint storage changed, but callback-done remained in `ip` |
| 14 | retained scaled `y0` and `y1` locals | 92 | callback-done remained in `ip`; rider and y allocation still differed |
| 15 | restored the four-endpoint-temporary historical draft | 92 | rider moved to `r7`; worse than the parked direct draft |

The live `Actor` fields at offsets `0xA8`, `0xAA`, `0xAC`, and `0xAE` remain
`unk16`; the parked scratch view uses `s16` after target `ldsh` instructions
proved signed accesses. The live callback helper has no prototype; the parked
scratch declaration uses `call_rider_94_8(CollisionRiderDraft*,
LevelGeometryAddresses*, GeometryLineDraft*, unk16)`.

| 16 | address-taken callback4 experiment: pass `&callbackDone` as a fifth argument to the typed indirect callback and activate the draft | 96 | target frame is 96 bytes and draft frame is 96 bytes; first instruction divergence is target `mov r8, r0` versus draft `mov r7, r0` at function offset `0x0C`, followed by the draft's input stack slots shifted by 8 bytes |

# Geometry matching notes — 2026-09-08

## sub_805C3BC (0x0805C3BC)

The matched implementation uses `s32 values[6]` and a `s32* result`; the six-word local is required for the target's 24-byte stack frame. A five-word local produced a 20-byte frame and diverged at offset `0x2`; the six-word form matches the complete function. The helper calls pass the original position as the fourth argument, while the returned pointer is tested before applying the first three output values.

Actor fields `unk9A`, `unk9C`, and `unk9E` were changed from `unk16` to `s16` because the target uses `ldsh` for all three reads at offsets `0x3E`, `0x50`, and `0x62`; this preserves the complete function and does not alter the existing C writer `rider_8058614`. Actor offset `0x88` was added as a word field, replacing the prior five-byte padding region with `unk32 unk88` plus one-byte padding; the fixed offset of `unk8D` remains unchanged. No byte-required temporary remains beyond the six-word output array and result pointer.

## Review measurements

`sub_805DD18` takes a signed `s32` position parameter: its target instruction at offset `0x2A` is a signed `bge`, and offset `0x2E` is an arithmetic right shift (`asr`). Changing the declaration from `unk32` to `s32` preserved the already matched `sub_805C3BC` bytes.

Folding the `result` temporary into a conditional helper call did not match. The first divergence is at target offset `0x22`: the target branches directly into the common result test, while the conditional-call candidate emits `cmp r0, #0` followed by `bne` at offsets `0x22` and `0x24`. The `result` temporary is retained.
