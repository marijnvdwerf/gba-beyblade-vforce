# Leaf matching round 4b

## sub_8057048 (0x08057048)

Matched the language-indexed item-name lookup with an `unk32 language` local. `ItemNames[language][arg0]` returns the selected string pointer. The implementation is byte-identical to the target, including the post-call table load and separate scaled item index.

## sub_8056F7C (0x08056F7C)

Matched the collectable-state synchronization as a copy from `gameData->collectables.collectedBits` to the current level state's `unk10`. Keeping `_gameData` in a local before calling `sub_8051734()` preserves the target's callee-saved game-data pointer and the `0x13F8` field address. The implementation is byte-identical to the target.

## sub_804FE9C (0x0804FE9C)

Matched the level HUD counter refresh with a `GameData* state` local and a `SpriteTextCleanup* hud` alias for `text3`. The counter byte is stored from `sub_80518F0()`, then the same byte is displayed followed by the slash string and the current level number. The typed `LevelHudData` offsets emit the target's `0x107D` byte store and `0xFE0` text pointer without offset arithmetic. The implementation is byte-identical to the target.

## Review fold questions

- `sub_8057048` language fold: not byte-identical. Folding `getLanguage()` directly into `ItemNames[...]` makes `ItemNames` remain live across the lookup and adds an `r5` save/restore. The `language` local is retained.
- `sub_8057048` names fold: byte-identical. Folding `names[arg0]` into `ItemNames[language][arg0]` preserves the target, so the `names` local is removed.
- `sub_8056F7C` game-data fold: not byte-identical. Using `_gameData` directly for the source expression reloads the global after `sub_8051734()` and changes the literal/register sequence. The `gameData` local is retained.
- `sub_8056F7C` state fold: not byte-identical. Using `&sub_8051734()->unk10` adds an `r5` save/restore and changes the helper-address load order. The `state` local is retained.
