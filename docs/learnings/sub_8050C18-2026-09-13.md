# sub_8050C18 (0x08050C18)

Matched in src/iconmenu.c; ROM SHA1 cd527c8c24e20e33913fc45199e64b3e6138a6e5.

## Layout and widths

FrontendMenuData +0x20 is the signed horizontal scale factor (multiply followed by ASR #8). FrontendMenuItem +8/+C are signed coordinates, +0x10 is the signed current scale, and +0x14 is the signed target scale (signed difference followed by ASR #3). The item stride remains 0x1C.

FrontendMenu +8 is the signed angle (ASR #8), +0x18/+0x1C are signed interpolation positions (difference followed by ASR #3), and +0x20/+0x24 are signed timers (difference and BLE). Coordinate bases +0x34/+0x38 remain unk32. Existing menu size and offsets are unchanged.

FrontendMenuItemData +0x28/+0x2C are word fields. The frame toggle reads +0x2C with both LDR and LDRH, and reads +0x28 with LDRH. A narrow comparison local and a direct ternary assigned to the sprite halfword recover these loads without a union or field casts. Declaring +0x28 as a halfword removes the target branch/move at function offsets 0xFA/0xFC. A word declaration restores the exact sequence.

## Measured source shapes

- A byte frameOffset and separate phase expressions for the angle index and timer pulse recover register allocation. A shared wide index across both phases changes allocation starting at the table load at +0x2C.
- The x/y displacement locals in each selected-item movement arm are byte-required. Folding any of the four into a direct coordinate expression changes ROM bytes. The staged subtraction also restores sprite-coordinate load order and the shared y-store tail.
- Cached step, textPosition, sprite, and offsetY are byte-required: folding each separately changes ROM bytes.
- The final velocity is selected in a local before one store. Storing it before the sign test emits an additional store.
- Removed the menu alias, position temporary, alternate-frame temporary, timer-pulse local, table signedness casts, call argument casts, and final byte cast. Each simplification preserved ROM SHA1.
- Replaced the guarded infinite loop and bottom break with a plain while loop; preserved ROM SHA1.
- Position masking with &= 0xFFFF preserves the target LDRH/STR pair. No union or cast on a struct-field read is needed.
- Unsigned step/index locals and coordinate bases, and spelling the wrap correction as -= 0xFF00, preserve ROM SHA1.

## Verification

The target and all compiled affected menu users have zero differing instruction rows: sub_8050DF8, sub_8050E80, sub_8050F0C, sub_8050F98, sub_8050FE8, and sub_8050FEC. Full ROM compare passes after formatting. No rule-breaking matching shapes remain.


## Fold-test review

Each probe below was built independently from the preceding matching source and reverted on mismatch. The listed SHA1 is the probe ROM; the retained matching source is `cd527c8c24e20e33913fc45199e64b3e6138a6e5`.

| Probe | Result | First difference | Probe SHA1 |
| --- | --- | --- | --- |
| final offsetY folded into the y store | BYTE-REQUIRED | +0x0A | 3b3560046a575542c6c4d4500bbe8874d7ae6180 |
| high-scale dx folded into the x update | BYTE-REQUIRED | +0x2C | bde0f31aa7e3a8597316cd0d18a870dc28f87b4a |
| high-scale dy folded into the y update | BYTE-REQUIRED | +0x2C | 8b4e8e5a441ebe544412d4f759ed721797527bdf |
| low-scale dx folded into the x update | BYTE-REQUIRED | +0xB0 | 81fbc62c0b2e3abf20be4795839d28c72ea7d2de |
| low-scale dy folded into the y update | BYTE-REQUIRED | +0xA8 | c750d3860f8d57306399959e8f39708be80ca3eb |
| comparison snapshot folded into the ternary condition | FOLDED | none | cd527c8c24e20e33913fc45199e64b3e6138a6e5 |
| selected high/low movement arms rewritten as shared conditional updates | BYTE-REQUIRED | +0x20 | 6fd394e86a21e1756aaaf92f16b70d5ed761775c |

The four selected-item displacement temporaries and the duplicated high/low movement arms are retained. The comparison snapshot is removed.
